# 15. Next Steps

| # | 다음 액션 |
|---|----------|
| 1 | **SM(Scrum Master)으로 전환** → Story 1.1 분해 + 실행 준비 (`*agent sm`) |
| 2 | **PO로 전환** → PRD/Architecture/UX 정합성 검토 (`*agent po`) |
| 3 | **Dev로 바로 시작** → Story 1.1 구현 (`*agent dev`) |
| 4 | **추가 화면 와이어프레임 요청** (Admin 화면 등) |
| 5 | **AI UI 생성 프롬프트 추가 화면** 확장 (`*generate-ui-prompt`) |

---

**문서 끝.** 🎨
