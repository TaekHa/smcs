# Story 2.1: 이슈 등록 폼 (CS 접수자)

## Status: Review

> QA Gate (2026-05-18, Quinn — Test Architect): 🟡 **CONCERNS**. AC1–5,7 코드추적+정적+로컬테스트(crypto 단위 8/8, 프론트 51/51) 검증. AC6 end-to-end(DB 암호문 저장)·Hibernate `validate`·`LocalDataSeeder` 회귀는 Testcontainers 통합(로컬 Windows 환경한계 불가)로 **CI 검증 필수 — CI 런 부재**(미커밋). 결함 없음. **PASS 조건**: 브랜치 푸시 후 GitHub Actions 그린(신규 통합 2종 + 기존 LocalDataSeeder/Flyway 회귀). 비차단 관찰 N-1(level 파라미터 500)/N-2(번들 가드레일)/N-3(기존 axios 취약점) — 하단 QA Results 참조.

## Story

- As a **CS 접수자**
- I want **통화 중에 빠르게 신규 이슈 카드를 작성**하기를
- so that **엑셀에 수기 기록할 필요가 없다**

## Acceptance Criteria (ACs)

1. `/issues/new` 화면에 등록 폼이 한 페이지에 모두 표시된다(스크롤 가능).
2. 필수 필드: 제목, 발신자명, 발신자 전화번호, **카테고리 3단계(L1 대분류 / L2 중분류 / L3 소분류) 각각 드롭다운**, 우선순위(라디오/버튼), 상세 내용.
3. 우선순위 라디오는 색상으로 시각 강조됨(URGENT=red, HIGH=orange, NORMAL=blue, LOW=gray).
4. 저장 시 `POST /api/issues` 호출하고 성공 시 이슈 상세 페이지로 이동한다.
5. 단축키: `Ctrl+S` 저장, `Esc` 취소.
6. 발신자 전화번호/이름은 백엔드에서 암호화되어 저장된다.
7. 카테고리 L1/L2/L3 사이에 종속성을 강제하지 않는다 (모든 조합 허용).

## Tasks / Subtasks

- [x] **Task 1: 암호화 인프라 (`com.smcs.crypto`) — 본 스토리 최초 도입** (AC: 6)
  - [x] `AesGcmCipher` — AES-256-GCM. 매 암호화마다 랜덤 12-byte IV 생성 후 ciphertext 앞에 prepend. `byte[] encrypt(String)`, `String decrypt(byte[])`. 키는 `smcs.crypto.data-key`(Base64 32 bytes) 주입.
  - [x] `HmacHasher` — HMAC-SHA256. 입력에서 **숫자만 추출**(`replaceAll("\\D", "")`)하여 정규화 후 HMAC → **hex 문자열**(소문자, 64자) 반환. 키는 `smcs.crypto.hmac-key`(데이터 키와 분리).
  - [x] `EncryptedStringConverter implements AttributeConverter<String, byte[]>` — 엔티티 필드는 `String`, DB 컬럼은 `BYTEA`. `@Converter(autoApply = false)`, 엔티티 필드에서 `@Convert`로 명시 적용. JPA 컨버터는 Spring bean 이 아니므로 `AesGcmCipher` 를 정적 홀더(예: `CryptoSupport` 컴포넌트가 `@PostConstruct` 로 static 주입) 또는 Hibernate 6 bean-managed converter 로 연결 — 구현 방식은 dev 재량, **null 입력은 null 반환**(컬럼 NULLABLE).
  - [x] 키 설정: `application.yml` 에 `smcs.crypto.data-key: ${SMCS_DATA_KEY:}` / `smcs.crypto.hmac-key: ${SMCS_HMAC_KEY:}` 추가. `application-local.yml` 에 로컬 전용 고정 dev 키(Base64 32 bytes, 명백한 dev 값) 추가. `application-prod.yml.example` 에 두 키 항목 + "env 필수, Git 미포함" 주석 추가. [Source: architecture/6-security-architecture-defense-in-depth.md#6.4, #6.5; prd/9-coding-standards.md#9.1(시크릿)]

- [x] **Task 2: Category 읽기 도메인 (`com.smcs.category`)** (AC: 2, 7)
  - [x] `Category` 엔티티 — `categories` 테이블 매핑. 컬럼: `id`, `parent_id`(nullable), `level`(SMALLINT, `Short` 또는 `Integer`), `name`, `keywords`(JSONB — 본 스토리 미사용, `@Column(columnDefinition="jsonb")` 또는 무매핑 가능), `sort_order`, `active`. **V2 DDL 과 정확히 일치**(ddl-auto: validate). [Source: backend/src/main/resources/db/migration/V2__create_tables.sql L6–15]
  - [x] `CategoryRepository` — `findByLevelAndActiveTrueOrderBySortOrderAsc(short level)`.
  - [x] `dto/CategoryOption` (record: `id`, `name`, `level`) + `from(Category)`.
  - [x] `CategoryController` — `GET /api/categories?level={1|2|3}`, `@PreAuthorize("isAuthenticated()")`, 활성 카테고리를 sort_order 순으로 반환. **L1/L2/L3 종속성 없음**: parent 필터 없이 level 만으로 조회(V3 시드의 parent_id 는 모두 NULL). [Source: prd/epic-2-issue-core-workflow-week-2.md#Story-2.1 AC7; backend/.../V3__seed_categories.sql; **DEVIATION**: PRD §6 표엔 `/api/admin/categories`(ADMIN)만 존재 — 본 엔드포인트는 신규 추가, Deviation Analysis 참조]

- [x] **Task 3: Issue 도메인 (`com.smcs.issue`) + `POST /api/issues`** (AC: 4, 6, 7)
  - [x] `Issue` 엔티티 — `issues` 테이블에 **V2 DDL 과 정확히 일치**하게 매핑. `caller_name_enc`/`caller_phone_enc` (`BYTEA`) 는 `String callerName`/`callerPhone` 필드 + `@Convert(converter = EncryptedStringConverter.class)`. `caller_phone_hash`(`VARCHAR(64)`) 는 일반 `String` 필드(서비스에서 set, 컨버터 미적용). `priority`/`status` 는 nested enum + `@Enumerated(EnumType.STRING)`. FK(`category_l1_id/l2_id/l3_id`, `created_by`)는 MVP 단순화를 위해 `Long` 스칼라 매핑 권장(연관 엔티티 매핑 불필요). `created_at/updated_at` `Instant`, `@PrePersist` 로 set. 기존 `User` 엔티티 패턴(`@Table`, `IDENTITY`, `@Column(name=...)`, `protected` 기본 생성자, getter only) 준수. [Source: backend/.../User.java; V2__create_tables.sql L30–47]
  - [x] `Priority` enum (`URGENT, HIGH, NORMAL, LOW`), `IssueStatus` enum (`NEW, ASSIGNED, IN_PROGRESS, DONE, VERIFIED`). 신규 이슈 status = **`NEW`**.
  - [x] `IssueEvent` 엔티티 + `IssueEventRepository` — `issue_events` 테이블 매핑(`event_type` CHECK: `CREATED` 등). 본 스토리는 `CREATED` 만 기록. [Source: V2__create_tables.sql L79–88; architecture/8-sequence-flows.md#8.2]
  - [x] `IssueRepository`, `IssueEventRepository` (Spring Data).
  - [x] `dto/CreateIssueRequest` — `title`(@NotBlank @Size(max=200)), `callerName`(@NotBlank), `callerPhone`(@NotBlank), `categoryL1Id/L2Id/L3Id`(@NotNull), `priority`(@NotNull enum), `description`(@NotBlank — AC2 "상세 내용" 필수). Bean Validation. [Source: architecture/7-api-design.md#7.1(검증)]
  - [x] `dto/IssueResponse` — 최소 필드(`id` 등 상세 페이지 이동에 필요한 것). 발신자 평문은 응답에 **포함하지 않음**(MVP, 상세 조회 권한은 Story 2.3).
  - [x] `IssueService.create(...)` — `@Transactional`. ① `callerPhone` 정규화→HMAC→`callerPhoneHash` ② Issue 저장(callerName/phone 은 컨버터가 AES-GCM 자동 암호화) ③ `IssueEvent(CREATED, actor=created_by)` 저장. **단일 트랜잭션**. 배정(assigneeId)은 본 스토리 범위 아님(Story 2.4) — 시퀀스 §8.2 의 assignee 분기는 구현 안 함. [Source: architecture/8-sequence-flows.md#8.2; architecture/7-api-design.md#7.2]
  - [x] `IssueController` — `POST /api/issues`, `@PreAuthorize("hasAnyRole('AGENT','ADMIN')")`, `@Valid @RequestBody CreateIssueRequest`, 현재 사용자 = `@AuthenticationPrincipal Object principal` → `(Long) principal` (기존 `MeController` 패턴), 성공 시 **`201 Created`** + `IssueResponse`. [Source: backend/.../MeController.java; prd/6-rest-api-spec-요약.md(POST /api/issues = AGENT,ADMIN); architecture/6-...#6.3]

- [ ] **Task 4: 보안/구조 정합 확인** (AC: 6)
  - [ ] `SecurityConfig` 는 `/api/**` 가 이미 `authenticated()` — 별도 변경 불필요. 세부 권한은 메서드 `@PreAuthorize`(이미 `@EnableMethodSecurity` 활성). 변경 필요 시에만 수정. [Source: backend/.../config/SecurityConfig.java]
  - [ ] 로깅 시 발신자 이름/전화 평문 출력 금지 — 서비스/컨트롤러에서 PII 로그 미출력 확인. [Source: prd/9-coding-standards.md#9.1(로깅)]

- [x] **Task 5: 백엔드 테스트** (AC: 4, 6, 7)
  - [x] 단위: `AesGcmCipher` round-trip(encrypt→decrypt 동일, IV 매번 상이로 ciphertext 매번 상이), `HmacHasher` 결정성 + 정규화(`010-1234-5678` 와 `01012345678` 동일 해시).
  - [x] 통합(Testcontainers): `POST /api/issues` — (a) AGENT 201 + DB 의 `caller_*_enc` 가 평문과 다름(암호화 확인) + `caller_phone_hash` 가 HMAC 일치 + `issue_events` 에 CREATED 1건, (b) FIELD 403, (c) 미인증 401, (d) 필수 필드 누락 400 `VALIDATION_FAILED`. `GET /api/categories?level=1` 인증 시 3건 반환.
  - [x] **CI 실행 주의**: Testcontainers 통합 테스트는 로컬 Windows(docker-java 비호환)에서 실패할 수 있음 — GitHub Actions(ubuntu-latest, `./gradlew build`)에서 실행/검증. [Source: docs/stories/1.2.story.md#0.4 정정 이력] — **확인됨**: 신규+기존(`MeIntegrationTest`) 통합 테스트 모두 로컬에서 동일한 `ExceptionInInitializerError` (POSTGRES.start) 로 실패. 환경 한계, CI 검증 대상.

- [x] **Task 6: 프론트 의존성 + API/타입 레이어 — RHF+Zod 최초 도입** (AC: 2, 4)
  - [x] `npm i react-hook-form zod @hookform/resolvers @tanstack/react-query` (architecture §9.1 폼=RHF+Zod, 서버상태=TanStack Query 5). `main.tsx` 에 `QueryClientProvider` 추가. [Source: architecture/9-frontend-architecture.md#9.1; docs/stories/1.5.story.md#carry-over-4]
  - [x] `types/issue.ts` — `Priority`, `IssueStatus`, `CategoryOption`, `CreateIssueRequest`, `IssueResponse` (백엔드 DTO 와 명명 동기화, PRD 데이터모델 SSOT). [Source: prd/9-coding-standards.md#9.1(타입 공유)]
  - [x] `api/issues.ts` — `createIssue(req): Promise<IssueResponse>` → `apiClient.post('/issues', req)`. `api/categories.ts` — `listCategories(level): Promise<CategoryOption[]>`. 기존 `api/auth.ts` 패턴 준수(직접 axios 금지, apiClient 경유). [Source: backend(api/auth.ts); prd/9-coding-standards.md#9.1(API 호출)]

- [x] **Task 7: 공유 컴포넌트 (`shared/components/`) — §9.6 최초 도입** (AC: 3)
  - [x] `PriorityBadge.tsx` — props `{ priority, size?, showIcon?, label? }`. 색상 URGENT `#ff4d4f`/HIGH `#fa8c16`/NORMAL `#1677ff`/LOW `#8c8c8c`, `role="status" aria-label="우선순위: 긴급"`, 색상+텍스트+아이콘 3중. [Source: architecture/9-frontend-architecture.md#9.6.1, #9.2.4]
  - [x] `CategoryPicker.tsx` — props `{ value:{l1?,l2?,l3?}, onChange, required?, disabled?, layout? }`. 내부 3개 Ant `<Select>`, **자유 조합(종속성 없음)**, 각 Select `aria-label`("대분류"/"중분류"/"소분류"). 각 레벨 옵션은 `useCategories(level)` (TanStack Query, `staleTime` 5min) 로 로딩. [Source: architecture/9-frontend-architecture.md#9.6.3; #9.7(카테고리 5min); AC7]

- [x] **Task 8: `IssueFormView` + 라우트 + 단축키** (AC: 1, 2, 3, 4, 5)
  - [x] `features/issue-form/IssueFormView.tsx` — RHF + Zod 스키마(title/callerName/callerPhone/categoryL1~3/priority/description 필수). 한 페이지 스크롤 폼. 우선순위는 라디오/버튼 그룹 + `PriorityBadge` 색상. 제출 시 `createIssue` → 성공 시 `navigate('/issues/' + res.id)`. 실패 시 표준 에러(`{code,message,traceId}`) → `LoginView.mapError` 패턴 재사용해 메시지 매핑. [Source: docs/stories/1.5.story.md#carry-over-6; frontend/.../LoginView.tsx]
  - [x] 단축키: `Ctrl+S`(또는 `Cmd+S`) → `preventDefault()` + submit, `Esc` → 취소(`navigate('/issues')`). 폼 영역 keydown 핸들러. [Source: prd/epic-2...#Story-2.1 AC5]
  - [x] `routes.tsx` 에 `/issues/new` 추가: `RequireAuth` → `RequireRole(['AGENT','ADMIN'])` → `AppLayout` → `IssueFormView` (Story 1.5 의 보호 패턴 그대로). [Source: architecture/9-frontend-architecture.md#9.5; docs/stories/1.5.story.md#routes-table]

- [x] **Task 9: 최소 `IssueDetailView` placeholder** (AC: 4)
  - [x] `features/issue-detail/IssueDetailView.tsx` — `/issues/:id` 라우트. 본격 상세는 **Story 2.3** 범위이므로, 본 스토리는 Story 1.5 placeholder 스타일(Ant `<Card>` + "이슈 #{id} — 상세 화면은 준비 중(Story 2.3)") 최소 구현. AC4 의 "성공 시 상세 페이지로 이동" 을 현재 충족시키기 위함. 라우트: `RequireAuth` + `AppLayout`. [**DEVIATION**: AC4 가 상세 페이지 이동을 요구하나 IssueDetailView 본체는 Story 2.3 — placeholder 로 해소, Deviation Analysis 참조]

- [x] **Task 10: 프론트 테스트** (AC: 1, 2, 3, 4, 5, 7)
  - [x] Vitest+RTL: `IssueFormView.test.tsx` — 필드 렌더, 필수 검증 메시지, 정상 제출 시 `createIssue` 호출+인자, 성공 후 `/issues/:id` navigate(api mock), 우선순위 색상, `Ctrl+S` 제출 / `Esc` 취소.
  - [x] `CategoryPicker.test.tsx` — 3 Select 렌더 + 자유 조합(L1 변경이 L2/L3 옵션 안 막음, AC7), `aria-label`.
  - [x] `PriorityBadge.test.tsx` — 4 우선순위 색상/라벨/aria.
  - [x] 회귀: 기존 Vitest 37건 전부 통과 유지. (전체 51 통과: 37 회귀 + 14 신규) [Source: docs/stories/1.5.story.md#testing]

- [x] **Task 11: 수동 골든 패스 검증** (자동 등가물 통과 — 라이브 스택 단계는 QA/사용자 수행, Story 1.5 선례)
  - [x] 백엔드+프론트 dev 가동, `agent1/dev1234` 로그인 → `/issues/new` 진입. — *라이브 스택 필요(QA 수행); 라우트/가드 자동 검증: `IssueFormView.test`(RequireRole 패턴은 1.5 회귀로 검증).*
  - [x] 폼 작성(전화 `010-1234-5678`) → 저장 → 상세 placeholder 이동. — *자동: `IssueFormView.test` 가 제출→`/issues/:id` 이동 검증.*
  - [x] DB 확인: enc 컬럼 평문 아님, hash 64자. — *자동: `IssueControllerIntegrationTest.agentCreatesIssueAndPiiIsEncrypted` (CI 게이트).*
  - [x] `field1` 로 `/issues/new` 직접 접근 → 403. — *자동: `IssueControllerIntegrationTest.fieldRoleIsForbidden` (CI 게이트) + 프론트 RequireRole.*
  - [x] `npm test` (51/51) / `npm run build` (✅, gzip 305.8KB) / `./gradlew build -x test` (✅ 어셈블) + crypto 단위 8/8. 통합은 CI(로컬 Windows Testcontainers 환경한계 확인됨).

## Dev Notes

[[LLM: 아래는 docs 아티팩트에서 추출한 본 스토리 한정 정보. dev 는 PRD/architecture 원문을 다시 읽지 않아도 됨. 코딩/테스트 표준은 dev 가 이미 인지하므로 반복하지 않음.]]

### Previous Story Insights (Story 1.5 carry-over)

[Source: docs/stories/1.5.story.md#epic-2-진입-시-핵심-carry-over]

- **이 스토리가 다음을 최초 도입**: JPA 엔티티(Issue/Category/IssueEvent), `crypto/` 암호화 인프라, 프론트 RHF+Zod+TanStack Query, `shared/components/`(`PriorityBadge`,`CategoryPicker`). 1.5 까지 백엔드는 auth/user 만 존재.
- 모든 프론트 API 호출은 `src/api/*.ts` 도메인 함수 → `apiClient` 경유(컴포넌트 직접 axios 금지).
- 에러 응답 `{code, message, traceId}` 표준 — `LoginView.mapError` 패턴 도메인별 코드 매핑으로 재사용.
- `ddl-auto: validate` enforced → **신규 엔티티는 V2 마이그레이션 컬럼과 정확히 일치해야 부팅됨**. 스키마 변경은 V5+ 마이그레이션으로만(V1~V4 수정 금지).
- 401 자동 logout 인터셉터 작동 중 — 신규 엔드포인트 자동 적용.

### Data Models (V2 실제 DDL — SSOT)

[Source: backend/src/main/resources/db/migration/V2__create_tables.sql]

- `issues`: `id BIGSERIAL PK`, `title VARCHAR(200) NOT NULL`, `description TEXT NULL`, `caller_name_enc BYTEA NULL`, `caller_phone_enc BYTEA NULL`, `caller_phone_hash VARCHAR(64) NULL`, `category_l1_id/l2_id/l3_id BIGINT NOT NULL → categories(id)`, `priority VARCHAR(10) CHECK(URGENT|HIGH|NORMAL|LOW)`, `status VARCHAR(15) CHECK(NEW|ASSIGNED|IN_PROGRESS|DONE|VERIFIED)`, `created_by BIGINT NOT NULL → users(id)`, `assigned_to BIGINT NULL`, `resolved_at TIMESTAMPTZ NULL`, `created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`.
- `categories`: `id BIGSERIAL PK`, `parent_id BIGINT NULL`, `level SMALLINT CHECK(1|2|3)`, `name VARCHAR(100) NOT NULL`, `keywords JSONB NOT NULL DEFAULT '[]'`, `sort_order INT NOT NULL DEFAULT 0`, `active BOOLEAN NOT NULL DEFAULT TRUE`. V3 시드: L1 3 / L2 4 / L3 3, **parent_id 전부 NULL**(자유 조합, AC7). [Source: V3__seed_categories.sql]
- `issue_events`: `event_type VARCHAR(30) CHECK(CREATED|STATUS_CHANGED|ASSIGNED|COMMENTED|ATTACHMENT_ADDED|RESOLVED)`, `from_value/to_value VARCHAR(50) NULL`, `actor_id BIGINT NOT NULL`.

### 암호화/해시 규약

[Source: architecture/6-security-architecture-defense-in-depth.md#6.4, #6.5; ADR-004-hmac-search-hash.md]

- `caller_name_enc`, `caller_phone_enc`: AES-256-GCM, 랜덤 12-byte IV ciphertext 앞 prepend. 키 `SMCS_DATA_KEY`(Base64 32 bytes).
- `caller_phone_hash`: HMAC-SHA256, **숫자만 정규화 후** 해시(`01012345678`), 별도 키 `SMCS_HMAC_KEY`. hex 64자 → `VARCHAR(64)` 적합. 정확 매칭 검색용(검색 기능 자체는 Story 2.2).
- 로그에 발신자 이름/전화 평문 금지(PRD §9.1 로깅; §6.9 마스킹).

### API 규약

[Source: architecture/7-api-design.md; prd/6-rest-api-spec-요약.md]

- `POST /api/issues`: AGENT/ADMIN, 201 + body. 트랜잭션 내 ① 이슈 ② IssueEvent(CREATED). (배정 분기는 Story 2.4.)
- 검증: Bean Validation, 실패 시 400 + `VALIDATION_FAILED` (기존 `GlobalExceptionHandler` 가 처리 — 추가 핸들러 불필요). [Source: backend/.../GlobalExceptionHandler.java]
- 에러 표준 `{code, message, traceId}` (`ErrorResponse` + `TraceIdFilter` 기존).
- 페이징/정렬 표준(Story 2.2 에서 사용): `?page=&size=&sort=priority,desc`.

### 백엔드 구조 규약 (실제 코드 기준)

[Source: backend/src/main/java/com/smcs/ (실측); architecture/4-monorepo-directory-structure.md#4.1]

- **실제 컨벤션 = flat 도메인 패키지**: `com.smcs.user` 안에 `User.java`, `UserRepository.java`, `MeController.java` + `dto/` 서브패키지. architecture §4.1 은 `entity/repository/service/controller/` 서브폴더를 제안하나 **Story 1.1~1.3 실구현은 flat** → 신규 `com.smcs.issue`, `com.smcs.category`, `com.smcs.crypto` 도 **flat + `dto/`** 따름(일관성). (Project Structure Notes 참조)
- 엔티티 패턴: `@Entity @Table(name=...)`, `@GeneratedValue(IDENTITY)`, `@Column(name=..., nullable=, length=)`, 시간은 `Instant`, enum 은 nested + `@Enumerated(EnumType.STRING)`, `protected` 기본 생성자, getter only.
- 현재 사용자: `@AuthenticationPrincipal Object principal` → `(Long) principal` (principal = userId Long, authority = `ROLE_<role>`).
- 권한: 메서드 `@PreAuthorize("hasAnyRole('AGENT','ADMIN')")` (이미 `@EnableMethodSecurity`). `SecurityConfig` 의 `/api/**` 는 이미 `authenticated()` — 변경 불필요.
- 설정: `application.yml`(공통) + `application-local.yml`(로컬 dev 값) + `application-prod.yml.example`(prod 템플릿, env 주입). 시크릿 하드코딩 금지.

### Frontend 구조 규약

[Source: architecture/4-...#4.2, /9-...#9.5, #9.6; docs/stories/1.5.story.md]

- `features/<feature>/` 자기완결, `shared/components/` 는 2+ feature 공용만(본 스토리가 §9.6 최초 도입처).
- 라우트 보호: `<RequireAuth><RequireRole roles={[...]}><AppLayout>...</AppLayout></RequireRole></RequireAuth>` (1.5 패턴).
- 폼: **RHF + Zod**(architecture §9.1, 1.5 carry-over #4 명시). 기존 `LoginView` 는 antd `<Form>`(2필드 단순 폼) — 본 스토리의 복합 폼은 RHF+Zod 채택(Deviation Analysis 참조). React 상태 불변 업데이트.
- 서버 상태: TanStack Query 5(카테고리 `staleTime` 5min). axios 인터셉터(JWT/401) 기존.

### Deviation Analysis (PO 검토 필요)

1. **`GET /api/categories` 신규 추가**: PRD §6 표엔 `/api/admin/categories?level=`(ADMIN 전용)만 존재. 등록 폼은 AGENT 가 사용 → 인증 사용자용 카테고리 조회 API 가 필수. 본 스토리에서 `GET /api/categories?level={1|2|3}`(`isAuthenticated()`, 활성·sort_order 순) 신규 정의. PRD §6 표에 후속 반영 권장.
2. **`/issues/:id` 상세 placeholder**: AC4 는 저장 후 "이슈 상세 페이지 이동" 요구하나 `IssueDetailView` 본체는 Story 2.3 범위. 본 스토리는 최소 placeholder(이슈 id 표시)로 AC4 를 충족, 본격 상세는 2.3 에서 대체. (Story 1.5 의 placeholder 선례와 동일 전략.)
3. **폼 스택 전환(RHF+Zod)**: 기존 `LoginView` 는 antd `<Form>`. architecture §9.1 + 1.5 carry-over #4 가 RHF+Zod 를 명시했고, 7필드 복합 폼이므로 본 스토리에서 RHF+Zod 도입. LoginView 와의 폼 스택 불일치는 의도적(단순 폼 vs 복합 폼) — 향후 폼은 RHF+Zod 표준.
4. **시퀀스 §8.2 의 assignee 분기 미구현**: 본 스토리는 등록만(status=NEW). 배정/알림은 Story 2.4. 시퀀스의 `assigneeId` 경로는 본 스토리 범위 밖.

### PO Validation (Sarah, 2026-05-18)

**판정: APPROVED** — Status Draft → Approved. 선행조건(Epic 1 Done/merged) 충족, 차단 의존성 없음.

- **Deviation 1 (GET /api/categories)**: 승인. 후속 — PRD §6 표에 `GET /api/categories?level={1|2|3}` (인증) 행 추가 필요(문서 정합성, 본 스토리 차단 아님).
- **Deviation 2 (/issues/:id placeholder)**: 승인. **AC4 해석 고정**: "성공 시 이슈 상세 페이지로 이동" = `/issues/:id` 라우트 이동 + 생성 이슈 id 표시까지가 2.1 범위. 상세 화면 본체 및 권한 가드(담당자/AGENT/ADMIN, architecture §9.5)는 **Story 2.3**. QA 는 본 AC 를 "상세 미완" 으로 fail 처리하지 말 것. → Story 2.3 드래프트 시 "`/issues/:id` 본격 구현 + §9.5 권한 가드(현재 placeholder 는 RequireAuth 만)" 를 명시 carry-over.
- **Deviation 3 (RHF+Zod)**: 승인. 아키텍처 §9.1 표준과 정합 — 계획 이탈 아님. **기존 `LoginView`(antd Form)는 리트로핏 금지**(범위 밖, 외과적 변경 원칙). 향후 폼 표준 = RHF+Zod.
- **Deviation 4 (assignee/알림 2.4 연기)**: 승인 — 올바른 시퀀싱. 신규 이슈 status=`NEW`(무배정).
- **스코프**: 대형이나 **단일 스토리 유지**(crypto 는 AC6 경성 선행조건; 분리 시 가치 없는 인프라-only 스토리 → BMAD 수직 슬라이스 위배). Dev 는 Task 순서(crypto→category→issue→frontend) 엄수.

### Project Structure Notes

- architecture §4.1 의 도메인 서브폴더(`entity/`,`service/`...) 제안과 **실구현 flat 패키지** 불일치 — 실구현 일관성 우선(flat + `dto/`). 향후 architecture 문서 동기화 시 정정 대상(차단 아님).
- 신규 backend 패키지: `com.smcs.crypto`, `com.smcs.category`, `com.smcs.issue`. 신규 frontend: `features/issue-form/`, `features/issue-detail/`(placeholder), `shared/components/`, `api/issues.ts`, `api/categories.ts`, `types/issue.ts`.
- 스키마 변경 없음(V2 가 이미 모든 테이블 보유) → **신규 Flyway 파일 불필요**. 엔티티는 V2 에 맞춤(ddl-auto: validate).

### Testing

Dev Note: Story Requires the following tests:

- [ ] **JUnit 단위** (nextToFile: false, `backend/src/test/java/com/smcs/...`): `AesGcmCipher`, `HmacHasher`. coverage 80%.
- [ ] **JUnit 통합 (Testcontainers PostgreSQL)** location: `backend/src/test/java/com/smcs/issue/`: `POST /api/issues`(AGENT 201/FIELD 403/미인증 401/검증 400 + DB 암호화·HMAC·IssueEvent 검증), `GET /api/categories`. **로컬 Windows 미실행 가능 → CI(GitHub Actions)에서 검증** [Source: docs/stories/1.2.story.md#0.4].
- [ ] **Vitest + RTL** (nextToFile: true): `IssueFormView.test.tsx`, `CategoryPicker.test.tsx`, `PriorityBadge.test.tsx`. 기존 37건 회귀 통과 유지.
- [ ] **E2E**: 본 스토리 범위 밖(Story 4.x 폴리시).

Manual Test Steps: Task 11 참조 (agent1 로그인 → `/issues/new` 작성·저장 → 상세 placeholder + DB 암호화 확인 → field1 403 → `npm test`/`npm run build`/`./gradlew build`).

## Dev Agent Record

### Agent Model Used: claude-opus-4-7 (1M context)

### Debug Log References

| Task | File | Change | Reverted? |
| :--- | :--- | :----- | :-------- |
| 10 | `IssueFormView.tsx` | 빈 제출 검증 메시지 미표시 → 원인: 문자열 필드 `defaultValues` 미설정 시 zod 가 `undefined` 를 invalid_type("Required")로 처리해 커스텀 `min(1)` 메시지 미발생. `defaultValues` 에 빈 문자열 추가(영구 수정). | No (영구 수정) |
| CI | `.github/workflows/ci.yml` | PR #3 CI 레드: `FlywayMigrationIntegrationTest`(default 프로파일, `@ActiveProfiles` 없음) 부팅 시 신규 `AesGcmCipher.@PostConstruct` 가 `smcs.crypto.data-key` 부재로 throw → BeanCreationException. 원인: CI 가 `SMCS_JWT_SECRET` 만 env 주입, crypto 키 미주입(default 프로파일은 `application.yml` 빈 기본값). 수정: ci.yml 에 `SMCS_DATA_KEY`/`SMCS_HMAC_KEY` env 추가(JWT 와 동일 패턴, prod 요구사항 정합). | No (영구 수정) |

### Completion Notes List

- **통합 테스트 CI 게이트**: 신규 `IssueControllerIntegrationTest`/`CategoryControllerIntegrationTest` 는 로컬 Windows 에서 기존 `MeIntegrationTest` 와 **동일한** `ExceptionInInitializerError`(Testcontainers `POSTGRES.start()`) 로 실패 — docker-java/Windows 환경 한계(스토리 Task 5 예측). 컴파일 클린, CI(ubuntu)에서 검증.
- **번들 가드레일 초과(경미)**: 프론트 초기 청크 gzip **305.8KB** > §9.7 가드레일 300KB. RHF+Zod+TanStack Query 신규 도입분. 코드 분할/vendor chunk 는 §9.7 + Story 1.5 carry-over #3(lazy)에서 후속 — 본 스토리 AC 아님. **SM/QA 인지 필요.**
- **QueryClientProvider 위치**: 스토리 지시대로 `main.tsx` 에 배치(앱 provider 합성은 `App.tsx` 지만 지시 우선). RHF 폼은 antd `<Form>` 미사용(RHF 상태와 충돌 회피) — 네이티브 `<form>` + `<label htmlFor>` + Controller.
- **AC4 placeholder**: PO 승인대로 `/issues/:id` 는 `IssueDetailView` placeholder(RequireAuth만). 본격 구현+§9.5 권한 가드 = Story 2.3 carry-over.
- 신규 백엔드 의존성 없음(AES/HMAC = JDK). 프론트 신규: `react-hook-form zod @hookform/resolvers @tanstack/react-query`. Node18 EBADENGINE 경고는 비차단(install/build/test 정상).
- **QA N-1 해소(v0.5)**: `GlobalExceptionHandler` 에 `MethodArgumentTypeMismatchException`·`MissingServletRequestParameterException` → 400 `VALIDATION_FAILED` 전역 핸들러 추가. `GlobalExceptionHandlerTest` 2/2 로컬 통과 + `CategoryControllerIntegrationTest` 에 nonNumeric/missing level→400 케이스 2건 추가(CI 게이트). **QA 재검증 대기.**

### Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                | Author    |
| :--------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------- |
| 2026-05-18 | 0.1     | Initial draft by SM (Bob). Epic 2 첫 스토리. Sourced: prd/epic-2 Story 2.1(AC7) + architecture §4/§5/§6/§7/§8.2/§9 + V2/V3 실 DDL + 기존 backend 컨벤션(User/MeController/SecurityConfig/GlobalExceptionHandler) + 1.5 carry-over. 4건 Deviation 명시(카테고리 API 신규/상세 placeholder/RHF+Zod 전환/assignee 미구현). | Bob (SM) |
| 2026-05-18 | 0.2     | PO 검증 완료 — Status Draft → **Approved**. Deviation 4건 전부 승인, AC4 해석 고정(상세 placeholder 범위), 스코프 단일 유지 결정. 후속: PRD §6 표 보강 + Story 2.3 carry-over(§9.5 권한 가드). | Sarah (PO) |
| 2026-05-18 | 0.3     | 구현 완료(James). Task 1~11 전부. 백엔드: `crypto`(AES-GCM/HMAC/Converter)+`category`+`issue` 도메인, `POST /api/issues`·`GET /api/categories`. 프론트: RHF+Zod 폼, `PriorityBadge`/`CategoryPicker`, 라우트, 상세 placeholder. 테스트: crypto 단위 8/8, 프론트 51/51(회귀 37+신규 14), 통합 5종 작성(CI 게이트). 빌드 통과. Status → **Review**. | James (Dev) |
| 2026-05-18 | 0.4     | QA 게이트(Quinn): 🟡 **CONCERNS**. 정적+로컬테스트 全그린, 결함 0. AC6 e2e/Hibernate validate/seeder 회귀는 CI 검증 필수이나 CI 런 부재(미커밋). PASS 조건 = 푸시 후 CI 그린. 비차단 N-1/N-2/N-3 기록. QA Results 섹션 추가. | Quinn (QA) |
| 2026-05-18 | 0.5     | QA N-1 해소(James): `GlobalExceptionHandler` 에 typeMismatch/missingParam → 400 VALIDATION_FAILED 전역 핸들러 추가. 단위 `GlobalExceptionHandlerTest` 2/2 로컬 통과, 통합 케이스 2건 추가(CI). QA 재검증 대기. | James (Dev) |
| 2026-05-18 | 0.6     | QA 재검증(Quinn): N-1 ✅ **RESOLVED** — 독립 재실행 `GlobalExceptionHandlerTest` 2/2 BUILD SUCCESSFUL + 코드리뷰 정확. 게이트 **🟡 CONCERNS 유지**(원인=CI 부재, N-1 무관). PASS 조건 불변. | Quinn (QA) |
| 2026-05-18 | 0.7     | CI 회귀 수정(James): PR #3 1차 CI 레드 — default 프로파일 통합테스트(`FlywayMigrationIntegrationTest`)가 신규 crypto 빈 부팅 실패(`SMCS_DATA_KEY` 미주입). ci.yml 에 `SMCS_DATA_KEY`/`SMCS_HMAC_KEY` env 추가(JWT 동일 패턴). 재푸시 → CI 재실행. | James (Dev) |

## QA Results

**Reviewer:** Quinn (Test Architect) · **Date:** 2026-05-18 · **Gate:** 🟡 CONCERNS (결함 0, CI 검증 대기)

### AC Traceability
| AC | 검증 방법 | 상태 |
| :- | :-------- | :--- |
| 1 폼 한 페이지 | 정적 + RTL 렌더 | ✅ |
| 2 필수 7필드 + 3단계 드롭다운 | RTL `IssueFormView`(6) + `CategoryPicker`(2) | ✅ |
| 3 우선순위 색상강조 | `PriorityBadge` 스펙색상, RTL 4종(라벨/aria/icon 3중) | ✅ (색상값 정적) |
| 4 POST→상세이동 | RTL 제출→`/issues/:id` navigate; 상세=PO승인 placeholder | ✅ |
| 5 Ctrl+S/Esc | RTL keydown 2 | ✅ |
| 6 PII 암호화 저장 | crypto 단위 8/8(round-trip,IV랜덤,정규화) + `@Convert` 정적 | ⚠️ DB e2e=CI 대기 |
| 7 L1/L2/L3 무종속 | level-only 조회 정적 + RTL 독립 Select; DB측 통합=CI | ✅ (정적/RTL) |

### 독립 재실행 증거 (QA 1차)
- 백엔드 `./gradlew test --tests com.smcs.crypto.*` → **BUILD SUCCESSFUL (8/8)**
- 프론트 `npx vitest run` → **51/51 (13 files)** — 회귀 37 무손상
- `Issue`/`Category`/`IssueEvent` ↔ V2 DDL 컬럼 단위 정적 대조: 전 컬럼 매핑·명·타입 정합 (기존 `User` 엔티티 `validate` 통과 패턴과 동형 → 정적 신뢰도 高)

### CI 검증 필수 (PASS 전환 단일 조건 — N-1 수정 후에도 불변)
> N-1 해소는 품질 향상일 뿐 게이트 차단요인이 아님. CONCERNS 의 원인은 아래 CI 부재이며 그대로 유지.

브랜치 커밋·푸시 → GitHub Actions(ubuntu, `./gradlew build`) 그린:
1. `IssueControllerIntegrationTest` — AGENT 201 + `caller_*_enc` 평문≠/복호일치 + `caller_phone_hash`=HMAC + `issue_events` CREATED 1 / FIELD 403 / 401 / 400 VALIDATION_FAILED
2. `CategoryControllerIntegrationTest` — 인증 L1 3건 / 401 / **nonNumeric level 400 / missing level 400 (N-1)**
3. 회귀: `LocalDataSeederIntegrationTest`·`FlywayMigrationIntegrationTest` 그린 = 신규 엔티티 추가 후에도 Hibernate `validate` 부팅·시드 무손상 입증
4. (로컬 선검증 완료, CI 재확인) `GlobalExceptionHandlerTest`·`com.smcs.crypto.*` 단위 + 프론트 51건

> 로컬 Windows 는 Testcontainers `POSTGRES.start()` `ExceptionInInitializerError`(docker-java 비호환, Story 1.2 v0.4 문서화)로 통합 실행 불가 — 신규·기존 통합 동일 실패 확인. Story 1.2 의 "CI 미확인 추론" 오류를 피하기 위해 직접 확인: 2.1 은 미커밋이라 CI 런이 **존재하지 않음**. 따라서 CONCERNS 는 추론이 아닌 사실.

### 비차단 관찰 (기술부채 — 후속 스토리)
- **N-1 (Low) — ✅ RESOLVED (v0.5, QA 재검증 2026-05-18)**: `GlobalExceptionHandler` 에 `MethodArgumentTypeMismatchException`/`MissingServletRequestParameterException` → 400 `VALIDATION_FAILED` 전역 핸들러 추가. QA 독립 재실행 `GlobalExceptionHandlerTest` **2/2 BUILD SUCCESSFUL**, 코드 리뷰 정확(핸들러 특이성·무충돌). 통합 e2e(`CategoryControllerIntegrationTest` nonNumeric/missing level→400) 2건은 CI 게이트. *원래 증상: 비숫자/누락 level → 500.*
- **N-2 (Low)** 프론트 초기 청크 gzip 305.8KB > architecture §9.7 가드레일 300KB. RHF+Zod+TanStack 도입분. 코드분할/manualChunks 후속(§9.7, Story 1.5 carry-over #3 lazy). 본 스토리 AC 아님.
- **N-3 (Info)** `npm audit`: high 1 = 기존 `axios@1.7.7`(Story 1.4 유래, **2.1 무관**), moderate 2 = dev 전용 `esbuild`(vite 툴체인). 신규 2.1 의존성 취약점 0. 별도 dependency-bump 스토리에서 axios 업그레이드 권장.
- **N-4 (Info)** `EncryptedStringConverter` 정적 브리지(`CryptoSupport`): bean 초기화 순서상 안전(컨버터 호출은 요청/시더 시점, `@PostConstruct` 이후)하나 `LocalDataSeederIntegrationTest`(CI)로 시더-경유 회귀 확인 권장 — 위 CI 항목 3 에 포함.
