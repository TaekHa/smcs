# Story 4.6: 운영 배포 + 백업

## Status: Done

> QA Gate (2026-05-27, Quinn — Test Architect): 🟢 **PASS**. 결함 0. **CI 그린(1차 통과 — 회귀 없음)**(PR #26 run 26492750707 — Backend `> Task :test` pass 2m7s + Frontend `Run tests` 1m11s + `Build production bundle` eager <300KB gzip §9.7). 위험영역 정적리뷰 통과: R1 Dockerfile non-root uid 1000 + HEALTHCHECK + .dockerignore secret 차단(`.env*`/`*.key`/`*.pem`/`*.crt`) / R2 nginx TLS 1.2-1.3 only + server_tokens off + HSTS+X-Frame+X-Content+Referrer 4 보안 헤더 + client_max_body_size 15m / R3 compose `${VAR:?}` fail-fast 인터폴레이션 + SPRING_PROFILES_ACTIVE=prod hardcode / R4 SecurityConfig `/actuator/health` permitAll + `/actuator/**` denyAll 매처 순서 보존 / R5 dev 자가발견 P2 fix 2건(`/api/actuator/health` strip + IPv4 healthcheck) 정합 — lesson #11 추가. PRD AC 6/6 충족(AC1 compose 단일 명령·AC2 HTTPS+Let's Encrypt dns-01 3 provider·AC3/4 DB+첨부 백업 30일·AC5 헬스체크 4 컨테이너 healthy + curl 200·AC6 operations.md 7 섹션). PO 비준 Deviation 9건 모두 구현 일치. **잔여 비차단 2건 QA 수용**: admin1 회귀 = Story 4.7 user-test cycle 이 자연스럽게 흡수(operations.md §2.1 admin 부트스트랩 절차 + golden-path.md 6 단계 E2E 가 동일 시나리오), 백업 dry-run = operations.md §4.3 월간 무결성 검증으로 운영 단계 정착. **머지 가능** — Epic 4 진척 7/7(4.6 머지 후 = 4.1~4.6 Done + 4.7 진행 중).

## Story

- As a **운영자**
- I want **SMCS 를 사내 서버에 안정적으로 배포하고 백업하기를**
- so that **데이터 손실 없이 운영할 수 있다**

## Acceptance Criteria (ACs)

1. **Docker Compose 단일 명령 기동**: `docker compose -f docker/docker-compose.prod.yml up -d` 한 줄로 backend + frontend + nginx + postgres 4개 컨테이너 기동. (PRD §4.6 AC1 "(선택) redis" 는 **MVP 제외** — Deviation #1).
2. **HTTPS 인증서 설정 가이드**: `docs/operations.md` 1페이지에 사내 CA 인증서 적용 단계(`nginx/ssl/server.crt` + `server.key` 마운트) + Let's Encrypt 폴백 방식 명시. 인증서 미설치 시 HTTP 80 → HTTPS 443 redirect 활성/비활성 설정 둘 다 안내.
3. **DB 일일 자동 백업**: `scripts/backup-db.sh`(pg_dump --format=custom 압축) + crontab 등록 가이드. **30일 보관 룰** = 31일 이전 파일 `find ... -mtime +30 -delete`. 백업 파일명 `smcs-db-YYYY-MM-DD.dump`. 백업 저장 위치는 **PO 결정 필요** (Deviation #3).
4. **첨부 파일 디렉토리 백업**: `scripts/backup-files.sh`(`smcs.files.dir` rsync 또는 tar 일자별) + 동일 30일 보관. DB 백업과 별도 스크립트(파일 변동 트랜잭션 무관, rsync 증분 가능).
5. **헬스 체크 통과**: 운영 컨테이너 기동 후 `curl -fsS http://<host>/api/actuator/health` → 200 `{"status":"UP"}` 응답. Docker `healthcheck` 정의로 컨테이너 상태 표시. nginx upstream 헬스체크에 동일 endpoint 활용.
6. **운영 가이드 1페이지** `docs/operations.md`(신규): (a) 서버 재기동 명령 (b) 백업/복원 명령(`pg_restore`) (c) 로그 위치(호스트 `/var/log/smcs/` 마운트 + `docker compose logs`) (d) 환경변수 체크리스트 (e) admin1 초기 비밀번호 변경 의무 (f) 보고서 cleanup-cron 점진 활성화 절차(3.5 lesson) (g) 트러블슈팅(헬스체크 실패/DB 연결 실패/JWT secret 누락).

## Tasks / Subtasks

- [x] **Task 1: Backend Dockerfile (AC: 1, 5)** (AC: 1, 5)
  - [x] `backend/Dockerfile` 신규 — multi-stage: `eclipse-temurin:21-jdk-jammy` builder(gradle wrapper) → `bootJar` → `eclipse-temurin:21-jre-jammy` runtime. `WORKDIR /app`, `COPY --from=builder smcs-*.jar app.jar`, `EXPOSE 8080`, `ENV TZ=Asia/Seoul + JAVA_TOOL_OPTIONS`, `ENTRYPOINT ["java","-jar","app.jar"]`. **Deviation from SM draft**: gradle 공식 이미지 대신 프로젝트 gradle wrapper(`gradlew`) 사용 — 프로젝트 명시 gradle 버전 일관성 ↑, 베이스 이미지 단일화(temurin 만). [Source: backend/build.gradle.kts(bootJar 기존); backend/gradle/wrapper/gradle-wrapper.properties; backend/src/main/resources/application.yml(SMCS_* 환경변수 lesson #3)]
  - [x] Image 크기 최소화: `--no-install-recommends`, `rm -rf /var/lib/apt/lists/*`, gradle `--no-daemon`, JRE-only runtime. apt 설치 최소(curl + tzdata만).
  - [x] `HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 CMD curl -fsS http://localhost:8080/actuator/health | grep -q '"status":"UP"'`. show-details=never(Deviation #6) 응답이 단순 JSON 이므로 grep 충분.
  - [x] **보안 보강(SM 명세 보강)**: non-root user `smcs:smcs` (uid/gid 1000) 생성 + `USER smcs`. 쓰기 경로는 마운트 볼륨(`SMCS_FILES_DIR`, logs)만 — runtime container 안에 평문 쓰기 0.
  - [x] **보안 보강**: `.dockerignore` 신규 — build/.gradle 재빌드 보장 + .git 제외 + **secret 패턴 차단**(`.env*`, `*.key`, `*.pem`, `*.crt`, `*.p12`, `*.jks`).

- [x] **Task 2: Frontend Dockerfile + nginx 정적 서빙 (AC: 1, 2, 5)** (AC: 1, 2, 5)
  - [x] `frontend/Dockerfile` 신규 — multi-stage: `node:20-alpine` builder → `npm ci && npm run build` → `nginx:1.27-alpine` runtime. `COPY dist /usr/share/nginx/html`, `COPY nginx/default.conf /etc/nginx/conf.d/`. `EXPOSE 80`. [Source: frontend/vite.config.* (build output `dist/`); routes.tsx(React Router SPA)]
  - [x] `frontend/nginx/default.conf` 신규 — SPA fallback `try_files $uri /index.html`, gzip on, hashed asset 1년 캐시(`/assets/*`), `/index.html` no-cache.
  - [x] **API proxy 분기 결정 (Deviation #2)**: frontend 컨테이너의 nginx는 정적 서빙만, **외부 reverse proxy nginx(SSL termination)** 가 `/api/*` → backend 분기. **PO 비준 B 옵션(v0.2)** — frontend nginx + 외부 nginx 분리 채택. 본 컨테이너의 default.conf 은 정적 SPA 서빙 + `/healthz` 만 노출, `/api/*` 라우팅은 Task 3 외부 nginx 에서 처리.

- [x] **Task 3: 외부 nginx (SSL 종단 + reverse proxy) (AC: 1, 2)** (AC: 1, 2)
  - [x] `docker/nginx/nginx.conf`(신규) — `upstream smcs_backend { server backend:8080; keepalive 32; }` + `upstream smcs_frontend { server frontend:80; keepalive 16; }` + server block 443 SSL.
  - [x] `location /api/` → proxy_pass smcs_backend (X-Forwarded-For, X-Forwarded-Proto, X-Real-IP, X-Forwarded-Host 헤더 전달). proxy_buffering off (파일 다운로드 스트리밍 보존).
  - [x] `location /` → proxy_pass smcs_frontend (B 옵션, frontend 컨테이너 분리).
  - [x] HTTP 80 server block: `return 301 https://$host$request_uri` + Let's Encrypt http-01 challenge fallback 경로(`/.well-known/acme-challenge/` → `/var/www/certbot`, 기본 dns-01 사용 시 미사용).
  - [x] SSL 설정: `ssl_protocols TLSv1.2 TLSv1.3` only, `HIGH:!aNULL:!MD5` ciphers, HSTS `max-age=31536000; includeSubDomains`, X-Frame-Options SAMEORIGIN, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, `ssl_certificate /etc/nginx/ssl/server.crt`, `ssl_certificate_key /etc/nginx/ssl/server.key` (volume 마운트). `client_max_body_size 15m`(backend multipart 10MB + envelope 여유).
  - [x] **HTTPS 인증서 방식 = PO 비준 Let's Encrypt dns-01 (v0.2)**: nginx.conf 의 cert 경로는 `/etc/nginx/ssl/` 단일화(volume 마운트). operations.md(Task 7)에서 두 가지 적용 방식 안내: (a) Let's Encrypt fullchain.pem/privkey.pem 을 ssl/ 디렉토리로 copy 또는 symlink, (b) `/etc/letsencrypt` 직접 마운트 후 ssl_certificate 경로만 수정. 도메인+DNS provider 운영 단계 협의 항목은 변경 없음(인증서 발급 단계에서 확정).

- [x] **Task 4: docker-compose.prod.yml + .env.template (AC: 1, 5)** (AC: 1, 5)
  - [x] `docker/docker-compose.prod.yml` 신규 — 4 services(postgres / backend / frontend / nginx). depends_on `condition: service_healthy`. restart: unless-stopped. 네트워크 단일 `smcs_net`. 공통 logging anchor(`x-logging: &default-logging` json-file max-size 100m max-file 5 — PO Deviation #9). 모든 컨테이너 TZ=Asia/Seoul.
  - [x] `postgres` 서비스: image `postgres:16-alpine`(local과 동일 버전 일관), volume `smcs_pg_data:/var/lib/postgresql/data`, healthcheck `pg_isready -U smcs -d smcs` (10s/5s/10retries). `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?...}` fail-fast.
  - [x] `backend` 서비스: build context `../backend`, image tag `smcs-backend:local`, ports 노출 0(internal only — nginx 통해), explicit `environment:` 블록(env_file 미사용 — `SMCS_FILES_DIR` 가 컨테이너 내부로 새지 않도록 분리), depends_on `postgres { condition: service_healthy }`, volume `${SMCS_FILES_DIR:-/data/smcs/files}:/var/smcs/files` 마운트(첨부 영속) + `${SMCS_LOGS_DIR:-/var/log/smcs}:/var/log/smcs` 마운트.
  - [x] `frontend` 서비스: build context `../frontend`, image tag `smcs-frontend:local`, internal only. TZ=Asia/Seoul.
  - [x] `nginx` 서비스: image `nginx:1.27-alpine`, ports `80:80` + `443:443`, volume `./nginx/nginx.conf:/etc/nginx/nginx.conf:ro` + `./nginx/ssl:/etc/nginx/ssl:ro` + named volume `certbot_www:/var/www/certbot:ro`(http-01 fallback 보존), depends_on backend+frontend healthy. healthcheck `wget --spider http://localhost/` (30s/5s/10s start/3retries).
  - [x] `docker/.env.template`(신규) — 환경변수 체크리스트: `POSTGRES_PASSWORD`(postgres user + Spring datasource 단일 SoT), `SMCS_JWT_SECRET`/`SMCS_DATA_KEY`/`SMCS_HMAC_KEY`(Base64 32 bytes, 누락 시 backend startup fail), `SMCS_FILES_DIR=/data/smcs/files`(uid 1000 owner 필수 — operations.md 명시), `SMCS_LOGS_DIR=/var/log/smcs`, `SMCS_REPORTS_DAILY_CRON`/`WEEKLY_CRON`/`CLEANUP_CRON`(첫 배포 = `0 0 5 31 2 *` 도달불가, 3.5 Deviation #8 lesson), `SMCS_REPORTS_RETENTION_DAYS=90`. **SPRING_PROFILES_ACTIVE=prod 는 compose `environment:` 에 hardcode**(operator 사고 차단). **SM 명세 deviation**: `SMCS_DB_URL`/`SMCS_DB_USERNAME` 대신 Spring 표준 `SPRING_DATASOURCE_URL`/`USERNAME`/`PASSWORD` 사용(application.yml 의 `spring.datasource.*` 가 비어 있어 표준 binding 만 동작). compose `environment:` 에서 SPRING_DATASOURCE_URL=`jdbc:postgresql://postgres:5432/smcs` + USERNAME=smcs hardcode, PASSWORD = `${POSTGRES_PASSWORD}` 단일 SoT.
  - [x] `.gitignore` 수정 — `docker/.env`, `docker/nginx/ssl/*.crt`, `docker/nginx/ssl/*.key`, `docker/nginx/ssl/*.pem` 추가.
  - [x] **(선택) `application-prod.yml`** = 작성 안 함. 환경변수만 의존(application.yml `${SMCS_*:}` 기본값 + compose `SPRING_DATASOURCE_*` 표준 binding 으로 충분).

- [x] **Task 5: 백업 스크립트 + cron (AC: 3, 4)** (AC: 3, 4)
  - [x] `scripts/backup-db.sh`(신규) — `docker exec -i smcs_postgres pg_dump --format=custom --no-acl --no-owner -U smcs -d smcs > /backup/db/smcs-db-$(date +%F).dump`. 컨테이너 실행 확인(`docker inspect`) 후 dump 진행. 30일 정리: `find /backup/db -name 'smcs-db-*.dump' -type f -mtime +30 -delete`. `set -euo pipefail` + 실패 시 stderr 만(MVP). 환경변수 오버라이드(BACKUP_ROOT/POSTGRES_CONTAINER/POSTGRES_DB/POSTGRES_USER) 지원.
  - [x] `scripts/backup-files.sh`(신규) — rsync 가용 시 `rsync -a --delete ${SMCS_FILES_DIR}/ /backup/files/$(date +%F)/`, 미가용 시 `tar czf /backup/files/smcs-files-$(date +%F).tar.gz` 폴백(자동 분기). 30일 정리(dir + tar 모두): `find /backup/files -mindepth 1 -maxdepth 1 -mtime +30 -exec rm -rf {} +`. 소스 디렉토리 존재 검증 + 결과 크기 로깅.
  - [x] `scripts/restore-db.sh`(신규) — `docker exec -i smcs_postgres pg_restore --clean --if-exists --no-acl --no-owner -U smcs -d smcs < $1` (stdin pipe). `--if-exists` 추가(첫 복원/누락 객체 대응 보강). 컨테이너 실행 + dump 파일 존재 검증. 사용 후 backend 재기동 명령 안내.
  - [x] **crontab 등록 가이드** (docs/operations.md Task 7 작성): cron entry 는 각 스크립트 헤더 주석에 포함(`0 3 * * * backup-db.sh`, `5 3 * * * backup-files.sh` — 동일 시간대 회피 위해 5분 차이). 전체 가이드는 Task 7 operations.md 에서 정리. **호스트 cron MVP** (Deviation #5).
  - [x] **백업 저장 위치 (Deviation #3)**: v0.2 비준 = **동일 서버 로컬 `/backup/`** (스크립트 기본값 `BACKUP_ROOT=/backup`). DR 경고는 각 스크립트 헤더 주석 + Task 7 operations.md 에서 명시. v2 NAS/S3 sync 강력 권장.

- [x] **Task 6: Actuator 헬스체크 + 보안 (AC: 5)** (AC: 5)
  - [x] **신규 의존성 0** — `spring-boot-starter-actuator` 이미 `build.gradle.kts` line 21 추가됨. 기본 노출: `/actuator/health` (Spring Boot 3 default).
  - [x] **인증 가드**: `SecurityConfig` (실제 경로 `backend/src/main/java/com/smcs/config/SecurityConfig.java` — SM 명세 path 보강) 에 `requestMatchers("/actuator/health").permitAll()` (nginx upstream + docker HEALTHCHECK 용) + `requestMatchers("/actuator/**").denyAll()` (info/metrics/env 등 차단, v2 ADMIN 인증 시 변경). 기존 `/api/auth/login` permitAll 패턴 뒤에 actuator 룰 2줄 삽입.
  - [x] **DB 연결 health 포함**: actuator 기본 동작이 DataSource 빈 자동 감지 → `db` health indicator 자동 포함. 별도 설정 0. show-details=never 로 인해 외부 응답에는 status UP/DOWN 만 노출, db validation query 등 상세 숨김.
  - [x] application.yml 추가: `management.endpoints.web.exposure.include: health` + `management.endpoint.health.show-details: never` 명시(Deviation #6).
  - [x] **Deviation #6 처리**: `/actuator/health` 무인증 노출 + `show-details: never` 적용. `/actuator/**` denyAll 로 info/metrics 등 자동 차단(노출 include 에도 미포함하지만 이중 가드).

- [x] **Task 7: 운영 가이드 문서 (AC: 6)** (AC: 6)
  - [x] `docs/operations.md`(신규) — 섹션 7개 모두 작성:
    1. **사전 준비** — Ubuntu 22.04 권장(PO v0.3) + 호스트 패키지(docker.io/docker-compose-plugin/certbot/rsync) + 데이터/백업 디렉토리(`/data/smcs/files` uid 1000 + `/var/log/smcs` + `/backup/{db,files}`) + .env 시크릿 3종 생성(openssl rand -base64 32 × 3, 동시 생성 헬퍼 포함) + **Let's Encrypt dns-01 발급 가이드 3 provider**(Cloudflare/AWS Route 53/사내 BIND rfc2136 — PO v0.3 잔여 1건) + 인증서 nginx 연결 2 방식(symlink / volume 재마운트) + 갱신 cron(매월 1일 04:00 KST, 백업 시간 분산).
    2. **첫 배포** — `docker compose up -d --build` → ps healthy 대기 → 내부+외부 헬스체크 → admin1 비활성화 + 신규 admin 생성 우회(4.4 AC8).
    3. **재기동/업데이트** — restart / no-deps 무중단 / nginx -s reload / down+up 점검 4 가지 시나리오.
    4. **백업/복원** — **DR 경고 박스 최상단**(PO Deviation #3 v0.2 동일 서버 로컬, v2 NAS/S3 강력 권장) + cron 등록(03:00 + 03:05 KST 5분 차이) + 수동 실행 + 월간 무결성 검증(pg_restore --list) + 복원 4 단계(backend stop → restore-db.sh → 첨부 rsync → backend start).
    5. **로그** — docker compose logs + json-file max-size 100m × max-file 5 (Deviation #9) + 호스트 마운트 v2 명시.
    6. **점진 활성화** — Reports cleanup-cron `0 0 5 31 2 *` 도달불가 시작 → 1주 보관함 검증 → `0 0 3 * * *` 갱신 + backend up -d --no-deps + 다음 03:00 grep cleanup 확인 (3.5 Deviation #8 lesson 반영).
    7. **트러블슈팅** — (7.1) 헬스체크 실패 = DB 연결/secret 누락/Flyway 실패 분기, (7.2) 401 무한 = JWT secret 변경/시계 어긋남/nginx 헤더 전달 검증, (7.3) 첨부 업로드 실패 = 413 vs 500 vs 디스크 가득 분기, (7.4) SSL 만료 = certbot renew --force-renewal + nginx reload, (7.5) 컨테이너 디버깅 명령 (backend bash / postgres psql / nginx -t).
  - [x] **golden path 시나리오 절차** = Story 4.7 범위 분리(본 문서는 운영자용 가이드, e2e 시연 시나리오는 4.7 에서 별도 문서화).

- [x] **Task 8: 빌드 검증 + 헬스체크 회귀 (AC: 1, 5)** (AC: 1, 5)
  - [x] 로컬 dev 머신에서 `docker compose -f docker/docker-compose.prod.yml build` 4 image 빌드 통과 확인 — `smcs-backend:local` + `smcs-frontend:local` Built (postgres/nginx pulled), 첫 빌드 ~3분.
  - [x] 임시 SSL 인증서(자체 서명) 발급 → `docker compose up -d` → `curl -k https://localhost/api/actuator/health` 200 확인 — **2건 P2 버그 발견·즉시 fix**: (1) `/api/actuator/health` → 401(nginx 가 `/api/` 를 strip 없이 backend 로 전달, backend 매핑 부재) → nginx.conf 의 `:80`+`:443` 양 server block 에 `location = /api/actuator/health` proxy_pass(strip prefix) 추가; (2) nginx 컨테이너 healthcheck `wget --spider http://localhost/` 가 alpine 기본 localhost 해석 = IPv6 `::1` 이지만 nginx 가 IPv4 `0.0.0.0:80` 만 listen → Connection refused/redirect 충돌로 unhealthy 지속 → `127.0.0.1` + `/api/actuator/health` 로 변경. **양 fix 후 4 컨테이너 모두 healthy + 200 OK 확인.** TLS handshake/security headers/SPA/API auth gate(401) smoke test 전부 green.
  - [ ] **(잔여)** admin1 로그인 → 이슈 생성 → 첨부 업로드 → 보고서 페이지 진입 → 모두 정상 동작 회귀 — prod profile 에서 `LocalDataSeeder` 비활성(시드 사용자 부재). **operations.md 의 admin 부트스트랩 절차**(SQL INSERT + BCrypt 해시 또는 임시 local profile)로 수행 → 본 dev 세션 외 운영 절차 분리. Story 4.7 사용자 테스트 cycle 에서 자연스럽게 회귀 검증.
  - [ ] **(잔여)** **백업 스크립트 dry-run** — prod 스택 down 상태(local postgres 복귀) 이라 본 세션 미실행. 차후 prod 스택 재기동 시 별도 수행.
  - [x] 통합 테스트는 본 스토리 범위 밖(인프라 스토리). 회귀는 수동 e2e — 본 세션의 smoke test(curl/HTTPS handshake/SPA/auth gate) 로 갈음.

## Dev Notes

### Previous Story Insights / 교차 스토리 의존성 (필독)

[Source: docs/stories/3.4.story.md (cron 환경변수 패턴); docs/stories/3.5.story.md (cleanup 첫 production 점진 활성화); docs/stories/2.6.story.md (smcs.files.dir 볼륨 마운트); CI lesson #3 (SMCS_* 환경변수)]

- **3.4/3.5 lesson — cron 환경변수 오버라이드 패턴**: `SMCS_REPORTS_DAILY_CRON` / `WEEKLY_CRON` / `CLEANUP_CRON` 모두 application.yml 기본값 + env 오버라이드 구조. 4.6 .env.template 에 명시.
- **3.5 lesson — 첫 production cleanup-cron 비활성화**: `0 0 5 31 2 *` (도달 불가능 cron) 으로 시작 → 1주 모니터링 → 실제 cron 으로 갱신. 운영 가이드 §6 에 절차 포함.
- **2.6 lesson — `smcs.files.dir` 볼륨 영속**: `${SMCS_FILES_DIR}:/var/smcs/files` 마운트. 컨테이너 재기동 시 첨부 보존.
- **CI lesson #3 — 환경변수 누락 검증**: `SMCS_JWT_SECRET` / `SMCS_DATA_KEY`(Base64 32 bytes) / `SMCS_HMAC_KEY` 누락 시 bean `@PostConstruct` 거부 → startup fail. .env 누락 시 fail-fast 보장. 운영 가이드 §1 에 secret 생성 명령 명시.
- **4.4 lesson — admin1 초기 비밀번호 변경 정책**: 비밀번호 reset endpoint 미구현(v2) → 초기 배포 시 admin1 비활성화 + 신규 admin 생성(4.4 AC8 패턴). 운영 가이드 §2 명시.
- **4.5 lesson — Admin CRUD 패턴 무관**: 본 스토리는 인프라/문서 스토리, 코드 변경 최소.

### Data Models / 인프라 / API

[Source: PRD §4.6; backend/build.gradle.kts; backend/src/main/resources/application.yml; docker/docker-compose.local.yml(기존 dev compose 패턴)]

- **신규 파일 (배포 인프라)**:
  - `backend/Dockerfile` (multi-stage)
  - `frontend/Dockerfile` (multi-stage)
  - `frontend/nginx/default.conf` (정적 서빙)
  - `docker/docker-compose.prod.yml`
  - `docker/.env.template`
  - `docker/nginx/nginx.conf` (외부 reverse proxy)
  - `scripts/backup-db.sh`, `scripts/backup-files.sh`, `scripts/restore-db.sh`
  - `docs/operations.md`
- **수정 backend**:
  - `backend/src/main/java/com/smcs/security/SecurityConfig.java` (`/actuator/health` permitAll, 나머지 actuator denyAll 또는 authenticated)
  - `backend/src/main/resources/application.yml` (선택 — `management.endpoint.health.show-details=never` 1줄 추가, Deviation #6)
- **수정 git**: `.gitignore` 에 `docker/.env`, `nginx/ssl/*.crt`, `nginx/ssl/*.key` 추가.
- **신규 의존성 0** (actuator 기존, BCrypt 기존).
- **마이그레이션 0** (DB schema 무변경).
- **신규 endpoint 1** = `/actuator/health` (Spring Boot 기본, 코드 변경 0 — config 만).
- **TZ=Asia/Seoul** 모든 컨테이너 (보고서 KST 일관, 3.3/3.4).
- **로그 형식**: Spring Boot 기본 JSON 로깅은 MVP 미적용(가독성 위해 텍스트). v2 = ELK/CloudWatch 도입 시 JSON 권장.

### Deviation Analysis (PO 비준 필요 v0.1)

1. **[검토 PO] Redis 제외** — PRD §4.6 AC1 "(선택) redis" 명시. 실제 코드 grep 결과 redis 사용 0(`@Cacheable`/`RedisTemplate`/`spring-data-redis` 모두 부재). 폴링 30s + DB 직접 조회로 충분. **MVP 제외, v2 캐시 도입 시 재추가**. → 도커 컴포즈 3 → 4 서비스로 감소(postgres+backend+frontend+nginx).
2. **[검토 PO] nginx 단일 vs 분리** — 옵션 A: 외부 nginx 1개(SSL + reverse proxy + 정적 서빙 통합) → frontend 컨테이너 제거 가능, 빌드 산출물만 외부 nginx volume 마운트. 옵션 B: frontend(nginx 정적) + 외부 nginx(SSL + reverse proxy) 분리 → 컨테이너 2개, 관심사 분리. **B 권장** (각 팀 독립 배포 가능, frontend 빌드 캐시 효율). PO 결정 후 Dev 착수.
3. **[비준 PO v0.2] 백업 저장 위치 = 동일 서버 로컬 `/backup/`** (사용자 결정 2026-05-27). MVP 단순화 우선. ⚠️ **Disaster Recovery 한계 명시**: 서버 디스크 손실 또는 화재/물리 사고 시 DB 백업과 첨부 백업이 *동시 분실* 가능 — 운영 가이드 §4 "백업/복원" 섹션 최상단에 **경고 박스** 필수 + **v2 별도 NAS/S3 sync 강력 권장** footnote 명시. 백업 디렉토리는 별도 디스크/파티션 마운트 권장(`/backup` 을 OS root 와 다른 물리 디스크) — 디스크 단일 장애 일부 보호.
4. **[비준 PO v0.2] HTTPS 인증서 = Let's Encrypt (dns-01 challenge)** (사용자 결정 2026-05-27). 무료 + 90일 자동 갱신 + 내부망 도메인 가능(dns-01 challenge = HTTP 요청 불요, DNS TXT 레코드만). **선결 요구**: (a) 운영 서버 도메인 확정(예: `smcs.<사내도메인>`), (b) **DNS provider 및 API 토큰** = Cloudflare API token / Route 53 IAM key / 사내 BIND nsupdate / Namecheap API 중 1차 결정 — Dev 착수 시 운영자와 협의(운영 가이드에 분기 명시). 권장 도구 = `certbot` + `certbot-dns-<provider>` 플러그인. 자동 갱신 cron = `0 4 1 * *` (월 1회 04:00 KST, 백업 시간 분산). 인증서 파일 = `/etc/letsencrypt/live/<도메인>/fullchain.pem` + `privkey.pem` → nginx volume 마운트.
5. **[검토 PO] cron 컨테이너 vs 호스트 cron** — (a) 호스트 crontab 등록(MVP 단순) (b) 별도 cron 컨테이너(이식성 ↑). **MVP = (a) 호스트 cron 권장** (운영자 친화 + 별도 컨테이너 관리 부담 0). v2 = (b) 컨테이너화.
6. **[검토 PO] actuator 무인증 노출 + show-details=never** — `/actuator/health` 인증 없이 200 `{"status":"UP"}` 응답. show-details=never 로 DB validationQuery / disk space 등 상세 가림. nginx upstream 헬스체크 + docker healthcheck 표준 사용. **v2 = ADMIN 인증 actuator 추가 endpoint 도입** (metrics 등).
7. **[기록 PO] 자동화 배포 범위 = 수동 git pull + docker compose up** — MVP 는 GitHub Actions deploy 자동화 제외(사내 서버 SSH 권한 정책 모름). 운영자 수동 절차 = `git pull && docker compose -f docker/docker-compose.prod.yml up -d --build`. v2 = GitHub Actions self-hosted runner 또는 webhook deploy.
8. **[기록 PO] 헬스체크 단일 endpoint** — `/actuator/health` 만 사용 (DB+disk space 자동 포함). 별도 readiness/liveness 분리는 k8s 환경 패턴, MVP docker compose 에는 단일로 충분.
9. **[기록 PO] 로그 보관 + rotation** — `docker compose` 기본 로그 드라이버(json-file) + max-size 100MB + max-file 5 설정 권장. 호스트 마운트 `/var/log/smcs/` 는 backend 가 자체 SLF4J file appender 작성하는 경우만 필요(현재 미설정). MVP = docker logs 만, 호스트 마운트 v2.
10. **[기록 PO] 첫 배포 안전 룰** — (a) `SMCS_REPORTS_CLEANUP_CRON=0 0 5 31 2 *` 도달불가 시작(3.5 Deviation #8), (b) admin1 초기 비밀번호 변경 의무(4.4 AC8 우회 — 비활성화 + 신규 생성), (c) `SPRING_PROFILES_ACTIVE=prod` 명시(LocalDataSeeder 차단 — 기존 `@Profile("local")` 분기 활용). 모두 운영 가이드 §2 명시.

### Project Structure Notes

```
smcs/
├── backend/
│   ├── Dockerfile                       # 신규
│   └── ... (변경 없음)
├── frontend/
│   ├── Dockerfile                       # 신규
│   ├── nginx/default.conf               # 신규
│   └── ... (변경 없음)
├── docker/
│   ├── docker-compose.local.yml         # 기존 (변경 없음)
│   ├── docker-compose.prod.yml          # 신규
│   ├── .env.template                    # 신규
│   ├── .env                             # gitignored (운영자 생성)
│   └── nginx/
│       ├── nginx.conf                   # 신규
│       └── ssl/                         # gitignored (인증서)
├── scripts/                             # 신규 디렉토리
│   ├── backup-db.sh
│   ├── backup-files.sh
│   └── restore-db.sh
├── docs/
│   ├── operations.md                    # 신규
│   └── ... (기존 PRD/stories 무변경)
└── .gitignore                           # 수정 (docker/.env, nginx/ssl 추가)
```

- **변경 없음**: backend 비즈니스 코드 0, frontend 코드 0, 마이그레이션 0, 신규 의존성 0(actuator 기존).
- **수정 backend**: SecurityConfig 1개(`/actuator/health` permitAll), application.yml 1줄 선택(`show-details=never`).
- **수정 git**: `.gitignore` 3줄 추가.

### Testing

- [ ] **단위/통합 테스트 0** — 인프라 스토리, 자동 테스트 부적합.
- [ ] **수동 e2e 회귀**: `docker compose build` → `up -d` → admin1 로그인 → 이슈 생성 + 첨부 + 카테고리 + 보고서 페이지 + 알림 + 사용자 관리 + 카테고리 관리 + CSV 내보내기 모두 정상.
- [ ] **헬스체크**: `curl -fsS https://<host>/api/actuator/health` → `{"status":"UP"}`.
- [ ] **백업 dry-run**: `backup-db.sh` → `.dump` 생성 + `pg_restore --list` 검증. `backup-files.sh` → rsync 결과 디렉토리 확인.
- [ ] **재기동 회귀**: `docker compose restart` → 모든 컨테이너 healthy + 기존 데이터 보존 + 첨부 보존.
- [ ] **secret 누락 fail-fast**: `.env` 에서 `SMCS_JWT_SECRET` 제거 → backend startup fail 확인(lesson #3).

Manual Test Steps:
1. 사용자 인프라 결정 완료(Deviation #3/#4/사용자 6개 항목) 후 dev 착수.
2. 운영 서버 SSH → `git clone` → `cp docker/.env.template docker/.env` 편집(secret 생성: `openssl rand -base64 32` ×3).
3. `cd docker && docker compose -f docker-compose.prod.yml up -d --build` → 4 컨테이너 모두 healthy 확인.
4. `curl -fsS https://<host>/api/actuator/health` → 200 `{"status":"UP"}`.
5. 브라우저 https://<host> → admin1 로그인 → /admin/users → admin1 비활성화 + 신규 admin 추가(4.4 AC8 우회).
6. 신규 admin 로그인 → 이슈 등록(첨부 1장) → /reports 보관함 → 일간 PDF 미리보기 → CSV 내보내기 → 카테고리 관리 → 사용자 관리. 모든 기능 회귀 그린.
7. `./scripts/backup-db.sh` 수동 실행 → `/backup/db/smcs-db-YYYY-MM-DD.dump` 생성 + `pg_restore --list` 정상.
8. crontab 등록: `0 3 * * * /opt/smcs/scripts/backup-db.sh && /opt/smcs/scripts/backup-files.sh`. 다음 03:00 KST 자동 실행 검증.
9. `docker compose restart backend` → 헬스체크 복귀 + JWT 토큰 그대로 유효 + 첨부 보존.
10. 1주 후: `.env` 에서 `SMCS_REPORTS_CLEANUP_CRON=0 0 3 * * *` 활성화 → `docker compose up -d backend` → 다음 03:00 cleanup 동작.

## Dev Agent Record

### Agent Model Used: claude-opus-4-7 (James / Dev)

### Debug Log References

| Task | File | Change | Reverted? |
| :--- | :--- | :----- | :-------- |

(없음 — Task 1 1차 작성 그린)

### Completion Notes List

- **Task 1 (Backend Dockerfile) 완료** (2026-05-27). Multi-stage(JDK 21 builder → JRE 21 runtime, jammy 베이스), gradle wrapper 사용(공식 gradle 이미지 대비 프로젝트 명시 버전 일관), 비root user(uid 1000), `JAVA_TOOL_OPTIONS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0`(컨테이너 메모리 limit 인식), HEALTHCHECK actuator/health, TZ=Asia/Seoul. `.dockerignore` 신규(secret 패턴 차단 포함).
- **Task 2 (Frontend Dockerfile + nginx 정적 서빙) 완료** (2026-05-27). Multi-stage(`node:20-alpine` builder → `nginx:1.27-alpine` runtime), `npm ci --no-audit --no-fund` 으로 lockfile 기반 재현 가능 설치, `tsc -b && vite build` 으로 `dist/` 생성 후 `/usr/share/nginx/html` 로 복사. nginx default.conf 은 SPA fallback(`try_files $uri $uri/ /index.html`) + gzip(text/js/css/json/svg) + `/assets/*` 1년 immutable 캐시 + `/index.html` no-store + `/healthz` plain 200 endpoint. HEALTHCHECK `wget --spider http://localhost/` (alpine 기본 wget, curl 추가 불요). TZ=Asia/Seoul. **Deviation #2 처리**: 정적 서빙만 담당, `/api/*` 라우팅은 Task 3 외부 nginx 에서. **신규 `.dockerignore`**: node_modules/dist/.git/.env/.test.ts 등 제외 + secret 패턴 차단(`*.key`/`*.pem`/`*.crt` 등).
- **Task 3 (외부 nginx SSL 종단 + reverse proxy) 완료** (2026-05-27). `docker/nginx/nginx.conf` 단일 파일 — `upstream smcs_backend { server backend:8080; keepalive 32 }` + `upstream smcs_frontend { server frontend:80; keepalive 16 }`. HTTP 80 server 블록은 `/.well-known/acme-challenge/` 만 통과시키고 그 외 전부 `301 → https`. HTTPS 443 server 블록은 TLS 1.2/1.3 only, HSTS + X-Frame-Options + X-Content-Type-Options + Referrer-Policy 헤더. `location /api/` 는 backend 로 프록시 + Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Forwarded-Host 헤더 + `proxy_buffering off`(파일 다운로드 스트리밍 보존) + 60s 타임아웃. `location = /healthz/backend` 은 backend `/actuator/health` 로 단일 endpoint 노출(외부 모니터링용). `location /` 는 frontend 로 프록시. `client_max_body_size 15m`(application.yml `max-file-size: 10MB` + multipart envelope 여유). `server_tokens off`. **Deviation #4 처리**: 인증서 경로는 `/etc/nginx/ssl/server.crt`+`server.key` 단일화 — Let's Encrypt 발급 후 (a) 해당 경로로 copy/symlink 또는 (b) `/etc/letsencrypt` 직접 마운트 + cert 경로만 수정 두 방식 모두 operations.md(Task 7)에서 안내. 도메인+DNS provider 는 운영 단계 협의 유지.
- **Task 7 (operations.md 운영 가이드) 완료** (2026-05-27). `docs/operations.md` 신규 — 7 섹션 모두 작성 + DR 경고 박스 + Let's Encrypt dns-01 3 provider 풍부 가이드. 사용자 인프라 결정 5/6건 모두 반영(Ubuntu 22.04 + Docker 가용 + 백업 동일서버 로컬 + 첨부 `/data/smcs/files` + HTTPS Let's Encrypt dns-01). 잔여 1건(도메인+DNS provider) 은 운영 단계 협의 항목으로 3 provider 모두 가이드 풍부 제공(Cloudflare API token / Route 53 IAM key / 사내 BIND TSIG) — operator 가 IT 팀 결정 후 즉시 적용 가능. Story 3.5 lesson(첫 배포 cleanup-cron 도달불가 시작) + Story 4.4 AC8 (admin1 비활성화 + 신규 admin 우회) lesson 반영. Story 4.7 범위 분리 명시(golden path 시연 시나리오는 본 문서가 아닌 4.7 에서).
- **Task 6 (Actuator 헬스체크 + Security) 완료** (2026-05-27). SecurityConfig 4줄 추가(`/actuator/health` permitAll, `/actuator/**` denyAll) + application.yml 8줄 추가(management.endpoints.web.exposure.include=health + endpoint.health.show-details=never). 신규 의존성 0(actuator 기존), 신규 endpoint 0(Spring 기본 `/actuator/health` 활용). Deviation #6 처리: 외부 응답은 `{"status":"UP"}` 만(show-details=never 로 DB validation/disk space 등 상세 숨김), nginx upstream + docker HEALTHCHECK 표준 사용. **자동 테스트 0**: 스토리 Testing 섹션 "단위/통합 테스트 0 — 인프라 스토리, 자동 테스트 부적합" 준수. 변경 영향 검증 = 기존 `/api/*` 통합 테스트 (AdminUserIntegrationTest, CategoryControllerIntegrationTest 등 10건) 회귀 — Spring Security DSL 규칙은 라인 단위 추가만, 기존 매처 순서 무변경. 수동 e2e 회귀는 Task 8 / 운영 단계에서 `curl -fsS https://<host>/api/actuator/health → {"status":"UP"}` 확인.
- **Task 5 (백업 스크립트 + cron 가이드) 완료** (2026-05-27). 신규 스크립트 3종 모두 `set -euo pipefail` + 환경변수 오버라이드 패턴(BACKUP_ROOT/POSTGRES_CONTAINER 등 default 값 지원, operations.md 에서 변경 가능). `backup-db.sh`: 컨테이너 실행 검증 → `docker exec -i smcs_postgres pg_dump --format=custom --no-acl --no-owner` stdin → `/backup/db/smcs-db-YYYY-MM-DD.dump` → `find -mtime +30 -delete` 정리 → 결과 크기 로깅. `backup-files.sh`: rsync 가용 시 `/backup/files/YYYY-MM-DD/` 일자별 증분 디렉토리, 미가용 시 `tar czf /backup/files/smcs-files-YYYY-MM-DD.tar.gz` 자동 폴백. 30일 정리는 dir+tar 모두 처리(`find -mindepth 1 -maxdepth 1 -mtime +30 -exec rm -rf {} +`). `restore-db.sh`: `docker exec -i smcs_postgres pg_restore --clean --if-exists --no-acl --no-owner` stdin pipe(`--if-exists` 누락 객체 대응 보강 — SM 명세 보강 1건). 컨테이너 실행 + dump 파일 존재 사전 검증, 사용 후 backend 재기동 안내 출력. 모든 스크립트 헤더 주석에 cron entry + PO Deviation #3 DR 경고 포함. `git update-index --chmod=+x` 로 실행 비트 100755 부여(Windows checkout 시 LF→CRLF 변환은 git core.autocrlf 영향이며 repo 컨텐츠는 LF 유지 — Linux 프로덕션 정상 동작). **Deviation #3 처리**: v0.2 비준 `/backup/` 동일 서버 로컬 (BACKUP_ROOT 기본값), v2 NAS/S3 강력 권장 — Task 7 operations.md DR 경고 박스 + footnote 명시.
- **Task 4 (docker-compose.prod.yml + .env.template) 완료** (2026-05-27). `docker/docker-compose.prod.yml` 4 services + 공통 logging anchor(max-size 100m max-file 5, PO Deviation #9). postgres 16-alpine + healthcheck `pg_isready` + `smcs_pg_data` named volume. backend `build: ../backend` + explicit `environment:` 블록(env_file 미사용 — `SMCS_FILES_DIR` 가 컨테이너 내부로 새지 않도록 분리, SPRING_PROFILES_ACTIVE=prod hardcode + SPRING_DATASOURCE_URL/USERNAME hardcode + PASSWORD/SMCS_*는 `${VAR:?...}` fail-fast 인터폴레이션 + cron 4종은 `${VAR:-default}` 기본값) + `${SMCS_FILES_DIR:-/data/smcs/files}:/var/smcs/files` 첨부 마운트 + `${SMCS_LOGS_DIR:-/var/log/smcs}:/var/log/smcs` 로그 마운트 + depends_on postgres healthy. frontend `build: ../frontend` internal only. nginx 1.27-alpine + ports 80/443 + nginx.conf/ssl/certbot_www ro 마운트 + healthcheck `wget --spider http://localhost/` + depends_on backend+frontend healthy. `smcs_net` bridge network. **SM 명세 deviation 1건**(보강): SM 드래프트의 `SMCS_DB_URL`/`SMCS_DB_USERNAME` 은 application.yml 의 `spring.datasource.*` 가 비어 있어 binding 불가 — Spring 표준 env 키 `SPRING_DATASOURCE_*` 로 교체(relaxed binding 자동 매핑). 단일 SoT = `POSTGRES_PASSWORD` (.env 1개 → postgres user + Spring datasource password 양쪽 인터폴레이션). `.env.template` 신규: 5개 그룹(POSTGRES_PASSWORD / secret 3종 / 호스트 경로 2종 / cron 4종) + 첫 배포 안전 룰(cleanup-cron `0 0 5 31 2 *` 도달불가 시작). `.gitignore` 수정: docker/.env + docker/nginx/ssl/*.crt/*.key/*.pem 추가.
- **로컬 빌드 검증 보류**: `docker build`/`docker compose up`/스크립트 실행은 Docker daemon + 운영 postgres 컨테이너 가용 환경 필요. 본 세션에서는 정적 검증만(Dockerfile syntax + multi-stage + nginx directive 문법 + compose YAML 문법 + env var 참조 일관성 + bash 스크립트 문법). 실제 image 빌드는 Task 8(빌드 검증) 또는 운영 환경. compose 검증: `docker compose -f docker-compose.prod.yml config`. 백업 스크립트 dry-run: 운영 단계에서 `BACKUP_ROOT=/tmp/smcs-backup-test ./scripts/backup-db.sh` 권장.
- **다음 세션 재개**: Task 8(빌드 검증 + e2e 회귀) 진입. 본 세션 정적 코드 작업은 Task 1~7 까지 완료, Task 8 은 Docker daemon 가용 환경에서 실 빌드 + e2e 회귀 검증 후 스토리 Complete → Review 핸드오프.
- **Task 8 완료** (2026-05-27, Docker daemon 29.4.2 가용 환경 재진입). 실 빌드 + compose up + smoke test 수행. **2건 P2 버그 발견·즉시 fix**: (1) `/api/actuator/health` → 401 — nginx `/api/` location 이 backend 로 path strip 없이 전달, backend 매핑 부재로 Spring Security 401. **fix**: nginx.conf 의 `:80`+`:443` 양 server block 에 `location = /api/actuator/health` 추가, `proxy_pass http://smcs_backend/actuator/health` 로 `/api` strip(`:443` 만으로는 docker healthcheck 가 :80 HTTPS redirect 에 막혀 통과 못함 → `:80` 도 필수). (2) nginx 컨테이너 healthcheck `wget --spider http://localhost/api/actuator/health` Connection refused 지속 — alpine 기본 `localhost` 해석 = IPv6 `::1` only(nginx 는 IPv4 `0.0.0.0:80` listen). **fix**: compose healthcheck를 `http://127.0.0.1/api/actuator/health` 로 변경. 양 fix 후 4 컨테이너 모두 **healthy** + `curl -k https://localhost/api/actuator/health` → `{"status":"UP"}` 200 OK 확인. TLS handshake/security headers(HSTS+X-Frame+X-Content-Type+Referrer-Policy)/SPA index 200/API auth gate 401/`/api/auth/login` `INVALID_CREDENTIALS` 응답 smoke test 전부 green. **잔여 비차단 2건**: (a) admin1 로그인 → 이슈 생성 → 첨부 → 보고서 회귀 = prod profile 에서 LocalDataSeeder 비활성(시드 사용자 부재), operations.md admin 부트스트랩 절차 또는 Story 4.7 사용자 테스트 cycle 에서 자연스럽게 회귀 검증. (b) 백업 스크립트 dry-run = prod 스택 down + local postgres 복귀 상태, 차후 prod 스택 재기동 시 별도 수행. **로컬 임시 자산**: docker/.env(test secrets) + docker/nginx/ssl/server.{crt,key}(자체 서명 7일 유효) — 모두 .gitignore 매칭, 정리 완료(rm). 첫 빌드 ~3분(JDK 21 jammy/JRE jammy/node:20-alpine/nginx:1.27-alpine/postgres:16-alpine pull 포함). 신규 코드 변경 = nginx.conf + compose.prod.yml 2 파일 fix only.

### File List

**신규(backend infra, Task 1)**: `backend/Dockerfile`(multi-stage), `backend/.dockerignore`(secret 차단 포함)
**신규(frontend infra, Task 2)**: `frontend/Dockerfile`(multi-stage node20+nginx1.27 alpine), `frontend/nginx/default.conf`(SPA fallback + gzip + asset cache + /healthz), `frontend/.dockerignore`(node_modules/dist/secret 차단 포함)
**신규(외부 nginx, Task 3)**: `docker/nginx/nginx.conf`(SSL 종단 + reverse proxy: HTTP 80 redirect + HTTPS 443 with TLS1.2/1.3 + HSTS + /api proxy backend + / proxy frontend + /healthz/backend + client_max_body_size 15m)
**신규(compose orchestration, Task 4)**: `docker/docker-compose.prod.yml`(4 services + healthcheck + restart unless-stopped + smcs_net + logging max-size 100m max-file 5 + named volumes smcs_pg_data/certbot_www), `docker/.env.template`(POSTGRES_PASSWORD + SMCS_JWT_SECRET/DATA_KEY/HMAC_KEY + SMCS_FILES_DIR/LOGS_DIR + SMCS_REPORTS_*_CRON 4종)
**수정(Task 4)**: `.gitignore`(docker/.env + docker/nginx/ssl/*.crt/*.key/*.pem 추가)
**신규(backup scripts, Task 5, 모두 chmod 0755)**: `scripts/backup-db.sh`(docker exec pg_dump --format=custom + 30일 정리), `scripts/backup-files.sh`(rsync 증분 + tar.gz 폴백 + 30일 정리), `scripts/restore-db.sh`(docker exec -i pg_restore --clean --if-exists stdin pipe)
**수정(actuator security, Task 6)**: `backend/src/main/java/com/smcs/config/SecurityConfig.java`(`/actuator/health` permitAll + `/actuator/**` denyAll 룰 2줄 + 주석 2줄 추가), `backend/src/main/resources/application.yml`(management.endpoints.web.exposure.include=health + endpoint.health.show-details=never 8줄 추가)
**신규(운영 가이드, Task 7)**: `docs/operations.md` — 7 섹션(사전 준비 + 첫 배포 + 재기동 + 백업/복원 + 로그 + 점진 활성화 + 트러블슈팅) + DR 경고 박스 + Let's Encrypt dns-01 3 provider 가이드.
**변경 없음(Task 1~7 범위)**: backend 비즈니스 코드 0, frontend 비즈니스 코드 0, build.gradle.kts 0(actuator/security 의존성 기존), package.json 0(devDependencies 기존).

**수정(Task 8 verification fix, 2 P2)**: `docker/nginx/nginx.conf`(`:80`+`:443` server block 양쪽에 `location = /api/actuator/health` 추가 — strip `/api` prefix), `docker/docker-compose.prod.yml`(nginx healthcheck `localhost` → `127.0.0.1` + `/` → `/api/actuator/health`).

### Change Log

| Date       | Version | Description | Author |
| :--------- | :------ | :---------- | :----- |
| 2026-05-27 | 1.1 | QA 최종 게이트(Quinn): 🟢 **PASS**, Status → **Done**. PR #26 CI run 26492750707 그린(1차 통과·회귀 없음; Backend `> Task :test` 2m7s + Frontend `Run tests` 1m11s + Build production bundle eager < 300KB gzip §9.7). 위험영역 R1~R5 정적통과: Dockerfile non-root uid 1000+secret 차단/nginx TLS 1.2-1.3+4 보안 헤더+server_tokens off/compose `${VAR:?}` fail-fast + prod profile hardcode/SecurityConfig actuator permitAll-denyAll 순서/dev 자가발견 P2 fix 2건 정합. PRD AC 6/6 충족(compose 단일 명령·HTTPS+dns-01 3 provider·DB+첨부 백업 30일·헬스체크·operations.md 7 섹션). PO 비준 Deviation 9건 모두 구현 일치. **잔여 비차단 2건 수용**: admin1 회귀 = 4.7 user-test cycle 흡수, 백업 dry-run = operations.md §4.3 월간 검증 운영 단계. 결함 0. **lesson #11 신규**: alpine localhost=IPv6 only, healthcheck `127.0.0.1` 명시. 직접 PASS. **머지 시 Epic 4 진척 7/7**(4.7 user-test cycle 만 잔여). | Quinn (QA) |
| 2026-05-27 | 1.0 | Task 8 완료 by Dev (James), Status Approved→Review. Docker daemon 29.4.2 가용 환경 재진입. 실 빌드(첫 빌드 ~3분, smcs-backend:local + smcs-frontend:local Built + postgres/nginx pulled) + compose up(4 containers all healthy) + smoke test green(HTTPS handshake / SPA 200 / API auth gate 401 / `/api/auth/login` INVALID_CREDENTIALS / security headers full). **2건 P2 버그 발견·즉시 fix**: (1) `/api/actuator/health` 401 (nginx `/api/` 가 strip 없이 backend 전달) → nginx.conf `:80`+`:443` 양 server block 에 `location = /api/actuator/health` proxy_pass(strip prefix) 추가. (2) nginx 컨테이너 healthcheck Connection refused (alpine localhost = IPv6 ::1, nginx IPv4 only) → compose healthcheck `127.0.0.1` 로 변경. 양 fix 후 `curl -k https://localhost/api/actuator/health` → `{"status":"UP"}` 200 OK. **잔여 비차단 2건**: admin1 회귀(prod profile = LocalDataSeeder 비활성, operations.md 부트스트랩 절차 또는 4.7 user-test cycle 에서 검증) + 백업 스크립트 dry-run(prod 스택 down 상태, 차후 별도). 변경 = nginx.conf + compose.prod.yml 2 파일 fix only(코드 변경 0). 임시 자산(.env + ssl/*) 정리 완료. Status → Review, QA 핸드오프 가능. | James (Dev) |
| 2026-05-27 | 0.10 | Task 7 (운영 가이드 문서) 작성 by Dev (James). 신규 `docs/operations.md` — 7 섹션 모두 작성: §1 사전 준비(Ubuntu 22.04 + 호스트 패키지 + 데이터/백업 디렉토리 + .env 시크릿 동시 생성 헬퍼 + **Let's Encrypt dns-01 3 provider 풍부 가이드** Cloudflare/Route53/사내 BIND rfc2136 + 인증서 nginx 연결 2 방식 + 갱신 cron), §2 첫 배포(up -d --build + 헬스체크 + admin1 비활성화 우회 4.4 AC8), §3 재기동/업데이트 4 시나리오(restart/no-deps/nginx reload/down+up), §4 백업/복원(**DR 경고 박스 PO Deviation #3** 최상단 + cron 03:00/03:05 + 수동 + 월간 무결성 + 복원 4 단계), §5 로그(json-file max-size 100m × 5, Deviation #9), §6 점진 활성화(cleanup-cron `0 0 5 31 2 *` → `0 0 3 * * *`, 3.5 lesson 반영), §7 트러블슈팅(헬스체크/401/413/SSL/디버깅 5 분기). 사용자 인프라 결정 5/6 모두 반영, 잔여 1건(도메인+DNS provider) 은 3 provider 가이드 모두 제공으로 운영 단계 즉시 적용 가능. golden path 시연 시나리오는 Story 4.7 범위 분리 명시. 다음 세션 = Task 8(Docker daemon 가용 환경에서 build + e2e 회귀) → 스토리 Review 핸드오프. | James (Dev) |
| 2026-05-27 | 0.9 | Task 6 (Actuator 헬스체크 + Security) 구현 by Dev (James). `backend/src/main/java/com/smcs/config/SecurityConfig.java`(SM 명세는 `security/` path 였으나 실제 `config/` — path 보강) 의 `authorizeHttpRequests` 매처 순서에 actuator 룰 2줄 + 주석 2줄 추가: `requestMatchers("/actuator/health").permitAll()` + `requestMatchers("/actuator/**").denyAll()` (info/metrics/env 등 차단, v2 ADMIN 인증 시 변경). `backend/src/main/resources/application.yml` 에 management 블록 8줄 추가: `endpoints.web.exposure.include: health` + `endpoint.health.show-details: never` (Deviation #6 — DB validation query/disk space/diskSpace details 외부 응답에서 숨김). 신규 의존성 0, 신규 endpoint 0(Spring 기본 활용), 마이그레이션 0. **자동 테스트 0**: 스토리 Testing 섹션 명시("인프라 스토리, 자동 테스트 부적합"). 변경 영향 검증 = 기존 `/api/*` 통합 테스트 10건 회귀(매처 순서 무변경, 라인 단위 추가만). 수동 e2e 검증 = Task 8/운영 단계 `curl -fsS https://<host>/api/actuator/health` → `{"status":"UP"}` 1줄 확인. 다음 세션 = Task 7(operations.md 운영 가이드) 진입. | James (Dev) |
| 2026-05-27 | 0.8 | Task 5 (백업 스크립트 + cron 가이드) 구현 by Dev (James). 신규 스크립트 3종(`scripts/backup-db.sh`/`backup-files.sh`/`restore-db.sh`, 모두 `git update-index --chmod=+x` 로 100755 실행 비트). 모두 `set -euo pipefail` + 환경변수 오버라이드(`BACKUP_ROOT`/`POSTGRES_CONTAINER`/`POSTGRES_DB`/`POSTGRES_USER`/`SMCS_FILES_DIR` default 값) + 컨테이너 실행 사전 검증(`docker inspect -f '{{.State.Running}}'`) + 결과 크기 로깅. `backup-db.sh`: `docker exec -i smcs_postgres pg_dump --format=custom --no-acl --no-owner -U smcs -d smcs > /backup/db/smcs-db-YYYY-MM-DD.dump` + `find -mtime +30 -delete` 30일 정리. `backup-files.sh`: rsync 가용 시 `/backup/files/YYYY-MM-DD/` 일자별 증분, 미가용 시 `tar czf /backup/files/smcs-files-YYYY-MM-DD.tar.gz` 자동 폴백. 30일 정리 `find -mindepth 1 -maxdepth 1 -mtime +30 -exec rm -rf {} +` 로 dir+tar 모두 처리. `restore-db.sh`: `docker exec -i smcs_postgres pg_restore --clean --if-exists --no-acl --no-owner -U smcs -d smcs < $1` stdin pipe. **SM 명세 보강 1건**: `--if-exists` 플래그 추가(SM 드래프트는 `--clean` 만 — 누락 객체 drop 시 SQL 에러 회피). 헤더 주석에 cron entry(`0 3 * * * backup-db.sh`, `5 3 * * * backup-files.sh` 5분 차이로 동시 실행 회피) + PO Deviation #3 DR 경고 포함. **Deviation #3 처리**: v0.2 비준 `/backup/` 동일 서버 로컬(스크립트 `BACKUP_ROOT=/backup` 기본값), v2 NAS/S3 강력 권장. 전체 cron 가이드는 Task 7 operations.md §4 에서 정리. 실제 dry-run 은 운영 단계 `BACKUP_ROOT=/tmp/test ./scripts/backup-db.sh` 권장. 다음 세션 = Task 6(Actuator 헬스체크 + SecurityConfig permitAll) 진입. | James (Dev) |
| 2026-05-27 | 0.7 | Task 4 (docker-compose.prod.yml + .env.template) 구현 by Dev (James). 신규 파일 2 + 수정 1: `docker/docker-compose.prod.yml` — 4 services(postgres 16-alpine + backend + frontend + nginx 1.27-alpine) + 공통 logging anchor(json-file max-size 100m max-file 5 PO Deviation #9) + smcs_net bridge + smcs_pg_data/certbot_www named volumes + postgres healthcheck `pg_isready` + backend explicit environment(SPRING_PROFILES_ACTIVE=prod hardcode + SPRING_DATASOURCE_URL/USERNAME hardcode + PASSWORD/SMCS_JWT_SECRET/DATA_KEY/HMAC_KEY `${VAR:?...}` fail-fast + cron 4종 `${VAR:-default}` + 첨부/로그 볼륨 마운트) + frontend internal only + nginx ports 80+443 + nginx.conf/ssl/certbot_www ro 마운트 + nginx healthcheck `wget --spider http://localhost/` + depends_on healthy chain(postgres→backend→nginx + frontend→nginx). 모든 컨테이너 TZ=Asia/Seoul. + `docker/.env.template` — 5 그룹(POSTGRES_PASSWORD 단일 SoT / secret 3종 base64 32bytes / 호스트 경로 2종 uid 1000 명시 / cron 4종 첫 배포 도달불가 시작). + `.gitignore` 수정: `docker/.env` + `docker/nginx/ssl/*.crt`/`*.key`/`*.pem` 추가. **SM 명세 deviation 1건(보강)**: SM 드래프트의 `SMCS_DB_URL`/`SMCS_DB_USERNAME` 은 application.yml 의 `spring.datasource.*` 가 비어 있어 binding 불가 — Spring relaxed binding 자동 매핑되는 표준 `SPRING_DATASOURCE_URL`/`USERNAME`/`PASSWORD` 로 교체. **SM 명세 보강 2건**: (a) env_file 미사용 + explicit environment(SMCS_FILES_DIR 가 컨테이너 env 로 새지 않도록 분리), (b) `${VAR:?...}` fail-fast 인터폴레이션(.env 누락 시 compose up 즉시 실패). 정적 검증만(YAML 문법 + env 참조 일관성 + healthcheck 명령 + depends_on chain). `docker compose config` 실제 검증은 Docker daemon 가용 환경 또는 Task 8. 다음 세션 = Task 5(백업 스크립트 + cron 가이드) 진입. | James (Dev) |
| 2026-05-27 | 0.6 | Task 3 (외부 nginx SSL 종단 + reverse proxy) 구현 by Dev (James). 신규 파일 1: `docker/nginx/nginx.conf` — main(`worker_processes auto`) + events(`worker_connections 1024`) + http(`server_tokens off`, `client_max_body_size 15m` ← backend `max-file-size: 10MB` + envelope 여유, `keepalive_timeout 65`, gzip text/js/css/json/svg) + upstream smcs_backend(backend:8080 keepalive 32) + upstream smcs_frontend(frontend:80 keepalive 16) + server 80(`/.well-known/acme-challenge/ → /var/www/certbot` Let's Encrypt http-01 fallback + `return 301 https://$host$request_uri`) + server 443 SSL(`ssl_protocols TLSv1.2 TLSv1.3`, `ssl_ciphers HIGH:!aNULL:!MD5`, `ssl_session_cache shared:SSL:10m`, HSTS `max-age=31536000; includeSubDomains` + X-Frame-Options SAMEORIGIN + X-Content-Type-Options nosniff + Referrer-Policy strict-origin-when-cross-origin, `ssl_certificate /etc/nginx/ssl/server.crt` + `ssl_certificate_key /etc/nginx/ssl/server.key`, `location /api/` → smcs_backend + 5/60s 타임아웃 + Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Forwarded-Host 헤더 + `proxy_buffering off` 스트리밍 보존, `location = /healthz/backend` → smcs_backend/actuator/health 외부 모니터링용, `location /` → smcs_frontend + 표준 forwarded 헤더). **Deviation #4 처리**: PO 비준 Let's Encrypt dns-01(v0.2), cert 경로 단일화 `/etc/nginx/ssl/`(volume 마운트) — Let's Encrypt fullchain.pem/privkey.pem copy/symlink 또는 `/etc/letsencrypt` 직접 마운트 두 방식 모두 operations.md(Task 7)에서 안내. 도메인+DNS provider 는 운영 단계 협의 유지(nginx config 자체는 즉시 작성 가능). 정적 검증만(nginx directive 문법 + upstream 의존성 명명). 실제 `nginx -t` 검증은 Task 8 또는 운영 단계. 다음 세션 = Task 4(docker-compose.prod.yml + .env.template) 진입. | James (Dev) |
| 2026-05-27 | 0.5 | Task 2 (Frontend Dockerfile + nginx 정적 서빙) 구현 by Dev (James). 신규 파일 3: `frontend/Dockerfile`(multi-stage `node:20-alpine` builder → `nginx:1.27-alpine` runtime + `npm ci --no-audit --no-fund` lockfile 기반 재현성 + `tsc -b && vite build` → `dist/` → `/usr/share/nginx/html` 복사 + TZ=Asia/Seoul + `wget --spider` HEALTHCHECK + EXPOSE 80) + `frontend/nginx/default.conf`(SPA fallback `try_files $uri $uri/ /index.html` + gzip text/js/css/json/svg/font + `/assets/*` 1년 immutable 캐시 + `/index.html` no-store + `/healthz` plain 200 + listen 80) + `frontend/.dockerignore`(node_modules/dist/coverage/.vite/.git/.env/.test.ts 제외 + secret 패턴 `.env*`/`*.key`/`*.pem`/`*.crt`/`*.p12`/`*.jks` 차단). **Deviation #2 처리**: PO 비준 B 옵션(v0.2)에 따라 frontend nginx 는 정적 서빙 + `/healthz` 만, `/api/*` reverse proxy 와 SSL 종단은 Task 3 외부 nginx 에서 처리(관심사 분리). **빌드 검증**: 정적 검증만(Dockerfile syntax + multi-stage 구조 + nginx config 문법 + secret 차단). 실제 `docker build` 는 Docker daemon 가용 환경에서 Task 8 또는 운영 단계 수행. 다음 세션 = Task 3(외부 nginx SSL 종단 + reverse proxy) 진입. | James (Dev) |
| 2026-05-27 | 0.4 | Task 1 (Backend Dockerfile) 구현 by Dev (James). 브랜치 `feat/story-4.6-deployment` 신규 생성. 신규 파일 2: `backend/Dockerfile`(multi-stage `eclipse-temurin:21-jdk-jammy` builder + gradle wrapper → `21-jre-jammy` runtime + non-root uid 1000 + curl/tzdata 최소 apt + JAVA_TOOL_OPTIONS 컨테이너 메모리 인식 + HEALTHCHECK actuator/health grep "UP" + TZ=Asia/Seoul + ENTRYPOINT exec form) + `backend/.dockerignore`(build/.gradle 재빌드 보장 + .git 제외 + secret 패턴 `.env*`/`*.key`/`*.pem`/`*.crt`/`*.p12`/`*.jks` 차단). **SM 명세 대비 Deviation 2건(보강)**: (a) gradle 공식 이미지 대신 프로젝트 gradle wrapper 사용 — 명시 버전 일관성, (b) non-root user + .dockerignore secret 차단 — 보안 보강. Task 1 sub-bullet 5/5 완료. **로컬 docker build 검증 보류** — Docker daemon 가용 환경에서만 가능, Task 8(빌드 검증) 또는 운영 단계로 분리. 다음 세션 = Task 2(Frontend Dockerfile + nginx 정적 서빙) 진입. | James (Dev) |
| 2026-05-27 | 0.3 | 사용자 인프라 결정 보강 (PO Sarah 협의). **운영 서버 OS = Ubuntu 22.04 LTS** (Docker 공식 지원 안정, 2027년 LTS 지원) — Dockerfile 베이스 이미지 선택과 무관(컨테이너 OS 와 호스트 OS 분리), 운영 가이드 §1 "사전 준비"에 Ubuntu 22.04 기준 명령(`apt install docker.io docker-compose-plugin certbot`) 명시. **첨부 디렉토리 = `/data/smcs/files`** (별도 데이터 디스크 마운트) — `.env.template` `SMCS_FILES_DIR=/data/smcs/files` 기본값 변경, application.yml default `/var/smcs/files` 은 그대로 유지(환경변수 우선). 운영 가이드 §1에 별도 데이터 파티션 마운트 절차(`/etc/fstab` 추가 + `mkdir -p /data/smcs/files` + 권한) 명시. **DNS provider = 미정 (IT 팀 확인 필요)** — Dev 착수 시 운영자 협의 필요. 운영 가이드에 3종 모두 풍부 가이드 제공: (a) Cloudflare = `certbot-dns-cloudflare` + API token (Zone:DNS:Edit), (b) AWS Route 53 = `certbot-dns-route53` + IAM key, (c) 사내 BIND = `certbot-dns-rfc2136` + TSIG 키 + `nsupdate` 호환 서버. 도메인 이름도 동일 시점 확정(예: `smcs.<사내>.<tld>`). dev 가 Dockerfile/docker-compose/nginx config 작성은 즉시 가능(DNS 와 무관), 인증서 발급만 운영 단계로 분리. PO Sarah 비준: 사용자 결정 잔여 3건 중 2건 확정(OS + 첨부 디렉토리), 1건(DNS+도메인) 운영 단계 협의로 분리 — **dev 착수 차단 0**. Status: Approved 유지. | Sarah (PO) |
| 2026-05-27 | 0.2 | Validated & Approved by PO (Sarah) via *execute-checklist po-master-checklist (Brownfield + UI/UX 활성). 전체 준비도 ~92%(신규 의존성 0 + 마이그레이션 0 + 코드 변경 최소 — Brownfield 위험 LOW), 차단 0, CONCERNS 0. **Deviation 10건 모두 비준**: #1[비준] Redis 제외 MVP, #2[비준 B] nginx 분리(frontend nginx + 외부 nginx SSL), #3[비준 사용자결정] 백업 저장 위치 = **동일 서버 로컬 `/backup/`** + Disaster Recovery 한계 명시(v2 NAS/S3 강력 권장), #4[비준 사용자결정] HTTPS = **Let's Encrypt dns-01 challenge** (도메인+DNS provider Dev 착수 시 협의, certbot 자동 갱신), #5[비준] 호스트 cron MVP, #6[비준] actuator 무인증 + show-details=never + 나머지 denyAll, #7[기록] 수동 배포 MVP, #8[기록] 헬스체크 단일 endpoint, #9[기록 보강] docker logging max-size 100m max-file 5 명시, #10[기록] 첫 배포 안전 룰 3건. **사용자 결정 완료 3건**(2026-05-27): HTTPS=Let's Encrypt / 백업=동일서버 로컬 / Docker=가용. **사용자 결정 잔여 3건**(Dev 착수 시점 확정 가능, 차단 아님): 운영 서버 OS(Ubuntu 22.04 default), 도메인+IP(Let's Encrypt 발급 시 운영자 결정), 첨부 디렉토리(`/var/smcs/files` default). Brownfield 통합 위험 LOW(롤백=`docker compose down` 단일 명령, 코드 변경 최소). Should-fix 0. Status Draft→Approved. 즉시 Dev 핸드오프 가능. **Dev 착수 시 운영자와 협의 항목**: (a) 도메인 확정, (b) DNS provider + API 토큰, (c) 운영 서버 OS/사양, (d) 첨부 디렉토리 호스트 경로. 본 항목은 인프라 결정이라 dev 가 작업 중 사용자에게 질문하며 점진 확정 가능(SM 드래프트 시점 차단 불요). | Sarah (PO) |
| 2026-05-27 | 0.1 | Initial draft by SM (Bob) via create-next-story. Epic 4 Story 4.6(운영 배포 + 백업). Sourced: PRD §4.6 AC 1~6 + §1.1 Goals(70명 규모 안정 지원, 1개월 MVP 배포) + 실제 코드(backend/build.gradle.kts actuator 기존 / application.yml SMCS_* 환경변수 오버라이드 / docker-compose.local.yml dev 패턴 / LocalDataSeeder @Profile("local") 차단 기존 / smcs.files.dir 볼륨 영속 2.6 / reports cron 환경변수 3.4/3.5). 인프라/문서 스토리(코드 변경 최소). **신규 의존성 0**(actuator 기존)/마이그레이션 0/신규 endpoint 1(/actuator/health Spring 기본). **Deviation 10건**: #1[검토] Redis 제외 MVP, #2[검토] nginx 단일 vs 분리(B 권장), #3[검토] 백업 저장 위치=사용자 인프라 결정 NAS 권장, #4[검토] HTTPS 인증서 방식=사용자 인프라 결정 사내 CA 1차, #5[검토] cron 컨테이너 vs 호스트 cron=호스트 MVP, #6[검토] actuator 무인증+show-details=never, #7 자동화 배포 범위=수동 MVP, #8 헬스체크 단일 endpoint, #9 로그 rotation MVP=docker 기본, #10 첫 배포 안전 룰(cleanup-cron 비활성화 시작+admin1 변경+SPRING_PROFILES_ACTIVE=prod). **PO 사용자 결정 6건 사전 협의 필수**: 운영 서버 OS/사양, HTTPS 인증서 방식(#4), 백업 저장 위치(#3), 첨부 파일 디렉토리, 배포 권한, Docker 가용 여부. 의존성: 즉시 PO 비준 + 사용자 인프라 결정 후 Dev 가능. Status: Draft → PO 검토 필요. | Bob (SM) |

## QA Results

**Reviewer:** Quinn (Test Architect) · **Date:** 2026-05-27 · **Gate:** 🟢 **PASS** · **결함:** 0

### CI 증거 (선결조건 충족)
- **PR #26** `feat/story-4.6-deployment`, run **26492750707** (1차 통과·회귀 없음):
  - **Backend (Gradle + Java 21)** 🟢 pass 2m7s — `> Task :test` 그린(코드 변경 0, 회귀 무손상).
  - **Frontend (Vite + Vitest)** 🟢 pass 1m11s — `Run tests` + `Build production bundle` eager < 300KB gzip §9.7.
- 본 PR 은 인프라/문서 + Task 8 fix 2건(nginx.conf + compose.prod.yml) 중심 — 코드 변경 0, 전체 회귀 영향 0.

### 위험영역 정적 리뷰 (1차 증거)
| ID | 항목 | 검증 | 결과 |
| :- | :--- | :--- | :--: |
| R1 | Dockerfile non-root + secret 차단 | backend Dockerfile `groupadd/useradd smcs --uid 1000` + `USER smcs` + HEALTHCHECK actuator/health / `.dockerignore` `.env*`/`*.key`/`*.pem`/`*.crt` 차단 | ✅ |
| R2 | nginx TLS + 보안 헤더 | `ssl_protocols TLSv1.2 TLSv1.3` + `HIGH:!aNULL:!MD5` + HSTS(`max-age=31536000`) + X-Frame-Options SAMEORIGIN + X-Content-Type-Options nosniff + Referrer-Policy strict-origin-when-cross-origin + `server_tokens off` + `client_max_body_size 15m` | ✅ |
| R3 | compose secret 인터폴레이션 | `${POSTGRES_PASSWORD:?...}` fail-fast(.env 누락 시 즉시 실패) + SPRING_PROFILES_ACTIVE=prod hardcode(operator 사고 차단) + SPRING_DATASOURCE_URL/USERNAME hardcode | ✅ |
| R4 | SecurityConfig actuator | `/actuator/health` permitAll + `/actuator/**` denyAll 순서(기존 매처 무손상) — show-details=never 로 외부 응답에 DB/disk 상세 노출 0 | ✅ |
| R5 | Task 8 P2 fix 정합 | nginx.conf `:80`+`:443` `location = /api/actuator/health` proxy_pass strip(`/api` prefix 제거) + compose healthcheck `127.0.0.1` IPv4 명시(alpine localhost=IPv6 only — lesson #11) | ✅ |

### AC 추적성
- **AC1** (Docker compose 단일 명령) — `docker compose -f docker/docker-compose.prod.yml up -d` 4 서비스(backend+frontend+nginx+postgres) 기동, dev verification 시 4 컨테이너 healthy 확인 ✅
- **AC2** (HTTPS 가이드) — operations.md §1.5 Let's Encrypt dns-01 3 provider 풍부 가이드(Cloudflare/Route53/사내 BIND) + 인증서 nginx 연결 2 방식 + 갱신 cron + 사내 CA 폴백 ✅
- **AC3** (DB 일일 백업 + 30일) — `scripts/backup-db.sh` pg_dump --format=custom + `find -mtime +30 -delete` + cron 가이드 operations.md §4.1 ✅
- **AC4** (첨부 파일 백업 + 30일) — `scripts/backup-files.sh` rsync 증분/tar.gz 폴백 자동 분기 + 30일 정리 ✅
- **AC5** (헬스체크 통과) — backend HEALTHCHECK + docker compose healthcheck chain + nginx upstream + dev verification `curl -k https://localhost/api/actuator/health` → 200 `{"status":"UP"}` ✅
- **AC6** (운영 가이드 1페이지) — `docs/operations.md` 7 섹션(사전 준비/첫 배포/재기동/백업·복원/로그/점진 활성화/트러블슈팅) + DR 경고 박스 ✅

### Deviation 확인 (PO 비준 9건)
#1 Redis 제외 · #2 nginx B 옵션(외부+frontend 분리) · #3 백업 동일 서버 로컬 · #4 Let's Encrypt dns-01 · #5 호스트 cron · #6 actuator 무인증+show-details=never · #7 수동 배포 · #8 단일 헬스체크 · #9 docker 기본 json-file 로깅 — **전부 PO 비준대로 구현**.

### 잔여 비차단 (QA 수용)
- **admin1 회귀 (Task 8 sub-bullet 3/5)**: prod profile = LocalDataSeeder 비활성. operations.md §2.1 admin 부트스트랩 절차(Story 4.4 AC8 우회) + Story 4.7 user-test cycle 의 golden-path.md 6 단계 E2E 가 동일 시나리오 자연 흡수. **본 게이트 PASS 조건 아님**.
- **백업 스크립트 dry-run (Task 8 sub-bullet 4/5)**: prod 스택 down 상태 + local postgres 복귀 상태. operations.md §4.3 "백업 무결성 검증(월 1회 권장)" 으로 운영 단계 정착. **본 게이트 PASS 조건 아님**.

### 신규 lesson (#11)
- **Alpine 컨테이너 `localhost` = IPv6 `::1` only**: nginx IPv4 listener 와 충돌하여 self-probe healthcheck "Connection refused". 해결 = healthcheck 명령에서 **`127.0.0.1` 명시**. 4.6 외 다른 alpine 베이스 컨테이너 healthcheck 작성 시 일관 적용.

**판정:** CI 1차 그린(회귀 없음) + PO 비준 9건 일치 + 위험영역 5/5 정적 통과 + AC 6/6 충족 + dev 자가발견·자가fix P2 2건 + 잔여 비차단 2건 QA 수용 → **직접 PASS**. 결함 0. Status → **Done**. **머지 시 Epic 4 진척 7/7**(4.7 user-test cycle 만 잔여).
