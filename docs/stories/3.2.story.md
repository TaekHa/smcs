# Story 3.2: 대시보드 화면

## Status: Done

## Story

- As a **관리자**
- I want **한 화면에서 핵심 지표와 차트를 보기를**
- so that **1분 안에 현황을 파악할 수 있다**

## Acceptance Criteria (ACs)

1. 상단에 KPI 카드 4개: [선택 기간(오늘/이번주/이번달, AC5)] 신규 / [선택 기간] 처리 / 미처리 총건(현재 시점, period 무관) / [선택 기간] 평균 처리시간. (PO 정제 2026-05-23 — AC5 기간 토글 반영 명문화; openCount는 §5.9 정의에 따라 현재 스냅샷.)
2. 카테고리별 막대 차트.
3. 일자별 신규 vs 처리 추세 라인 차트.
4. 담당자별 처리량 막대 차트.
5. 기간 필터(오늘/이번주/이번달) 토글.
6. 모든 차트는 데이터 0건일 때 빈 상태 메시지 표시.

## Tasks / Subtasks

- [x] **Task 1: 의존성 + 타입 (AC: 2,3,4)** (AC: 2, 3, 4)
  - [x] `frontend/package.json` 에 `@ant-design/charts ^2.2.x` 추가(**현재 미설치** — PRD §4.3 확정). [Source: prd/4-technical-assumptions.md#4.3(차트=Ant Design Charts 2.2.x); 실제 package.json(charts 부재 확인)]
  - [x] `frontend/src/types/stats.ts`: `DashboardStats`(§5.9 미러) — `kpi{newCount,resolvedCount,openCount,avgResolveMinutes}`, `byCategory{name,count}[]`, `byAssignee{name,resolved}[]`, `byPriority{priority,count}[]`, `trend{date,newCount,resolvedCount}[]`. `priority`는 **기존 `types/issue.ts`의 `Priority` 재사용**(중복 정의 금지, §9.1 타입 SSOT). [Source: prd/5-data-models.md#5.9(DashboardStats); frontend/src/types/issue.ts(Priority); prd/9-coding-standards.md#9.1(타입 동기화)]

- [x] **Task 2: API + 훅 (AC: 1~5)** (AC: 1, 2, 3, 4, 5)
  - [x] `frontend/src/api/stats.ts`: `getDashboardStats(period: StatsPeriod): Promise<DashboardStats>` = `apiClient.get('/stats/dashboard', { params: { period } })`. **직접 fetch 금지 — apiClient 경유**(§9.1, JWT 자동첨부). `StatsPeriod='today'|'week'|'month'`. [Source: prd/6-rest-api-spec-요약.md(GET /api/stats/dashboard); frontend/src/api/notifications.ts(apiClient 패턴); prd/9-coding-standards.md#9.1(apiClient)]
  - [x] `frontend/src/shared/hooks/useDashboardStats.ts`: `useQuery({ queryKey:['stats','dashboard',period], queryFn:()=>getDashboardStats(period) })`. period 변경 시 자동 리페치(queryKey 의존). [Source: frontend/src/shared/hooks/useNotifications.ts(useQuery 패턴); prd/4-technical-assumptions.md#4.3(TanStack Query)]

- [x] **Task 3: DashboardView — KPI + 기간 토글 (AC: 1, 5)** (AC: 1, 5)
  - [x] `frontend/src/features/dashboard/DashboardView.tsx`(named export). 기간 토글: antd `Segmented`(또는 `Radio.Group`) `오늘|이번주|이번달`→`today|week|month`, 로컬 state로 `useDashboardStats(period)` 구동. 기본값 `today`. [Source: epic-3 Story 3.2 AC5; frontend/src/features/*(View 컨벤션)]
  - [x] KPI 카드 4개(antd `Card`+`Statistic`): 신규=`kpi.newCount`, 처리=`kpi.resolvedCount`, 미처리 총건=`kpi.openCount`, 평균 처리시간=`kpi.avgResolveMinutes`(분→읽기 좋은 형식 헬퍼, 예: "1시간 23분"/"45분"). **라벨은 기간 토글 반영**(오늘/이번주/이번달 신규·처리). **openCount는 현재 시점(period 무관)** — 카드 부제/툴팁으로 명확화. [Source: epic-3 Story 3.2 AC1; prd/5-data-models.md#5.9(openCount=현재, avg 단위=분)]

- [x] **Task 4: 차트 3종 + 빈 상태 (AC: 2, 3, 4, 6)** (AC: 2, 3, 4, 6)
  - [x] 카테고리별 막대(`byCategory`): Ant Charts `Column`/`Bar`(x=name, y=count). 담당자별 처리량 막대(`byAssignee`): x=name, y=resolved. 일자별 추세 라인(`trend`): 신규 vs 처리 2계열(x=date, y=newCount/resolvedCount). [Source: epic-3 Story 3.2 AC2/AC3/AC4; prd/5-data-models.md#5.9]
  - [x] **빈 상태(AC6)**: 각 차트는 데이터 0건일 때 antd `Empty`(빈 메시지). 차트 컴포넌트에 빈 배열 그대로 넘기지 말고 길이 0 가드. [Source: epic-3 Story 3.2 AC6]
  - [x] **a11y(§3.4 WCAG A)**: 색상에만 의존 금지 — 범례 텍스트 + KPI 라벨 텍스트 병기. [Source: prd/3-user-interface-design-goals.md#3.4]

- [x] **Task 5: 라우팅 + 내비 (AC: 1)** (AC: 1)
  - [x] `frontend/src/routes.tsx`: `/dashboard` 추가(기존 자리표시 주석 위치). **lazy 로드**(named→default 리맵, 기존 패턴) + `RequireAuth` + `RequireRole roles={['ADMIN']}` + `AppLayout`. **화면 가드=ADMIN**(§5.9; API 자체는 인증이나 화면은 ADMIN 한정). [Source: frontend/src/routes.tsx(lazy+RequireRole 패턴/`/dashboard` 자리표시 주석); prd/5-data-models.md#5.9(대시보드 ADMIN 화면 가드)]
  - [x] `frontend/src/layout/AppLayout.tsx`: nav 에 ADMIN 전용 `<Link to="/dashboard">대시보드</Link>` 추가(기존 role 조건부 링크 패턴). [Source: frontend/src/layout/AppLayout.tsx(role 조건부 nav)]

- [x] **Task 6: 번들 가드 + 테스트 (AC: 6)** (AC: 6)
  - [x] **번들**: Ant Charts 무거움 → `DashboardView`(lazy 라우트) 내부에서만 import 하여 **dashboard 청크에 격리**. eager 번들 누수 금지(epic3_architecture 리스크#1, eager ~260KB gzip / 300KB 가드레일 근접). `npm run build` 청크 분리 확인. [Source: epic3_architecture(번들 가드레일); frontend/src/routes.tsx(lazy 청크 선례)]
  - [x] **컴포넌트 테스트(RTL, 스모크)**: `DashboardView.test.tsx` — apiClient/훅 **mock**(고정 DashboardStats)으로 KPI 카드 4개 렌더, 기간 토글 클릭 시 리페치(queryKey 변경), 데이터 0건 시 빈 상태 표시. **Ant Charts 는 mock**(jsdom canvas 미지원) — 차트 존재/props 스모크만. [Source: prd/4-technical-assumptions.md#4.4(핵심 컴포넌트 RTL 스모크); frontend/src/features/*/*.test.tsx(테스트 선례)]

- [x] **Task 7: 빌드 검증**
  - [x] `npm run build`(tsc -b + vite build) ✅, `npm test` 그린. 백엔드 변경 없음(3.2=프론트 전용). [Source: frontend/package.json(scripts)]

## Dev Notes

### Previous Story Insights / 교차 스토리 의존성 (필독)

[Source: docs/stories/3.1.story.md; prd/5-data-models.md#5.9; PO Epic 3 시퀀스 점검 2026-05-22]

- 🟡 **3.2는 3.1의 HTTP 계약에만 의존**(코드 공유 아님). `GET /api/stats/dashboard?period=` + `DashboardStats`(§5.9). **컴포넌트 개발/테스트는 apiClient mock 으로 3.1과 병렬 가능**; 수동/통합 확인만 3.1 배포 후. (3.3과 달리 코드 의존 없음 → 차단 약함.)
- **재사용 패턴**: `api/*.ts`(apiClient.get), `shared/hooks/use*.ts`(useQuery), `features/<name>/<Name>View.tsx`, `routes.tsx`(lazy+RequireRole), `AppLayout`(role 조건부 nav), `types/issue.ts`(`Priority`).
- **번들 주의**(epic3_architecture 리스크#1): Ant Charts 가 eager 로 새면 가드레일 초과. dashboard 청크 격리 필수.

### Data Models / 권한 / API

[Source: prd/5-data-models.md#5.9; prd/6-rest-api-spec-요약.md; prd/3-user-interface-design-goals.md]

- `GET /api/stats/dashboard?period=today|week|month` → `DashboardStats`(§5.9). API=인증, **화면=ADMIN 라우트 가드**(§5.9).
- KPI: newCount/resolvedCount/openCount/avgResolveMinutes(분). openCount=현재(period 무관). 추세=KST 일자별 신규 vs 처리.
- 빈 상태(AC6)=antd `Empty`. a11y=색상 비의존(§3.4 WCAG A).
- **백엔드 변경 없음** — 3.2=프론트 전용 소비 화면.

### Deviation Analysis (PO 검토 필요)

1. **[기록] KPI 카드 라벨 = 기간 반영**: AC1 문구는 "오늘 신규/오늘 처리"지만 AC5 기간 토글이 있어 선택 기간(오늘/이번주/이번달)에 따라 데이터·라벨 변동. openCount 만 현재 시점(period 무관) — 부제로 명확화.
2. **[기록] 화면 ADMIN 가드**: stats API 는 §6 인증이나, 대시보드 화면은 ADMIN 한정(§5.9 PO 비준). routes `RequireRole(['ADMIN'])`.
3. **[기록] Ant Charts lazy 격리**: 번들 가드레일 위해 dashboard 청크에서만 로드. 차트는 테스트에서 mock(jsdom canvas 미지원).
4. **[기록] 3.1 병렬 가능**: 계약 의존만 → mock 으로 선개발. 실데이터 확인은 3.1 배포 후.

### Project Structure Notes

- 신규 frontend: `features/dashboard/DashboardView.tsx`(+test), `api/stats.ts`, `shared/hooks/useDashboardStats.ts`, `types/stats.ts`.
- 변경: `routes.tsx`(/dashboard 라우트 — 자리표시 주석 대체), `AppLayout.tsx`(ADMIN nav 링크), `package.json`(@ant-design/charts).
- 백엔드/마이그레이션 변경 없음.

### Testing

- [ ] **RTL 스모크**: KPI 4카드 렌더, 기간 토글 리페치, 빈 상태 표시. 훅/apiClient mock, Ant Charts mock.
- [ ] **번들 검증**: `npm run build` → dashboard 청크에 charts 격리(eager 누수 0).
- [ ] E2E: 범위 밖(수동 — admin1 로그인 → /dashboard 차트/KPI 확인).

Manual Test Steps: `admin1` 로그인 → nav "대시보드" → KPI 4개·차트 3종 표시, 기간 토글 동작, 데이터 0건 빈 상태. `agent1`/`field1` `/dashboard` 접근 시 403(RequireRole). `npm run build`/`npm test`.

## Dev Agent Record

### Agent Model Used: claude-opus-4-7 (James / Dev)

### Debug Log References

| Task | File | Change | Reverted? |
| :--- | :--- | :----- | :-------- |

(없음 — 1회 그린)

### Completion Notes List

- **Epic 3 종결**: 본 스토리 머지 시 Epic 3 (3.1~3.5 + TD-2) 전체 Done.
- **번들 가드레일 통과**: eager `index` gzip 194.91 KB (<300 KB §9.7), Ant Charts(2.2.x, ~441 KB gzip)는 **`DashboardView` lazy 청크에 완전 격리** — epic3_architecture 리스크 #1 통과. ReportsView/MobileFieldHome 등 다른 라우트 영향 0.
- **차트 mock**: jsdom canvas 미지원 → `vi.mock('@ant-design/charts', ...)` 으로 `Column`/`Line` stub(`data-testid` + `data-rows`). 실제 렌더는 운영에서만.
- **PO 정제 AC1 반영**: KPI 라벨 동적(`{periodWord} 신규/처리/평균 처리시간`), `openCount` 만 "현재 시점 기준" 부제 — 테스트로 검증.
- **a11y**: Segmented `aria-label="기간 필터"`, Statistic `suffix="건"` 단위 텍스트, Line legend 텍스트 표시(색상 비의존).
- 백엔드 변경 0, 마이그레이션 0 — 3.1 StatsService 머지 실데이터 그대로 소비.

### File List

**신규(frontend)**: `types/stats.ts`, `api/stats.ts`, `shared/hooks/useDashboardStats.ts`, `features/dashboard/DashboardView.tsx`(+test 6 통과)
**수정(frontend)**: `routes.tsx`(/dashboard lazy + RequireRole ADMIN), `layout/AppLayout.tsx`(ADMIN nav "대시보드" 링크), `package.json`+`package-lock.json`(@ant-design/charts ^2.2.6 신규)
**백엔드/마이그레이션**: 무변경

### Change Log

| Date       | Version | Description | Author |
| :--------- | :------ | :---------- | :----- |
| 2026-05-23 | 1.3 | QA (Quinn) 게이트 PASS · Status Review→Done. PR #19 CI green(run 26331405910, Backend 2m5s + Frontend 1m6s). 코드 추적 R1~R6 정적 + CI 산출물 확인(작성자 주장 비의존). 결함 0, 잔여 관찰 1건(비차단). **Epic 3 종결**: 3.1·3.2·3.3·3.4·3.5 + TD-2 전체 Done. 자세한 결과는 QA Results 섹션. | Quinn (QA) |
| 2026-05-23 | 1.2 | Implemented by Dev (James). 프론트: @ant-design/charts ^2.2.6 신규 설치(115 packages) + types/stats(DashboardStats §5.9 미러, Priority 재사용) + api/stats(apiClient.get) + useDashboardStats(TanStack Query, queryKey=[stats,dashboard,period]) + DashboardView(named export, Segmented 오늘/이번주/이번달, Card+Statistic 4개 — periodWord 동적 라벨, openCount "현재 시점 기준" 부제, formatMinutes 헬퍼, 카테고리/담당자 Column + 추세 Line 2계열, 빈 상태 antd Empty 가드, Spin 로딩, Error placeholder). /dashboard 라우트 lazy + RequireRole ADMIN + AppLayout ADMIN nav. RTL 6/6 통과(KPI 4카드 today 라벨/주간 클릭 리페치 + week 라벨/차트 3종 렌더/빈 상태 메시지/로딩 spinner/에러 placeholder). 프론트 전체 108/108 통과(기존 102 + DashboardView 6). npm run build 그린 — eager index gzip 194.91KB<300KB §9.7, DashboardView lazy gzip 441KB(Ant Charts 완전 격리 — eager 누수 0). 백엔드 무변경. Status Approved→Review. | James (Dev) |
| 2026-05-23 | 1.1 | PO (Sarah) AC1 정제: 기간 토글(AC5) 반영 라벨로 본문 명문화 — Deviation#1을 AC 본문에 반영하여 Dev 혼동 방지. openCount는 현재 스냅샷 명시. ACs/Tasks 본질 무변경(문구 명확화). Status Approved 유지. | Sarah (PO) |
| 2026-05-22 | 1.0 | Validated & Approved by PO (Sarah). 정합성 확인: AC↔epic-3 일치, DashboardStats §5.9 미러(+Priority 재사용 §9.1), 화면 ADMIN 가드(§5.9), apiClient 규약(§9.1), Ant Charts lazy 청크 격리(번들 가드레일). 3.1 의존=HTTP 계약만(mock 병렬 가능, 코드 비의존). KPI 라벨 기간 반영(Deviation#1) 승인 — openCount는 현재 스냅샷 부제 표기. Status Draft→Approved. | Sarah (PO) |
| 2026-05-22 | 0.1 | Initial draft by SM (Bob) via create-next-story. Epic 3 Story 3.2(대시보드 화면, 첫 프론트 Epic3 스토리). Sourced: prd/epic-3 Story 3.2(AC1-6) + §5.9(DashboardStats/period/openCount=현재/avg=분/화면 ADMIN 가드) + §6(stats=인증) + §4.3(Ant Charts 2.2.x·TanStack Query)/§4.4(RTL 스모크) + §3(UI 목표·WCAG A·대시보드=차트 화면) + §9.1(타입 SSOT·apiClient) + 3.1(HTTP 계약 의존·병렬 가능) carry-over + 실제 코드(routes.tsx /dashboard 자리표시+lazy+RequireRole, AppLayout role nav, api/hook 패턴, types/issue Priority, package.json charts 부재) + epic3_architecture(번들 가드레일). 프론트 전용. Deviation 4건(KPI 라벨 기간반영/ADMIN 화면가드/Charts lazy 격리/3.1 병렬). | Bob (SM) |

## QA Results

**검수자: Quinn (QA Test Architect) · 2026-05-23 · 게이트: 🟢 PASS · Status Done (PR #19 CI green run 26331405910)**

**CI 확정**: Backend(Gradle+Java21) pass 2m5s — 회귀 통합(Stats/Report/Notification/Issue/Attachment/ReportArchiveAccess) Testcontainers Postgres 그린. Frontend pass 1m6s — `npm test` 108/108(기존 102 + DashboardView 6) + `npm run build` 그린.

코드를 직접 추적 검증(작성자 주장 비의존). 위험영역 R1~R6 (Epic 3 마지막 — 프론트 전용):

| 위험영역 | 검수 방법 | 결과 |
| :--- | :--- | :--- |
| R1 AC1 PO 정제 (기간 토글 라벨 반영 + openCount 현재 스냅샷) | `DashboardView.KpiCards` 정적 — `${periodWord} 신규/처리/평균 처리시간` + `미처리 총건` + "현재 시점 기준" 부제(periodWord 무관). PERIOD_LABEL 매핑 today/week/month → 오늘/이번주/이번달. RTL `renders the four KPI cards with the active period label`(default today → "오늘 신규" + "1시간 30분" formatMinutes) + `changing the period triggers a refetch with the new key`(주간 클릭 → useDashboardStats('week') + "이번주 신규") | 🟢 PASS · CI 확인 |
| R2 차트 3종 + 빈 상태 (AC2/3/4/6) | `CategoryChart`/`AssigneeChart`/`TrendChart` 각각 `data.length === 0` 가드 → `<Empty description>`. TrendChart는 2계열 series flatMap(date, type="신규"/"처리", count). RTL `renders all three charts when data is present`(Column 2개 + Line 1개, line series rows = trend.length × 2) + `shows an empty-state message per chart when data is 0`(3종 Empty 메시지 + queryByTestId 차트 null) | 🟢 PASS · CI 확인 |
| R3 번들 가드레일 (Ant Charts lazy 격리) | `routes.tsx` `DashboardView = lazy(() => import('./features/dashboard/DashboardView'))` lazy + `RequireRole ADMIN`. `DashboardView.tsx` 내부에서만 `@ant-design/charts` import(다른 라우트 import 0 — Grep 검증). npm run build 산출물: **eager `index` gzip 194.91 KB**(<300 KB §9.7 가드레일), **DashboardView lazy gzip 441 KB**(Ant Charts G2 포함, 완전 격리). epic3_architecture 리스크 #1(Charts eager 누수) 통과 | 🟢 PASS · CI 확인 |
| R4 ADMIN 권한 화면 가드 (§5.9 PO 비준) | `routes.tsx` `/dashboard` `<RequireAuth><RequireRole roles={['ADMIN']}><AppLayout>...` 명시. `AppLayout.tsx` nav `user.role === 'ADMIN' && <Link to="/dashboard">대시보드</Link>`(보관함과 동일 패턴). API(`/api/stats/dashboard`)는 §6 인증(전 역할) — 화면 가드와 별개 PO 비준 일치 | 🟢 정적 PASS |
| R5 apiClient 규약 (§9.1) | `api/stats.ts` `apiClient.get<DashboardStats>('/stats/dashboard', { params: { period } })` — JWT 자동 첨부, 직접 fetch/axios 호출 0. useDashboardStats(TanStack Query queryKey=['stats','dashboard',period]) → period 변경 시 자동 리페치(RTL 검증) | 🟢 정적 PASS |
| R6 회귀 (프론트 + 백엔드) | CI BUILD SUCCESSFUL = 프론트 108/108(기존 102 + 신규 6, 영향 0) + 백엔드 통합 6종 그린 유지. 신규 의존성 `@ant-design/charts ^2.2.6`(+115 packages) 다른 라우트 영향 0(lazy 격리). 백엔드/마이그레이션/엔드포인트 무변경 | 🟢 PASS · CI 확인 |

**잔여 관찰(비차단):**
1. **DashboardView lazy 청크 크기 441 KB gzip** — Ant Charts G2 라이브러리 자체 무게. 첫 `/dashboard` 진입 시 한 번 다운로드 + 브라우저 캐싱 → 재진입 무영향. eager 가드레일은 안전(eager 누수 0). MVP OK. 운영에서 dashboard 진입 빈도가 높지 않다면(ADMIN만, 일일 1~2회) 실 사용자 부담 적음. v2 에서 (a) Ant Charts → 더 가벼운 chart 라이브러리(예: Recharts) 검토, 또는 (b) chart 컴포넌트별 sub-lazy 분할 검토 가능.

**결함 0.** AC 1~6 충족 — AC1(KPI 4카드 동적 periodWord 라벨 + openCount 현재 스냅샷, RTL 검증), AC2(byCategory Column), AC3(trend Line 2계열 신규 vs 처리), AC4(byAssignee Column), AC5(Segmented 오늘/이번주/이번달 + queryKey 자동 리페치, RTL 검증), AC6(차트별 Empty 가드 + RTL 검증). PO 정제 v1.1(AC1 기간 토글 라벨 명문화) 그대로 반영.

**Epic 3 종결**: 본 머지로 **Story 3.1·3.2·3.3·3.4·3.5 + TD-2 전체 Done** — 자동 보고서(매일 08:00 KST 적재) + 보관함(미리보기/다운로드, 90일 cleanup) + 대시보드(실시간 KPI/차트, 기간 토글) 풀스택 완성. PRD §1 핵심 차별화 달성.

**게이트 확정:** PR #19 CI green(run 26331405910) + 코드 추적 검수 완료 → 게이트 PASS, Status Review→Done. 머지 준비 완료.
