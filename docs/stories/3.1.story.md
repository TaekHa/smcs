# Story 3.1: 통계 집계 백엔드

## Status: Done

## Story

- As a **백엔드(시스템)**
- I want **이슈 통계를 효율적으로 집계할 수 있는 API를 제공하기를**
- so that **대시보드(3.2)와 보고서(3.3)가 동일한 데이터를 사용한다**

## Acceptance Criteria (ACs)

1. `GET /api/stats/dashboard?period=today|week|month` 이 신규/처리/미처리/평균처리시간을 반환한다.
2. 카테고리별 건수, 담당자별 처리량, 우선순위별 분포를 제공한다.
3. 시간대별(일자 또는 주차 단위) 추세 데이터를 제공한다.
4. 응답 시간 P95 < 500ms.
5. 집계 로직에 단위 테스트가 작성되어 있다.

## Tasks / Subtasks

- [x] **Task 1: V5 마이그레이션 — `resolved_at` 인덱스 (첫 신규 Flyway, AC4)** (AC: 4)
  - [x] `V5__add_issue_resolved_at_index.sql`: `CREATE INDEX idx_issues_resolved_at ON issues (resolved_at);`. **나머지 집계 인덱스는 V2 기존**(`created_at`·`status`·`(assigned_to,status)`·`category_l1_id`·`(priority,created_at)`) → 추가 불필요. `resolved_at` 만 미존재(처리건수/평균처리시간 집계 적중). [Source: backend/.../db/migration/V2__create_tables.sql L48-55(기존 인덱스); prd/5-data-models.md#5.9(V5 인덱스 근거); Deviation #1]
  - [x] **Epic 2 "스키마 무변경(validate)" 종료** — Epic 3 첫 마이그레이션. reports 테이블(3.4 V6)·notifications 변경(3.4 V7)은 각 스토리 별도 마이그레이션. ddl-auto=validate 유지(엔티티 무변경, 인덱스만 추가). [Source: prd/5-data-models.md#5.8/#5.9(V5 도입); Deviation #1]

- [x] **Task 2: stats 도메인 + period 범위 해석** (AC: 1, 3)
  - [x] `com.smcs.stats` 신규(flat 도메인 + `dto/`): `dto/DashboardStats` (record) — `Kpi kpi, List<CategoryCount> byCategory, List<AssigneeCount> byAssignee, List<PriorityCount> byPriority, List<TrendPoint> trend`. nested records: `Kpi{newCount, resolvedCount, openCount, avgResolveMinutes}`, `CategoryCount{name, count}`, `AssigneeCount{name, resolved}`, `PriorityCount{priority, count}`, `TrendPoint{date, newCount, resolvedCount}`. [Source: prd/5-data-models.md#5.9(DashboardStats 형태)]
  - [x] `StatsPeriod`(enum/헬퍼): `today|week|month` → KST `[from, to)` Instant 범위. **today**=오늘(KST), **week**=이번 ISO 주(월~일, KST), **month**=이번 캘린더 월(KST). 2.2 `IssueQueryService.startOfDay`(KST `atStartOfDay`) 패턴 재사용. **순수 로직 — 단위 테스트 대상(AC5)**. [Source: backend/.../issue/IssueQueryService.java#startOfDay/KST; prd/5-data-models.md#5.9(KST 경계); prd/4-technical-assumptions.md(보고서 KST 일자 경계)]

- [x] **Task 3: `StatsService` 집계** (AC: 1, 2, 3)
  - [x] `StatsService.dashboard(StatsPeriod period)` `@Transactional(readOnly=true)` → `DashboardStats`. **집계는 `JdbcTemplate` native SQL**(KST `created_at AT TIME ZONE 'Asia/Seoul'` 그룹 — JPQL 표현 불가, §5.9; LocalDataSeeder JdbcTemplate 선례). [Source: prd/5-data-models.md#5.9(추세 native SQL); backend/.../common/seed/LocalDataSeeder.java(JdbcTemplate 선례)]
  - [x] **범위(range) 집계 노출 (3.3 재사용 — PO 추가 2026-05-22)**: 핵심 집계는 `[from, to)` Instant 범위를 받는 메서드(예: `DashboardStats aggregate(Instant from, Instant to)`)로 구현하고, `dashboard(StatsPeriod period)`는 `StatsPeriod`→범위 변환 후 이 메서드에 **위임**한다. 보고서(3.3)는 임의 과거 날짜/주차 범위로 동일 메서드를 호출 → 집계 SQL/로직 단일 출처 유지(중복 금지). [Source: docs/stories/3.3.story.md Task3(StatsService range 의존); prd/5-data-models.md#5.9(KST [from,to) 범위 집계)]
  - [x] 지표 정의(§5.9, PO 비준): `kpi.newCount`=period 내 `created_at`; `kpi.resolvedCount`=period 내 `resolved_at`; `kpi.openCount`=현재 `status NOT IN (DONE,VERIFIED)`(**period 무관**); `kpi.avgResolveMinutes`=period 내 해결분 `avg(resolved_at−created_at)`(**최종 완료 기준** — 2.7 재오픈 시 resolved_at 클리어/재완료 시 갱신, PO 비준). [Source: prd/5-data-models.md#5.9(resolved_at=최종완료); docs/stories/2.7.story.md(transitionTo resolvedAt)]
  - [x] 분포(AC2): `byCategory`=period 내 created 그룹 `category_l1_id`→이름(`CategoryRepository.findAll` 맵, 2.2 N+1 회피); `byPriority`=period 내 created 그룹 `priority`; `byAssignee`=period 내 resolved 그룹 `assigned_to`→`User.displayName`(`UserRepository.findAllById`). 추세(AC3): period 내 KST 일자별 `newCount` vs `resolvedCount`. [Source: backend/.../issue/IssueQueryService.java(카테고리/사용자 이름 해석); prd/5-data-models.md#5.9]

- [x] **Task 4: `StatsController` — `GET /api/stats/dashboard`** (AC: 1)
  - [x] `StatsController`(`@RequestMapping("/api")`): `@GetMapping("/stats/dashboard")`, **`@PreAuthorize("isAuthenticated()")`**(§6 stats=인증; 대시보드 화면 ADMIN 가드는 3.2 — PO 비준). `@RequestParam(defaultValue="today") String period` → `StatsPeriod` 파싱(잘못된 값 400 `VALIDATION_FAILED`). 200 + `DashboardStats`. [Source: prd/6-rest-api-spec-요약.md(GET /api/stats/dashboard=인증); backend/.../issue/IssueController.java(컨트롤러 패턴); prd/5-data-models.md#5.9(권한)]

- [x] **Task 5: 테스트** (AC: 4, 5)
  - [x] **단위(AC5)**: `StatsPeriod` 범위 해석(today/week/month → KST [from,to)) 순수 단위 테스트(JUnit5, DB 불요) + avg 계산 등 계산 헬퍼. [Source: prd/4-technical-assumptions.md(집계 함수 단위테스트 필수)]
  - [x] **통합(Testcontainers, CI)**: 시드 이슈(다양한 created_at/resolved_at/status/category/assignee/priority) → `GET /stats/dashboard?period=...` 집계 정확성 — kpi 4종/byCategory/byAssignee/byPriority/trend 값 검증, 미인증 401, 잘못된 period 400. 회귀: 기존 통합 그린. **로컬 Windows 미실행 → CI**(2.x 선례). seed FK 정리(lesson #7). [Source: docs/stories/2.x(통합 패턴·CI); docs/stories/2.8.story.md(lesson #7)]
  - [x] **AC4 P95<500ms**: 인덱스 기반 설계로 보장(idx 적중 — created_at/resolved_at/status/category_l1/assigned_to). 테스트에서 엄밀 측정 안 함(MVP 데이터 소량) — 인덱스 근거를 Dev Notes 에 기록. [Source: prd/5-data-models.md#5.9(P95 인덱스 근거); Deviation #5]

- [x] **Task 6: 빌드 검증**
  - [x] `./gradlew build -x test` ✅ assemble + 단위(통합 CI). Flyway V5 적용 후 부팅(validate) 확인은 CI. 프론트 변경 없음(3.1=백엔드 전용). [Source: docs/stories/2.x#빌드]

## Dev Notes

### Previous Story Insights / Architect·PO 선결 (Epic 2 + Epic 3 점검)

[Source: prd/5-data-models.md#5.9; docs/stories/2.2.story.md, 2.4/2.7]

- **§5.9 DashboardStats 설계 = Architect 작성·PO 비준(2026-05-22)** — 본 스토리의 1차 소스. 대시보드(3.2)·PDF(3.3)가 동일 `StatsService` 공유(중복 집계 금지).
- **resolved_at = 통계 핵심 축, PO 비준 "최종 완료 기준"**: 2.4(→DONE set)/2.7(재오픈 clear, 재완료 갱신)이 관리. avgResolveMinutes = 접수→최종완료(§13 KPI 부합).
- **재사용**: 2.2 `IssueQueryService`(KST `startOfDay`, 카테고리/사용자 이름 N+1 회피 맵), `LocalDataSeeder`(JdbcTemplate 선례), `IssueRepository`/`CategoryRepository`/`UserRepository`, `GlobalExceptionHandler`(VALIDATION_FAILED).
- **V2 인덱스 대부분 존재** — stats용 누락은 `resolved_at` 하나(V5).
- **CI 게이트**(Windows Testcontainers 미실행), readOnly 집계, 단일 출처(StatsService).

### Data Models / 권한 / API

[Source: prd/5-data-models.md#5.9; prd/6-rest-api-spec-요약.md; architecture/6-...#6.3]

- `GET /api/stats/dashboard?period=today|week|month` (인증; 화면 ADMIN 가드=3.2). 응답 `DashboardStats`(§5.9).
- KST 경계, UTC 저장. 추세=KST 일자별. avg 단위=분(minutes).
- 신규 엔티티 없음(stats=계산 모델). 신규 테이블 없음(인덱스만 V5).

### Deviation Analysis (PO 검토 필요)

1. **[기록] V5 = resolved_at 인덱스만**: 나머지 집계 인덱스 V2 기존. 첫 신규 Flyway(스키마 무변경 종료, PO 비준). reports/notifications 변경은 3.3/3.4 별도 마이그레이션(V6/V7).
2. **[기록] 집계 = JdbcTemplate native SQL**: KST date 그룹(`AT TIME ZONE`)은 JPQL 불가(Architect/§5.9). LocalDataSeeder 선례. 계산/범위 로직은 분리해 순수 단위테스트.
3. **[기록] period 스코프 정의**: openCount=period 무관(현재 open); newCount/byCategory/byPriority=period 내 created; resolvedCount/byAssignee/avg=period 내 resolved; trend=일자별 new vs resolved. (3.2 대시보드가 이 의미로 렌더.)
4. **[기록] 권한 = §6 isAuthenticated**: stats API 는 인증(전 역할 호출 가능). 대시보드 화면 ADMIN 제한은 3.2 라우트 가드. §6 PO 비준.
5. **[기록] AC4 P95<500ms = 인덱스 설계 보장**: 엄밀 부하측정은 MVP 범위 밖. 인덱스 적중 근거를 코드/Dev Notes 로. week/month/today 정의: today=오늘 KST, week=이번 ISO 주(월~일) KST, month=이번 캘린더 월 KST.

### Project Structure Notes

- 신규 backend: `com.smcs.stats`(`StatsController`, `StatsService`, `StatsPeriod`, `dto/DashboardStats`+nested records). 신규 마이그레이션 `V5__add_issue_resolved_at_index.sql`.
- 변경 없음: 엔티티(인덱스만), 프론트(3.1=백엔드 전용 — 화면은 3.2).
- flat 도메인 + `dto/` 컨벤션(Epic 2 동일).

### Testing

- [ ] **JUnit 단위**: StatsPeriod 범위 해석(today/week/month KST), 계산 헬퍼. DB 불요(AC5).
- [ ] **JUnit 통합(Testcontainers, CI)**: 시드 데이터 집계 정확성(kpi/분포/추세)·미인증 401·잘못된 period 400. 회귀 그린. seed FK 정리.
- [ ] E2E: 범위 밖.

Manual Test Steps: `agent1`/`admin1` `GET /api/stats/dashboard?period=today` JSON 확인 / `./gradlew build`(통합 CI) / Flyway V5 부팅.

## Dev Agent Record

### Agent Model Used: claude-opus-4-7 (James / Dev)

### Debug Log References

| Task | File | Change | Reverted? |
| :--- | :--- | :----- | :-------- |
| 3 | StatsService.java | `jdbc.query(...)` 표현식 람다가 ResultSetExtractor/RowCallbackHandler 간 모호 → 블록(void) 람다로 수정 | No (영구 수정) |

### Completion Notes List

- 집계 SQL 바인딩은 `OffsetDateTime(UTC)`로 통일(timestamptz 모호성 회피) — 시드는 `Timestamp.from`이나 정확 비교 위해 쿼리/테스트는 OffsetDateTime.
- `aggregate(from,to)` 공개 → Story 3.3 가 임의 날짜/주차로 재사용(단일 출처).
- 통합 테스트 로컬 미실행(Docker 미가용, Windows) → CI 게이트. 단위/컴파일/assemble 그린.
- QA(Quinn) 보강 요청 반영: `weekTrendSpansMultipleKstDays` 추가 — 다일(月~일 7포인트) 추세 zero-fill·일자별 new/resolved 검증(통합, CI).

### File List

**신규(main)**: `db/migration/V5__add_issue_resolved_at_index.sql`, `stats/dto/DashboardStats.java`, `stats/StatsRange.java`, `stats/StatsPeriod.java`, `stats/StatsPeriodConverter.java`, `stats/StatsService.java`, `stats/StatsController.java`
**신규(test)**: `stats/StatsPeriodTest.java`(단위, 통과), `stats/StatsControllerIntegrationTest.java`(통합, CI)
**수정**: 없음(엔티티/기존 코드 무변경 — 인덱스만 추가)

### Change Log

| Date       | Version | Description | Author |
| :--------- | :------ | :---------- | :----- |
| 2026-05-22 | 0.1 | Initial draft by SM (Bob) via create-next-story. Epic 3 Story 3.1(첫 스토리). Sourced: prd/epic-3 Story 3.1(AC1-5) + prd §5.9(DashboardStats/StatsService — Architect 작성·PO 비준)/§5.8(V5 도입)/§13(KPI 접수→완료) + REST §6(stats=인증) + 4-technical-assumptions(집계 단위테스트·KST 경계) + 2.2(IssueQueryService KST/이름해석)·2.4/2.7(resolved_at)·LocalDataSeeder(JdbcTemplate) carry-over + 실제 코드(V2 인덱스 — resolved_at만 누락, stats 패키지 없음). 백엔드 전용. Deviation 5건(V5 인덱스만/native SQL/period 스코프/§6 권한/P95 인덱스 보장). | Bob (SM) |
| 2026-05-22 | 1.4 | Dev (James) QA 보강: `StatsControllerIntegrationTest.weekTrendSpansMultipleKstDays` 추가(다일 추세 7포인트 zero-fill·일자별 검증). 테스트 컴파일 그린(통합 CI). | James (Dev) |
| 2026-05-22 | 1.3 | Implemented by Dev (James). stats 도메인(DashboardStats/StatsPeriod/StatsRange/Converter/StatsService/StatsController) + V5 인덱스. range `aggregate(from,to)` 노출(3.3 재사용). 단위(StatsPeriodTest) 통과·컴파일·`build -x test` 그린; 통합(StatsControllerIntegrationTest) CI. Status Approved→Review. | James (Dev) |
| 2026-05-22 | 1.2 | PO (Sarah) Story 3.3 드래프트 검토 후속: Task 3 에 **range 집계 메서드 노출** 서브태스크 추가 — `StatsService`가 `[from,to)` 범위 메서드를 노출하고 `dashboard(period)`가 위임하도록 하여 보고서(3.3)가 임의 날짜/주차로 동일 집계를 재사용(중복/재작업 방지). ACs 무변경(구현 가이드 보강). | Sarah (PO) |
| 2026-05-22 | 1.1 | PO (Sarah) Epic 3 시퀀스 점검 후속: reports 테이블 소속 = Story 3.4(보관함 적재)로 확정 → Task 1 참조 "(3.3 V6)"→"(3.4 V6)" 정정. 마이그레이션 버전 V5(3.1 resolved_at)/V6(3.4 reports)/V7(3.4 notifications) 확정 — 데이터모델 §5.7/§5.8/§5.9 동기화. 스토리 ACs/Tasks 변경 없음(참조 정합성만). | Sarah (PO) |
| 2026-05-22 | 1.0 | Validated & Approved by PO (Sarah). Cross-checked against actual artifacts: V2 인덱스 인벤토리 확인(resolved_at만 누락 — Task 1 claim 정확), §6(stats=인증) / §5.9(DashboardStats) 일치, code-reuse anchors(IssueQueryService.startOfDay+KST, LocalDataSeeder JdbcTemplate) 존재 확인, com.smcs.stats 부재(greenfield). Status Draft→Approved. 기록 2건: (1) AC4 P95는 인덱스 설계 보장이며 부하측정 아님(MVP 허용); (2) byAssignee는 V2 (assigned_to,status) 복합 + V5 resolved_at 로 커버 — 중복 인덱스 불필요. | Sarah (PO) |

## QA Results

**검수자: Quinn (QA Test Architect) · 2026-05-22 · 게이트: 🟢 PASS · Status Done (PR #14 CI green run 26277696452)**

**CI 확정**: Backend(Gradle+Java21) pass 1m29s — 통합 테스트 4종(today 집계 정확성 / week 다일 추세 7포인트 / 미인증 401 / 잘못된 period 400) Testcontainers Postgres에서 통과. R1(집계)·R4(400 메커니즘) 경험적 검증 완료. Frontend pass(회귀 없음).

코드를 직접 추적 검증(작성자 주장 비의존). 위험영역 R1~R5:

| 위험영역 | 검수 방법 | 결과 |
| :--- | :--- | :--- |
| R1 집계 정확성(KPI/분포/추세) | StatsService SQL 정적 추적 + 통합 테스트(today: new4/resolved2/open2/avg90/byCat1·4/byPri4/byAsg1·2/trend1) | 🟢 정적 PASS · CI 확인 |
| R2 KST 경계(today/week/month, `AT TIME ZONE` 추세) | `StatsPeriodTest` 독립 재실행 통과(TODAY/MONTH 정확 instant, WEEK=ISO월요일·7일) | 🟢 PASS |
| R3 timestamptz 바인딩 | `OffsetDateTime(UTC)` 통일 — 시드 `Timestamp.from`과 달리 모호성 제거. 정적 검증 | 🟢 PASS |
| R4 권한·입력검증 | `isAuthenticated()`(§6) + invalid period→converter→`MethodArgumentTypeMismatch`→400. 401/400/default 통합 테스트 | 🟢 정적 PASS · 400 메커니즘 CI 확인 |
| R5 회귀·스키마 | V5=resolved_at 인덱스만, 엔티티 무변경(validate). 컴파일+`build -x test` 그린 | 🟢 PASS |

**보강 반영(1차 검수 관찰 #1):** 다일 추세 통합 미검증 → Dev가 `weekTrendSpansMultipleKstDays` 추가(월~일 7포인트 zero-fill, trend[0]=월 new2/resolved1, trend[1]=화 new1/resolved1). QA 트레이스 검증 — 임팩트 일치. **해소.**

**잔여 관찰(비차단):** `trend()`는 `to`가 KST 자정 정렬임을 전제 — period·3.3 범위 모두 자정이라 안전(3.3 스토리에 KST 경계 명시됨).

**결함 0.** AC 1~5 충족(P95<500ms = 인덱스 설계 보장, Deviation#5 — PO 비준). 단위/컴파일/assemble 로컬 그린, 통합은 Docker 미가용(Windows)로 **CI 실행 대기**.

**게이트 확정:** PR #14 CI green(run 26277696452) 확인 → 게이트 PASS, Status Review→Done. 머지 준비 완료.
