# Story 2.5: 현장 작업자 모바일 홈

## Status: Done

> QA Gate (2026-05-22, Quinn — Test Architect): 🟢 **PASS**. 결함 0. **CI 그린 선결**(PR #9 run 26268268723 — Backend `> Task :test` = `MeAssignedIntegrationTest`(a~d: FIELD 본인 배정분만+severity 정렬 / 타 FIELD 제외 / 비FIELD 403 / 미인증 401) + 회귀 통합, Frontend 81/81). QA 독립 재실행 프론트 81/81. 위험영역 정적리뷰 통과: me/assigned `@PreAuthorize(hasRole FIELD)` + principal 기반 self-scope(assignedTo=userId, IDOR 불가)(R1)·IssueCard a11y(role=button/tabIndex/Enter·Space/minHeight 44)(R2)·AC6 분리 준수(모바일 홈에 알림/벨 미렌더 — Story 2.8)(R3)·스키마 무변경(R4)·severity 정렬 defaultOrder 재사용. PO 비준 Deviation 일치. 잔여 비차단: ADMIN 모바일 me/assigned 접근 엣지(FIELD 한정, Deviation #3)·테스트 jsdom 경고(Ant portal, 비기능).

## Story

- As a **현장 작업자(FIELD)**
- I want **폰에서 내게 배정된 이슈들을 우선순위 순으로 보기를**
- so that **무엇부터 할지 즉시 알 수 있다**

## Acceptance Criteria (ACs)

1. `/m` 모바일 홈에 내게 배정된 이슈가 카드 스택으로 표시(**기본 정렬: 우선순위 내림차순 → 접수일 오름차순**).
2. 각 카드는 제목, **카테고리 L1>L2>L3**, 우선순위 색상 뱃지, 접수일을 보여준다.
3. 우선순위 URGENT 카드는 좌측 빨간 막대 또는 강한 색상 뱃지로 시각 강조.
4. 카드 탭 시 모바일 상세 화면으로 이동.
5. 모바일 반응형(360px 폭부터 정상 동작), 큰 터치 영역(최소 44px).
6. ~~화면 상단에 미읽음 알림 카운트가 표시되고, 탭하면 알림 페이지로 이동.~~ **→ 별도 Notification 스토리로 이관(Deviation #1, 사용자 결정). 본 스토리 범위 밖.**

## Tasks / Subtasks

- [x] **Task 1: `GET /api/me/assigned` — 내게 배정된 이슈 목록** (AC: 1, 2)
  - [x] `IssueQueryService.listAssigned(Long userId)` 추가(`@Transactional(readOnly = true)`) — `assigned_to = userId` 인 이슈를 **우선순위 내림차순(severity) → 접수일 오름차순**으로 조회. 정렬은 기존 `IssueSpecifications.defaultOrder()` 재사용(2.2 의 CASE severity 정렬), 필터는 `assignedTo` equal 술어(2.2 `IssueSpecifications` L36-37 패턴). 결과는 `IssueSummary` 로 매핑 — 카테고리/담당자 이름 해석은 기존 `list()` 의 N+1 회피 매핑(`CategoryRepository.findAll()` 맵, `UserRepository.findAllById`) 재사용. [Source: backend/.../issue/IssueQueryService.java(list 매핑·defaultOrder 사용); backend/.../issue/IssueSpecifications.java#L36-37,#defaultOrder; prd/5-data-models.md#5.2(기본 정렬 priority DESC→createdAt ASC)]
  - [x] `IssueController` 확장: `@GetMapping("/me/assigned")`, **`@PreAuthorize("hasRole('FIELD')")`**(§6 `/api/me/assigned` = FIELD), `@AuthenticationPrincipal Object principal` → `(Long)` userId. 응답 `List<IssueSummary>`(페이징 없음 — 내 배정분 소량, Deviation #4). [Source: prd/6-rest-api-spec-요약.md(GET /api/me/assigned = FIELD); backend/.../issue/IssueController.java(principal 패턴); architecture/6-...#6.3(FIELD 본인 배정만)]

- [x] **Task 2: 프론트 API/훅** (AC: 1)
  - [x] `api/issues.ts` 확장: `getMyAssigned(): Promise<IssueSummary[]>` → `apiClient.get('/me/assigned')`. apiClient 경유(직접 axios 금지). 타입은 기존 `IssueSummary`(2.2) 재사용 — 신규 타입 불필요. [Source: frontend/.../api/issues.ts(2.2~2.4 패턴); frontend/.../types/issue.ts(IssueSummary); prd/9-coding-standards.md#9.1]
  - [x] `shared/hooks/useMyAssigned.ts` 신규 — TanStack Query `useQuery` `queryKey:['me','assigned']`, `staleTime` 30s(리스트 신선도 §9.7). [Source: frontend/.../shared/hooks/useIssues.ts(TanStack 선례); architecture/9-frontend-architecture.md#9.7(이슈 리스트 30s)]

- [x] **Task 3: `<IssueCard>` 컴포넌트 — §9.6.5** (AC: 2, 3, 5)
  - [x] `shared/components/IssueCard.tsx` 신규: props `{issue: IssueSummary; onClick?; showAssignee?=true; compact?=false}`. **좌측 우선순위 색상 막대**(URGENT=red 강조, AC3) + 제목 + 카테고리 `L1>L2>L3` + 우선순위 `<PriorityBadge>` 재사용 + 접수일(KST `dayjs`). **a11y/터치**: `role="button"`, `tabIndex=0`, Enter/Space 키 → onClick, 최소 44x44px 터치 영역(§9.3). 색상 단독 금지(텍스트 라벨 동반 — PriorityBadge 가 보장). [Source: architecture/9-frontend-architecture.md#9.6.5(IssueCard props·좌측 색상막대·44px·role=button·Enter/Space); #9.3(360px~·44px); frontend/.../shared/components/PriorityBadge.tsx(재사용)]

- [x] **Task 4: `MobileFieldHomeView` 실구현 (placeholder 교체)** (AC: 1, 3, 4, 5)
  - [x] `features/mobile-field/MobileFieldHomeView.tsx` — 현재 placeholder(Ant Card "준비 중") 교체. `useMyAssigned()` → `IssueCard` 카드 스택(서버 정렬 순서 유지). 빈 상태(`<Empty>` "배정된 이슈가 없습니다"), 로딩(`<Spin>`). **반응형**: 컨테이너 max 480 / 360px 폭부터 정상(가로 스크롤 없음), 카드 세로 스택. URGENT 강조는 IssueCard 가 처리(AC3). [Source: frontend/.../features/mobile-field/MobileFieldHomeView.tsx(placeholder); architecture/9-frontend-architecture.md#9.3(모바일 360~768px)]
  - [x] 카드 탭(AC4): `navigate('/m/issues/' + issue.id)`. **모바일 상세 `/m/issues/:id`(MobileFieldDetailView)는 Story 2.6** — 본 스토리는 경량 placeholder 라우트/뷰 추가(2.1→2.3 placeholder 선례, Deviation #2). [Source: architecture/9-frontend-architecture.md#9.5(`/m/issues/:id` = MobileFieldDetailView, FIELD 본인배정+ADMIN); docs/stories/2.1.story.md(placeholder 선례)]
  - [x] `routes.tsx`: `/m/issues/:id` 라우트 추가 — `RequireAuth` + `RequireRole(['FIELD','ADMIN'])` + lazy `MobileFieldDetailView`(placeholder). 기존 `/m` 가드 무손상. "Future routes" 주석에서 `/m/issues/:id` 현행화(외과적). [Source: frontend/.../routes.tsx(기존 /m 가드·lazy 패턴); architecture/9-frontend-architecture.md#9.5]
  - [x] `features/mobile-field/MobileFieldDetailView.tsx` 신규 placeholder(Ant `Result`/`Card` "Story 2.6 준비 중") — 2.6 이 실구현 교체. [Source: docs/stories/2.1.story.md#IssueDetailView placeholder 선례]

- [x] **Task 5: 백엔드 통합 테스트** (AC: 1)
  - [x] 통합(Testcontainers, `@ActiveProfiles("local")`, 자체 데이터 — 2.3/2.4 패턴): **(a)** `field1` 에 배정된 이슈 2건(우선순위 URGENT/LOW) 생성·배정 후 `GET /api/me/assigned` → 200, **본인 배정분만** + **정렬 URGENT 먼저**(severity desc), **(b)** `field2` 의 배정 이슈는 `field1` 응답에 **미포함**(본인 스코프), **(c)** 비 FIELD(`agent1`) → 403, **(d)** 미인증 → 401. 배정은 2.4 `POST /assign`(agent1) 또는 jdbc 로 설정. **로컬 Windows 미실행 → CI 검증**(2.1~2.4 선례). [Source: docs/stories/2.4.story.md#Task8(통합 패턴·CI 주의); prd/6-rest-api-spec-요약.md]
  - [x] 회귀: 기존 통합(2.1~2.4) 그린.

- [x] **Task 6: 프론트 테스트** (AC: 2, 3, 4, 5)
  - [x] Vitest+RTL: `IssueCard.test.tsx` — 제목/카테고리 경로/`PriorityBadge`/접수일 렌더, **URGENT 강조 요소 존재**(AC3), `role="button"` + Enter/Space·click → onClick(AC5 a11y), 카테고리 `L1>L2>L3` 포맷. `MobileFieldHomeView.test.tsx` — mock `useMyAssigned` → 카드 스택 렌더(서버 정렬 순서), 카드 클릭 → `/m/issues/:id` navigate(AC4), 빈 목록 → 빈 상태. QueryClientProvider/MemoryRouter 래핑(2.2~2.4 패턴). [Source: docs/stories/2.2.story.md#Task6(RTL 패턴); docs/stories/2.4.story.md#Task9]
  - [x] 회귀: 기존 75건 통과 유지(2.4 기준 75/75). [Source: docs/stories/2.4.story.md(75/75)]

- [x] **Task 7: 골든 패스 검증 + 빌드** (라이브 스택 = QA/사용자, 자동 등가물 통과 — 2.4 선례)
  - [x] `field1`(FIELD) `/m` 진입 → 내 배정 이슈 카드 스택(URGENT 먼저·강조)·카드 탭 → `/m/issues/:id`(placeholder). — 자동: `MobileFieldHomeView.test` + `MeAssignedIntegrationTest`(CI).
  - [x] `npm test`(기존 75 + 신규 회귀 그린) / `npm run build` ✅ **§9.7 가드레일 재확인**(eager < 300KB gzip; `/m` 모바일 라우트는 별도 lazy 청크 §9.7 — IssueCard 포함) / `./gradlew build -x test` ✅ assemble + 단위(통합 CI). [Source: architecture/9-frontend-architecture.md#9.7(모바일 라우트 별도 청크); docs/stories/2.4.story.md#Task10]

## Dev Notes

[[LLM: docs 아티팩트 추출 본 스토리 한정 정보. 코딩/테스트 표준은 dev 가 이미 인지.]]

### Previous Story Insights (2.1~2.4 carry-over)

[Source: docs/stories/2.2.story.md, 2.4.story.md]

- **재사용 산출물**: `IssueSummary` DTO(2.2 — id/title/categoryL1~3Name/priority/status/assigneeName/createdAt, 카드에 그대로 적합), `IssueSpecifications.defaultOrder()`(severity desc→createdAt asc) + `assignedTo` 술어 패턴(2.2), `IssueQueryService.list()` 의 카테고리/사용자 이름 N+1 회피 매핑, `PriorityBadge`(색상+텍스트+아이콘), `apiClient`+`api/issues.ts`, TanStack Query(QueryClientProvider in main.tsx), `RequireRole`/`RequireAuth`/`AppLayout`, `IssueController` principal `(Long)` 패턴.
- **`MobileFieldHomeView` 는 현재 placeholder**(Ant Card "준비 중"). 본 스토리가 실구현.
- **`/m` 라우트 = `RequireRole(['FIELD','ADMIN'])`** 이미 존재. `/m/issues/:id` 는 미라우팅 → 본 스토리가 placeholder 라우트 추가(상세 실구현은 2.6).
- **`GET /api/me/assigned` 미구현** — 본 스토리가 신규(FIELD 본인 배정 피드).
- **알림 모듈 전무**(V2 `notifications` 테이블만) — AC6 및 알림 생성 부수효과(2.3 #4/2.4 #1 미룸)는 **별도 Notification 스토리**(사용자 결정, Deviation #1).
- **통합테스트 CI 게이트**: 로컬 Windows Testcontainers 미실행 → GitHub Actions. ci.yml 시크릿 기주입.
- **번들**: 모바일 라우트(`/m/*`)는 별도 lazy 청크(§9.7·§9.5) — 데스크톱 사용자가 안 받도록. `/m` 이미 lazy.
- **단일 트랜잭션·readOnly·apiClient 경유·React 불변·a11y(색상 단독 금지)** — 2.1~2.4 동일.

### Data Models / 권한

[Source: prd/5-data-models.md#5.2; architecture/6-...#6.3; prd/6-rest-api-spec-요약.md]

- **`IssueSummary`**(응답): `id, title, categoryL1Name, categoryL2Name, categoryL3Name, priority, status, assigneeName, createdAt`. PII(발신자) 없음 — 카드에 불필요.
- **정렬**: priority severity 내림차순(URGENT→LOW) → createdAt 오름차순. `IssueSpecifications.defaultOrder()` 가 CASE 식으로 보장(2.2 R2).
- **권한(§6.3)**: 이슈 조회 = FIELD 본인 배정만. `GET /api/me/assigned` 는 호출자 `assigned_to = self` 만 반환하므로 자체 스코프 충족. `@PreAuthorize("hasRole('FIELD')")`(§6 표). ADMIN 모바일 접근은 엣지(데스크톱 사용) — Deviation #3.
- 시간 ISO-8601, 서버 UTC 저장(2.2~2.4 동일). KST 표기는 프론트 `dayjs`.

### API / 규약

[Source: prd/6-rest-api-spec-요약.md; architecture/7-api-design.md#7.3]

- `GET /api/me/assigned` (FIELD) — 응답 `List<IssueSummary>`(페이징 없음 — Deviation #4). `GET /api/*` no-store(§7.3) — 프론트 30s staleTime.
- 에러 표준 `{code,message,traceId}` — 기존 핸들러 재사용(신규 에러 코드 없음).

### Deviation Analysis (PO 검토 필요)

1. **[1급] AC6(미읽음 알림 카운트/벨) → 별도 Notification 스토리 (사용자 결정)**: 알림 모듈(엔티티/`unread-count`/`list`/`read`/`read-all` + 벨 뱃지 + 30s 폴링 §8.5 + **생성 부수효과**[배정/코멘트/전이 시 INSERT — 2.3 #4/2.4 #1 미룸])은 2.3/2.4/2.7 에 걸치는 횡단 모듈. 본 스토리(모바일 홈 AC1~5)와 분리해 신규 전용 스토리로. **AC6 는 본 스토리 범위 밖** — 모바일 홈 상단 벨/카운트 미렌더(알림 스토리에서 추가). **PO 비준 필요** + 알림 스토리 신설(권장 번호: 2.5 뒤 또는 2.7 전).
2. **[2급] 모바일 상세 `/m/issues/:id` = Story 2.6**: §9.5 `/m/issues/:id` = `MobileFieldDetailView`(사진 첨부+조치, 2.6). 본 스토리 AC4 카드 탭 → `/m/issues/:id` 이동 + **경량 placeholder 라우트/뷰** 추가(2.1→2.3 `/issues/:id` placeholder 선례). 2.6 이 실구현 교체. **PO 비준**.
3. **[기록] `GET /api/me/assigned` 권한 = FIELD**: §6 표 FIELD. 자체 스코프(`assigned_to=self`). ADMIN 모바일 접근은 엣지(ADMIN=데스크톱) → FIELD 한정. 필요 시 후속 `hasAnyRole('FIELD','ADMIN')` 확장.
4. **[기록] me/assigned = 페이징 없는 `List<IssueSummary>`**: 내 배정분은 소량(모바일 카드 스택) → 페이징 불필요. `IssueSummary`(2.2) 재사용, 신규 DTO 없음.
5. **[기록] `<IssueCard>` §9.6.5 신규**: 모바일/데스크톱 공용. `PriorityBadge` 재사용, a11y(role=button/tabIndex/Enter·Space/44px), 좌측 우선순위 색상막대(AC3). 색상 단독 금지(텍스트 동반).
6. **[기록] 모바일 청크 분리**: `/m` 이미 lazy(§9.7). IssueCard·MobileFieldHomeView 는 모바일 청크. 빌드 시 eager <300KB gzip 재확인.

### Project Structure Notes

- 확장 backend: `IssueQueryService`(listAssigned — readOnly), `IssueController`(`GET /me/assigned`), `IssueSpecifications`(assignedTo 술어 — 또는 인라인 Specification). 스키마/Flyway 변경 없음(assigned_to V2 기존, ddl-auto validate).
- 신규 frontend: `shared/components/IssueCard.tsx`(+test), `shared/hooks/useMyAssigned.ts`, `features/mobile-field/MobileFieldDetailView.tsx`(placeholder). 확장: `api/issues.ts`(getMyAssigned), `features/mobile-field/MobileFieldHomeView.tsx`(실구현+test), `routes.tsx`(`/m/issues/:id` 라우트+주석).
- `IssueSummary` 타입 재사용(2.2) — `types/issue.ts` 변경 없음.

### Testing

Dev Note: Story Requires the following tests:

- [x] **JUnit 통합(Testcontainers, `backend/src/test/java/com/smcs/issue/`)**: `GET /api/me/assigned`(FIELD 본인 배정분만+severity 정렬 / 타 FIELD 배정 미포함 / 비FIELD 403 / 미인증 401). **로컬 Windows 미실행 → CI 검증**(2.1~2.4 선례). 회귀 통합 그린.
- [x] **Vitest+RTL(nextToFile:true)**: `IssueCard.test.tsx`(렌더·URGENT 강조·a11y role=button/키보드·카테고리 포맷), `MobileFieldHomeView.test.tsx`(카드 스택·정렬순서·탭 navigate·빈 상태). 기존 75건 회귀 통과.
- [ ] E2E: 범위 밖(Story 4.x).

Manual Test Steps: Task 7 참조 (field1 `/m` → 내 배정 카드 스택·URGENT 강조·카드 탭 → /m/issues/:id placeholder / `npm test`·`build`·`gradlew`(통합 CI)).

## Dev Agent Record

### Agent Model Used: James (dev) — claude-opus-4-7[1m]

### Debug Log References

| Task | Issue | Resolution | Reverted? |
| :--- | :---- | :--------- | :-------- |
| 6 | `IssueCard.test` URGENT 테두리 단언이 jsdom 색상/shorthand 정규화로 실패(hex→rgb, borderLeft 분해). | 색상 문자열 의존 제거 → URGENT(6px) vs 비URGENT(4px) 두께 차이로 AC3 강조 검증. | No (테스트 보강) |

### Completion Notes List

- **AC6 미구현(정상)**: 미읽음 알림 카운트는 Story 2.8(Notification 모듈)로 분리됨(PO 비준). 모바일 홈 상단 벨/카운트 미렌더.
- **`GET /api/me/assigned`**: `IssueQueryService.listAssigned` — `assignedTo` equal Specification + 기존 `IssueSpecifications.defaultOrder()`(severity) 재사용, `IssueSummary` 인라인 매핑(list() 무손상). `@PreAuthorize("hasRole('FIELD')")`.
- **`<IssueCard>`(§9.6.5)**: 좌측 우선순위 막대(URGENT 6px+그림자 강조), role=button/tabIndex/Enter·Space, PriorityBadge 재사용. 모바일/데스크톱 공용.
- **AC4 탭 → `/m/issues/:id`**: `MobileFieldDetailView` placeholder + 라우트(RequireRole FIELD/ADMIN) 추가 — 실구현은 Story 2.6(2.1→2.3 선례).
- **로컬 검증**: 프론트 81/81(75+6), `npm run build` 그린(eager ≈256KB gzip<300KB, MobileFieldHomeView lazy 2KB·DetailView 0.5KB). BE 컴파일·assemble·통합 컴파일 클린. **통합 `MeAssignedIntegrationTest`(a~d)는 CI 검증**(Windows Testcontainers 한계).

### File List

**Backend — new:** `test/.../issue/MeAssignedIntegrationTest.java`

**Backend — modified:** `issue/IssueQueryService.java`(listAssigned), `issue/IssueController.java`(GET /me/assigned)

**Frontend — new:** `shared/components/IssueCard.tsx`(+test), `shared/hooks/useMyAssigned.ts`, `features/mobile-field/MobileFieldDetailView.tsx`

**Frontend — modified:** `api/issues.ts`(getMyAssigned), `features/mobile-field/MobileFieldHomeView.tsx`(placeholder→impl, +test), `routes.tsx`(/m/issues/:id 라우트)

### Change Log

| Date       | Version | Description | Author |
| :--------- | :------ | :---------- | :----- |
| 2026-05-22 | 1.2 | QA 최종 게이트(Quinn): 🟢 **PASS**, Status → **Done**. PR #9 CI run 26268268723 그린(Backend `> Task :test`=MeAssignedIntegrationTest a~d Testcontainers + 회귀 + Frontend 81/81). QA 독립 재실행 81/81. 위험영역 R1~R4 정적통과(me/assigned FIELD+self-scope IDOR불가/IssueCard a11y/AC6 분리 준수/스키마 무변경). 결함 0. PO 비준 Deviation 일치. 직접 PASS. | Quinn (QA) |
| 2026-05-22 | 1.1 | Dev 구현 완료(James) → Status Approved→Review. Task 1–7. BE: IssueQueryService.listAssigned(assignedTo spec+defaultOrder 재사용)+GET /me/assigned(@PreAuthorize FIELD). FE: getMyAssigned/useMyAssigned, IssueCard(§9.6.5 좌측막대 URGENT강조·a11y), MobileFieldHomeView 실구현(카드스택/반응형/빈상태), MobileFieldDetailView placeholder + /m/issues/:id 라우트. AC6은 Story 2.8 분리(미렌더). 로컬: 프론트 81/81(75+6), build 그린(eager≈256KB gzip, mobile lazy 2KB), BE 컴파일·assemble·통합 컴파일 클린. 통합(a~d)=CI. | James (dev) |
| 2026-05-22 | 1.0 | PO 검증 통과 → Status Draft→Approved. story-draft-checklist READY(9/10). 출처 독립검증: Epic 2.5 AC1~5 정확 일치·REST §6(me/assigned=FIELD)·§9.5(/m·/m/issues/:id)·§9.6.5(IssueCard)·재사용 자산(IssueSummary/IssueSpecifications.defaultOrder/IssueQueryService 매핑/PriorityBadge/RequireRole) 실재·me/assigned 미구현·알림 모듈 전무 확인. **사용자/PO 비준**: Deviation #1(AC6 미읽음 알림 → 별도 Notification 스토리 분리) 비준 — 알림은 횡단 모듈(2.3/2.4/2.5/2.7). **필수 후속: Notification 전용 스토리 신설**(없으면 Epic 2.5 AC6 미충족 잔존; SM ad-hoc 생성 또는 epic 추가). Deviation #2(모바일 상세 /m/issues/:id placeholder=2.6, 2.1→2.3 선례) 비준; #3(me/assigned FIELD 권한, ADMIN 모바일 엣지)·#4(페이징 없는 List<IssueSummary>)·#5(IssueCard §9.6.5 신규)·#6(모바일 청크) PO 승인. dev 착수 가능. | Sarah (PO) |
| 2026-05-22 | 0.1 | Initial draft by SM (Bob) via create-next-story. Epic 2 Story 2.5. Sourced: prd/epic-2 Story 2.5(AC1-6) + architecture §6.3(FIELD 본인배정)/§9.3(모바일 360~768px·44px)/§9.5(/m·/m/issues/:id 라우트)/§9.6.5(IssueCard)/§9.7(모바일 청크) + prd §5.2(IssueSummary·정렬)/§5.7(Notification)/§8.5(폴링) + REST §6(me/assigned=FIELD, notifications) + 2.2~2.4 carry-over(IssueSummary/IssueSpecifications.defaultOrder/IssueQueryService 매핑/PriorityBadge/RequireRole) + 실제 코드(MobileFieldHomeView placeholder, /m 라우트, me/assigned 미구현, notifications 테이블만). **AC6(알림)는 사용자 결정으로 별도 Notification 스토리 분리** — 2.5 = 모바일 홈(AC1~5). Deviation 6건(AC6 분리[1급]/모바일상세 2.6[2급]/me-assigned 권한 FIELD/페이징 없음/IssueCard 신규/모바일 청크). | Bob (SM) |

## QA Results

**Reviewer:** Quinn (Test Architect) · **Date:** 2026-05-22 · **Gate:** 🟢 **PASS** · **결함:** 0

### CI 증거 (선결조건 충족)
- **PR #9** `feat/story-2.5-mobile-home` (head `a90017e`), run **26268268723**:
  - **Backend (Gradle + Java 21)** 🟢 pass 1m16s — CI 로그 `> Task :test` 실행 → `MeAssignedIntegrationTest`(a~d) Testcontainers 통과 + 회귀 통합(2.1~2.4) 무손상.
  - **Frontend (Vite + Vitest)** 🟢 pass 44s — 81/81.
- **QA 독립 재실행**: 프론트 `vitest run` → **81/81** (19 파일).

### 위험영역 정적 리뷰 (1차 증거)
| ID | 항목 | 검증 | 결과 |
| :- | :--- | :--- | :--: |
| R1 | me/assigned 권한·스코프 | `@PreAuthorize("hasRole('FIELD')")` + `listAssigned(userId)` 의 `userId` = `@AuthenticationPrincipal` (요청 파라미터 아님) → 본인 배정분만, **IDOR 불가** | ✅ |
| R2 | IssueCard a11y | `role="button"`, `tabIndex=0`, `onKeyDown`(Enter/Space), `minHeight:44` (§9.3·§9.6.5) | ✅ |
| R3 | AC6 분리 준수 | `features/mobile-field/` 에 알림/벨/unread 미렌더 — Story 2.8 이관(PO 비준) | ✅ |
| R4 | 스키마 무변경 | 신규 Flyway/SQL diff 0건 (assigned_to V2 기존) | ✅ |
| — | 정렬 | `IssueSpecifications.defaultOrder()`(severity CASE) 재사용 — 통합 (a) URGENT-first 검증 | ✅ |

### AC 추적성
- AC1(카드 스택·severity 정렬)·AC2(제목/카테고리/우선순위/접수일)·AC3(URGENT 강조)·AC4(탭→/m/issues/:id)·AC5(반응형 360·44px a11y) — 통합(a~d) + 프론트 RTL(IssueCard·MobileFieldHomeView)로 매핑.
- **AC6 = Story 2.8 이관**(PO 비준) — 본 스토리 미충족이 정상. Epic 2.5 AC6 은 2.8 완료 시 충족.

### Deviation 확인
- #1(AC6 → Story 2.8)·#2(모바일 상세 placeholder=2.6)·#3(me/assigned FIELD, ADMIN 엣지)·#4(페이징 없음)·#5(IssueCard 신규)·#6(모바일 청크) — **전부 PO 비준대로 구현**.

### 잔여 (비차단)
- ADMIN 의 `/m` 접근 시 me/assigned 403(FIELD 한정) — ADMIN=데스크톱 사용 엣지(Deviation #3). 필요 시 후속 `hasAnyRole` 확장.
- 테스트 stderr `getComputedStyle` 경고 — Ant portal jsdom 미구현, 비기능.

**판정:** CI 선결 충족 + PO 비준 선행 → **직접 PASS**. 결함 0. Status → **Done**.
