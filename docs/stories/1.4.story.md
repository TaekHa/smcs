# Story 1.4: 로그인 화면 및 인증 컨텍스트 (프론트)

## Status: Review

## Story

- As a **사용자**
- I want **로그인 화면에서 인증한 후 토큰이 자동으로 모든 API 호출에 첨부**되기를
- so that **내가 매번 토큰을 신경 쓰지 않고 작업할 수 있다**

## Acceptance Criteria (ACs)

1. 로그인 화면이 username/password 폼과 에러 메시지 영역을 제공한다.
2. 로그인 성공 시 JWT를 메모리(Zustand 스토어) + `localStorage` 에 저장하고 메인 화면으로 이동한다.
3. `AuthProvider` 컨텍스트(또는 Zustand 스토어 + `useAuth` 훅)가 현재 사용자 정보를 앱 전체에 제공한다.
4. API 클라이언트가 모든 요청에 `Authorization: Bearer <token>` 헤더를 자동 첨부한다.
5. 토큰 만료/401 응답 시 자동 로그아웃 + 로그인 화면 리다이렉트.
6. 로그아웃 버튼이 동작한다 (토큰/사용자 상태 클리어 + `/login` 리다이렉트).

## Tasks / Subtasks

- [x] **Task 1: 의존성 추가 (`package.json`)** (AC: 1, 2, 3, 4, 6)
  - [x] Production: `react-router-dom@6`, `zustand`, `antd@5`, `@ant-design/icons`, `dayjs`.
  - [x] Dev: `vitest`, `@vitest/coverage-v8`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`.
  - [x] `@axe-core/react` 는 본 스토리 미도입 (선택 항목).
  - [x] `npm run build` 회귀 통과 (178KB → gzip 60KB, 82 modules).

- [x] **Task 2: Vitest + RTL 설정**
  - [x] `vitest.config.ts` (jsdom + globals + setup), `src/test/setup.ts` (`@testing-library/jest-dom` + matchMedia polyfill — antd 응답형 그리드 요구).
  - [x] tsconfig types (`vitest/globals`, `@testing-library/jest-dom`).
  - [x] **`vitest.config.ts` 는 tsc include 에서 제외** (vitest 4 의 번들 vite 타입과 프로젝트 vite@5.4.8 충돌 회피).
  - [x] npm scripts: `test`, `test:watch`, `test:coverage`. 샘플 App.test.tsx 통과.

- [x] **Task 3: 타입 정의 src/types/auth.ts** — Role, UserSummary, LoginRequest, LoginResponse, ApiError 모두 백엔드 record 와 1:1 일치.

- [x] **Task 4: AuthStore (Zustand)** — `src/auth/useAuthStore.ts`. setSession/logout/hydrate, `'smcs.auth'` key, hydrate 가 잘못된 JSON/부분 페이로드 안전 처리, `useAuth` + `useHydrated` selector hooks.

- [x] **Task 5: apiClient 인터셉터** — Request 에 Bearer 자동 첨부, Response 401 → logout + `redirectToLogin()` (`/auth/login` 401 은 제외). **`window.location.assign` 호출을 `redirectToLogin()` util 로 추출** (jsdom 의 non-writable location.assign 우회 → 테스트 mock 용이성).

- [x] **Task 6: API 함수** — `src/api/auth.ts`(`login`), `src/api/me.ts`(`getMe`). 기존 `src/api/health.ts` 는 App.tsx 가 더 이상 사용 안 함 → **삭제** (CLAUDE.md 의 "본인 변경으로 orphan 된 코드 제거" 원칙).

- [x] **Task 7: 부팅 hydrate** — `App.tsx` 가 mount 시 hydrate → token 있으면 getMe 검증 → `hydrated && !validating` 까지 `<Spin>` splash.

- [x] **Task 8: LoginView (Ant Design)** — Form/Input/Button, `<Alert role="alert">`, traceId 작은 글씨, username 자동 포커스, 에러 코드 한글 매핑 (`mapError`).

- [x] **Task 9: 라우팅 + RequireAuth + AppLayout** — `BrowserRouter`, `/login`, `/`, `*→/` fallback. `<LoginRoute>` 이 인증된 사용자 자동 `/` 리다이렉트. `<HomeView>` 가 환영 메시지 (Story 1.5 가 역할별 리다이렉트로 대체 예정 명시).

- [x] **Task 10: 로그아웃** — `<AppLayout>` 헤더의 "로그아웃" 버튼 → `useAuthStore.logout()` + `navigate('/login', {replace:true})`.

- [x] **Task 11: 단위 + 컴포넌트 테스트 4건** (총 20 tests, 5 files, all pass)
  - [x] `useAuthStore.test.ts` (5 cases): setSession 저장, logout 클리어, hydrate 복원, hydrate corrupted JSON 무시, hydrate partial payload 무시.
  - [x] `client.test.ts` (5 cases, `redirectToLogin` mock): Authorization 헤더 첨부 / 토큰 없을 때 헤더 미첨부 / 401 non-login → logout + redirect / 401 /auth/login → logout 호출 안 함 / 500 → 세션 유지.
  - [x] `LoginView.test.tsx` (7 cases): 폼 렌더 / required validation / 성공 → setSession + navigate / 401 → INVALID_CREDENTIALS 한글 / 423 → ACCOUNT_LOCKED 한글 / 429 → RATE_LIMIT 한글 / 네트워크 오류 한글.
  - [x] `RequireAuth.test.tsx` (2 cases): 미인증 → `/login` Navigate / 인증 → children 렌더.
  - [x] `App.test.tsx` (1 case): 미인증 부팅 시 로그인 화면 렌더.

- [x] **Task 12: 수동 골든 패스 검증**
  - [x] **build 회귀**: `npm run build` 통과 (681KB → gzip 224KB. chunk 분할 권고는 Epic 4 폴리시).
  - [x] **백엔드/프론트 통합 smoke** (curl via Vite proxy):
    - `/api/health` → 200 + `{"status":"UP"}` (Story 1.1 AC 3 보호).
    - `/api/auth/login` `agent1/dev1234` → 200 + JWT + UserSummary.
    - `/api/me` with Bearer → 200 + `{id:1, displayName:"김상담1", role:"AGENT"}`.
  - [x] **AC 1, 2, 3, 4, 5, 6 자동 검증**은 Task 11 의 RTL 테스트가 jsdom 환경에서 모두 통과 (각 케이스 한글 메시지/리다이렉트 검증). 브라우저 시각 검증은 dev 서버 가동 후 사용자가 직접 수행 권장.
  - [x] **로그인 에러 UX (잘못된 비밀번호 → 한글)**: `LoginView.test.tsx` 의 401 case 가 "사용자명 또는 비밀번호를 확인하세요." 매칭 통과.

## Dev Notes

### Previous Story Insights (Story 1.3)

[Source: docs/stories/1.3.story.md#completion-notes]

- **백엔드 API 스펙 확정**:
  - `POST /api/auth/login` 요청: `{username, password}`. 응답 200: `{token, expiresInSeconds, user:{id, username, displayName, role}}`. 응답 401/423: `{code, message, traceId}`.
  - `GET /api/me` 응답 200: `{id, username, displayName, role}` (`UserSummary`). 응답 401: 표준 에러.
- **JWT 알고리즘은 HS512** (시크릿 길이에 따라 자동 선택, HS256 → HS512 deviation). **프론트는 알고리즘 무관** — 토큰을 그대로 `Authorization: Bearer` 로 첨부만 하면 됨.
- **에러 코드 매핑** (1.3 Completion Notes 그대로):
  - `INVALID_CREDENTIALS` (401) — "사용자명/비밀번호 확인"
  - `ACCOUNT_LOCKED` (423) — "10분 후 다시 시도"
  - `UNAUTHORIZED` (401, entryPoint) — 토큰 만료/누락 시 자동 로그아웃 트리거
  - `RATE_LIMIT_EXCEEDED` (429) — Retry-After 헤더로 backoff
- **표준 에러 포맷**: `{code, message, traceId}` + `X-Trace-Id` 응답 헤더. 디버깅 시 traceId 가 백엔드 로그와 매칭됨.
- **Vite proxy 설정** (Story 1.1): `frontend/vite.config.ts` 의 `server.proxy['/api']` 가 백엔드 8080 으로 포워딩 → dev 시 CORS 신경 안 써도 됨.

### Tech Stack (이 스토리에 한정)

[Source: architecture/9-frontend-architecture.md#9.1]

| 분류 | 도구/버전 | 비고 |
|---|---|---|
| 라우팅 | `react-router-dom@6` | createBrowserRouter 또는 BrowserRouter + Routes |
| 클라이언트 상태 | `zustand@4` 또는 `@5` | AuthStore + 향후 UI 상태 (e.g. 사이드바 토글) |
| UI 키트 | `antd@5` + `@ant-design/icons@5` | Form/Input/Button/Alert 등. 한국어 친화 |
| HTTP | `axios` (이미 1.7.7 설치됨, 인터셉터만 추가) | JWT 자동 첨부 + 401 자동 로그아웃 |
| 날짜 | `dayjs@1.11+` | Ant Design 5 의 내부 의존성 (직접 import 도 가능). 본 스토리는 사용 안 함 — Epic 2 이슈 카드에서 첫 사용 |
| 테스트 | `vitest`, `@testing-library/react@16`, `@testing-library/jest-dom@6`, `jsdom` | 첫 셋업. 향후 모든 프론트 테스트 기반 |

**TanStack Query, React Hook Form, Zod 는 본 스토리 범위 밖** — Epic 2 의 데이터 페치/복잡 폼 스토리에서 도입.

### AuthStore 상세 스펙

```typescript
// src/auth/useAuthStore.ts (개요)
interface AuthState {
  token: string | null;
  user: UserSummary | null;
  hydrated: boolean;          // localStorage 복원 완료 플래그
  setSession(token: string, user: UserSummary): void;
  logout(): void;
  hydrate(): void;            // boot 시 1회 호출
}

const STORAGE_KEY = 'smcs.auth';

export const useAuthStore = create<AuthState>((set) => ({
  token: null,
  user: null,
  hydrated: false,
  setSession: (token, user) => {
    localStorage.setItem(STORAGE_KEY, JSON.stringify({ token, user }));
    set({ token, user });
  },
  logout: () => {
    localStorage.removeItem(STORAGE_KEY);
    set({ token: null, user: null });
  },
  hydrate: () => {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (raw) {
        const { token, user } = JSON.parse(raw);
        if (typeof token === 'string' && user?.id) {
          set({ token, user, hydrated: true });
          return;
        }
      }
    } catch { /* corrupted — fall through */ }
    set({ hydrated: true });
  },
}));

export const useAuth = () => useAuthStore((s) => s.user);
```

### API 클라이언트 인터셉터 상세

```typescript
// src/api/client.ts (수정 후 개요)
apiClient.interceptors.request.use((config) => {
  const token = useAuthStore.getState().token;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

apiClient.interceptors.response.use(
  (r) => r,
  (err) => {
    const status = err.response?.status;
    const url = err.config?.url ?? '';
    const isLoginRequest = url.endsWith('/auth/login');
    if (status === 401 && !isLoginRequest) {
      useAuthStore.getState().logout();
      // SPA 라우터 우회 — 단순 location.assign 으로 hard navigate
      if (window.location.pathname !== '/login') {
        window.location.assign('/login');
      }
    }
    return Promise.reject(err);
  }
);
```

**중요 결정**:
- `/auth/login` 의 401 은 logout 트리거 제외 — 그렇지 않으면 로그인 실패 시 자기 자신을 logout 하는 무의미한 동작.
- 423/429 는 logout 트리거 X — 사용자가 메시지를 봐야 의미가 있음.
- 라우터 redirect 는 `window.location.assign` 으로 단순 처리. React Router 의 `useNavigate` 는 hook 이라 인터셉터 컨텍스트에서 사용 불가.

### File Locations

[Source: architecture/4-monorepo-directory-structure.md#4.2]

```
frontend/
├── package.json                                                    # 수정 (Task 1)
├── tsconfig.json                                                   # 수정 (Task 2 — types 추가)
├── vitest.config.ts                                                # NEW (Task 2)
└── src/
    ├── main.tsx                                                    # 수정 (Task 7 — hydrate + router 셋업)
    ├── App.tsx                                                     # 수정 (현재 health 호출 → AuthGate 로직)
    ├── App.test.tsx                                                # NEW (Task 2 sample test)
    ├── routes.tsx                                                  # NEW (Task 9)
    ├── test/setup.ts                                               # NEW (Task 2)
    ├── api/
    │   ├── client.ts                                               # 수정 (Task 5 — 인터셉터)
    │   ├── client.test.ts                                          # NEW (Task 11)
    │   ├── auth.ts                                                 # NEW (Task 6)
    │   ├── me.ts                                                   # NEW (Task 6)
    │   └── health.ts                                               # 유지
    ├── auth/
    │   ├── useAuthStore.ts                                         # NEW (Task 4)
    │   ├── useAuthStore.test.ts                                    # NEW (Task 11)
    │   ├── RequireAuth.tsx                                         # NEW (Task 9)
    │   └── RequireAuth.test.tsx                                    # NEW (Task 11)
    ├── features/auth/
    │   ├── LoginView.tsx                                           # NEW (Task 8)
    │   └── LoginView.test.tsx                                      # NEW (Task 11)
    ├── layout/
    │   └── AppLayout.tsx                                           # NEW (Task 9)
    └── types/
        └── auth.ts                                                 # NEW (Task 3)
```

본 스토리는 **인증 흐름의 토대**만 구현. 본격적인 네비게이션 바와 역할별 라우팅 (`/issues` vs `/m`) 은 Story 1.5 범위.

### Routing Decisions (Story 1.5 와의 경계)

- **Story 1.4 의 `/`**: 임시 환영 화면. 인증 상태 + 로그아웃 버튼 표시만.
- **Story 1.5 의 `/`**: 역할별 자동 리다이렉트 (`AGENT/ADMIN → /issues`, `FIELD → /m`).
- **Story 1.5 의 `<AppLayout>`**: 본격 네비게이션 바 (상단 nav + 알림 벨 + 메뉴).
- 본 스토리는 1.5 의 작업이 들어갈 자리(`AppLayout`) 를 만들어두기만 함 — 1.5 가 컴포넌트 내용을 확장.

### Source References

- architecture/9-frontend-architecture.md#9.1 — Tech stack 선택 (Ant Design 5, Zustand, React Router 6, axios)
- architecture/9-frontend-architecture.md#9.2.1 — API 클라이언트 인터셉터 패턴
- architecture/9-frontend-architecture.md#9.5 — Routes Table (`/login` Public, `/` 역할 리다이렉트는 1.5)
- architecture/9-frontend-architecture.md#9.8 — Accessibility (Form labels, role="alert", 키보드 포커스)
- prd/9-coding-standards.md#9.1 — API 호출은 apiClient 경유 필수, 시크릿 하드코딩 금지
- prd/9-coding-standards.md#9.2 — Frontend naming (PascalCase 컴포넌트, camelCase + use 훅)
- docs/stories/1.3.story.md#completion-notes — 백엔드 DTO 스키마 + 에러 코드 매핑 + JWT alg 무관성

### Project Structure Notes

- architecture §4.2 의 `features/` 구조 따름. `features/auth/` 가 로그인 화면 위치.
- `auth/` 디렉터리는 store + RequireAuth (cross-cutting) 위치 — feature 라기보다 인프라.
- architecture §9.6 의 공유 컴포넌트(`<PriorityBadge>`, `<StatusBadge>` 등) 는 본 스토리 미사용 — Epic 2 에서 도입.

### Testing

Dev Note: Story Requires the following tests:

- [ ] **Vitest 단위 테스트** — `useAuthStore.test.ts`: setSession/logout/hydrate 행위. location: `src/auth/`. Coverage 90%+.
- [ ] **Vitest 단위 테스트** — `client.test.ts`: 인터셉터 동작 (Authorization 헤더 첨부, 401 자동 logout, login 401 제외). location: `src/api/`. axios-mock-adapter 또는 vi.spyOn 으로 mock.
- [ ] **RTL 컴포넌트 테스트** — `LoginView.test.tsx`: 폼 렌더, 빈 제출 validation, 성공 시 setSession + navigate 호출, 에러 코드별 메시지 매핑. location: `src/features/auth/`.
- [ ] **RTL 컴포넌트 테스트** — `RequireAuth.test.tsx`: 미인증 시 `<Navigate>` 렌더 / 인증 시 children 렌더. `MemoryRouter` 로 라우팅 컨텍스트 제공.
- [ ] **E2E**: 본 스토리 범위 밖. Story 4.x 폴리시 단계에서 Playwright 도입 검토.
- [ ] **Accessibility**: `@axe-core/react` 가 dev 모드에서 콘솔 경고. 자동 a11y 테스트 (axe-playwright) 는 v2.

**Manual Test Steps** (Task 12 와 동일):

1. 백엔드 가동 (`./gradlew bootRun --args="--spring.profiles.active=local"`).
2. 프론트 dev (`npm run dev`) → `http://localhost:5173/login`.
3. `agent1/dev1234` → 로그인 성공 → `/` 의 환영 메시지.
4. localStorage `smcs.auth` 키 존재 확인 + `/api/me` 요청 헤더 `Authorization: Bearer` 확인.
5. 로그아웃 → `/login` 이동 + localStorage 키 삭제.
6. localStorage 토큰 변조 → 새로고침 → 401 → 자동 logout.
7. 잘못된 비밀번호 5회 → 6회째 "10분 후 다시 시도" 메시지.
8. `npm run build` 회귀 통과.

## Dev Agent Record

### Agent Model Used: claude-opus-4-7 (1M context)

### Debug Log References

| Task | File | Change | Reverted? |
| :--- | :--- | :----- | :-------- |
| 1 | package.json | npm 이 antd 를 devDependencies 로 잘못 배치 → uninstall + reinstall 로 dependencies 로 이동 | No (정상 상태) |
| 1 | package.json | 기존 vitest@3 + coverage-v8@4 peer 불일치 → vitest 를 4.x 로 통일 | No (정상 상태) |
| 2 | tsconfig.json | `vitest.config.ts` 를 `include` 에 잠시 포함 → vitest 4 번들 vite 와 프로젝트 vite@5.4.8 타입 충돌 → include 에서 제외 | No (영구 제외) |
| 5, 11 | api/client.ts, api/navigation.ts | jsdom 의 `window.location.assign` 이 non-writable → `redirectToLogin()` util 로 추출하여 mock 가능하게 | No (개선) |
| 2 | test/setup.ts | jsdom 미구현 `window.matchMedia` polyfill 추가 (antd 응답형 그리드 요구) | No (필수) |

### Completion Notes List

- **AC 1~6 모두 통과** — Task 11 의 20 단위/컴포넌트 테스트 (5 파일) 가 모든 AC 의 코드 경로를 jsdom 환경에서 검증. Task 12 의 통합 smoke 가 Vite proxy → Backend → Postgres 풀스택 응답을 확인.

- **Tech stack version deviations**:
  - `antd@5.29.3` (story 명시 v5 유지), `@ant-design/icons@5.6.1` (story 명시 v5 유지). npm 의 latest 인 v6 은 antd v5 와 paired 가 다르므로 명시 핀.
  - `vitest@4.1.6` (story 추정 v1 미명시 → @vitest/coverage-v8@4 peer 일치 위해 v4 채택). 기존 install 의 v3 와 충돌 → v4 로 통일.
  - `zustand@5.0.13` (story 가 v4 또는 v5 허용 — 최신 v5 채택).

- **`window.location.assign` 추상화**: 인터셉터의 redirect 호출을 `redirectToLogin()` util 로 분리 → jsdom 의 non-writable Location 메서드 우회 + 테스트 mock 용이성 확보. **향후 인터셉터에서 추가 navigation 이 필요해지면 이 util 에 합치는 패턴** 권장.

- **`matchMedia` polyfill 필수**: antd 5 의 `<Grid>` 가 `window.matchMedia` 사용 → jsdom 환경 polyfill 을 `setup.ts` 에 영구 추가. 향후 모든 antd 컴포넌트 테스트의 전제.

- **Orphan 정리**: 기존 `src/api/health.ts` 는 Story 1.1 의 health 호출용이었으나 Story 1.4 의 `App.tsx` 재작성으로 미참조. **CLAUDE.md "본인 변경이 만든 orphan 제거" 원칙으로 삭제**. backend `/api/health` 엔드포인트는 그대로 유지 (Vite proxy 검증 시 200 확인).

- **Story 1.5 (역할별 메인 화면 라우팅) 영향**:
  1. `<HomeView>` 의 환영 메시지는 1.5 의 역할별 리다이렉트 로직으로 **교체 대상**. 1.5 에서 `/` 가 AGENT/ADMIN → `/issues`, FIELD → `/m` 으로 분기.
  2. `<AppLayout>` 헤더는 1.4 가 최소 뼈대 (사용자명/역할/로그아웃) — 1.5 가 본격 네비게이션 바 (메뉴 링크 + 알림 벨 자리) 로 **확장**.
  3. `<RequireAuth>` 는 role-agnostic — 1.5 에서 `<RequireRole roles={[...]}>` 컴포넌트가 추가되어 라우트별 권한 검사. 두 컴포넌트는 공존 가능 (RequireAuth 먼저 → RequireRole 안쪽).
  4. 라우트 fallback `*→/` 은 1.5 에서 `<NotFoundView>` 또는 `<Navigate to="/">` 로 폴리시.
  5. **테스트 인프라 활용**: Vitest + RTL 가 셋업 완료. 1.5 의 RequireRole / 역할별 라우팅 테스트는 동일 패턴 (`MemoryRouter` + `useAuthStore.setState`) 으로 작성 가능.

### Change Log

| Date | Version | Description | Author |
| :--- | :------ | :---------- | :----- |
| 2026-05-18 | 0.1 | Initial draft by SM (Bob) — sourced from PRD epic-1 Story 1.4 + architecture §9.1/9.2.1/9.5/9.8 + Story 1.3 Dev Agent Record carry-overs (백엔드 DTO 스키마, 에러 코드 매핑, JWT alg 무관). | Bob (SM) |
| 2026-05-18 | 0.2 | Implementation complete (James). 12 tasks 완료. 20/20 tests pass (5 files), build 통과, Vite proxy → backend 통합 smoke 통과. Deviations: vitest v4 통일, redirectToLogin util 추출, matchMedia polyfill, health.ts orphan 삭제. | James (Dev) |
